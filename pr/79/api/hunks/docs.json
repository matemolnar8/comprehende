{"hunks":[{"path":"README.md","oldStart":42,"oldLines":7,"newStart":42,"newLines":7,"header":"@@ -42,7 +42,7 @@ This tool is for preventing this cognitive surrender while trying to maintain mo","language":"markdown","lines":[{"kind":"ctx","oldNumber":42,"newNumber":42,"text":""},{"kind":"ctx","oldNumber":43,"newNumber":43,"text":"`pnpm sync:skill` copies the JSON Schema into `skills-next/comprehende/`, pins `npx comprehende@<version>` there, and mirrors that tree into `.agents/skills/comprehende` so agents in this checkout use the next skill. It does not touch `skills/comprehende/`."},{"kind":"ctx","oldNumber":44,"newNumber":44,"text":""},{"kind":"del","oldNumber":45,"newNumber":null,"text":"`comprehende serve` and `comprehende export` share one UI and one git payload. Serve resolves refs to commit SHAs when it starts, then computes those payloads from the objects on each request. Export writes the same JSON (and image bytes) next to the UI so any static file server can host the review."},{"kind":"add","oldNumber":null,"newNumber":45,"text":"`comprehende serve` and `comprehende export` share one UI and one git payload. Serve resolves refs to commit SHAs when it starts, then computes those payloads from the objects on each request. Export writes the same JSON (and image bytes) next to the UI so any static file server can host the review. The URL hash is the UI location (`#overview`, `#group/<id>`, `#unassigned`, `#lockfiles`), so a refresh or a shared link opens the same view in either mode."},{"kind":"ctx","oldNumber":46,"newNumber":46,"text":""},{"kind":"ctx","oldNumber":47,"newNumber":47,"text":"This repository publishes those exports to GitHub Pages. A pull request review lives at `https://matemolnar8.github.io/comprehende/pr/<number>/`. A named site lives at `https://matemolnar8.github.io/comprehende/site/<name>/`. Enable Pages once. In the GitHub repo, open Settings, Pages, Deploy from a branch, branch `gh-pages`, folder `/`. A workflow removes the pull request folder when that pull request closes, and drops sites older than 30 days."},{"kind":"ctx","oldNumber":48,"newNumber":48,"text":""}]},{"path":"src/api/export.test.ts","oldStart":58,"oldLines":6,"newStart":58,"newLines":12,"header":"@@ -58,6 +58,12 @@ describe(\"export static site\", () => {","language":"typescript","lines":[{"kind":"ctx","oldNumber":58,"newNumber":58,"text":"    const frozen = await startStaticSite(outDir, 0);"},{"kind":"ctx","oldNumber":59,"newNumber":59,"text":"    servers.push(frozen.server);"},{"kind":"ctx","oldNumber":60,"newNumber":60,"text":""},{"kind":"add","oldNumber":null,"newNumber":61,"text":"    const liveShell = await fetch(`${live.url}/`);"},{"kind":"add","oldNumber":null,"newNumber":62,"text":"    const frozenShell = await fetch(`${frozen.url}/`);"},{"kind":"add","oldNumber":null,"newNumber":63,"text":"    assert.equal(liveShell.status, 200);"},{"kind":"add","oldNumber":null,"newNumber":64,"text":"    assert.equal(frozenShell.status, 200);"},{"kind":"add","oldNumber":null,"newNumber":65,"text":"    assert.equal(await liveShell.text(), await frozenShell.text());"},{"kind":"add","oldNumber":null,"newNumber":66,"text":""},{"kind":"ctx","oldNumber":61,"newNumber":67,"text":"    const resources = listResources(ctx);"},{"kind":"ctx","oldNumber":62,"newNumber":68,"text":"    assert.ok(resources.length > 4);"},{"kind":"ctx","oldNumber":63,"newNumber":69,"text":""}]}],"files":[{"path":"README.md","kind":"text","status":"modified","patch":"diff --git a/README.md b/README.md\nindex 958518e..79d33cf 100644\n--- a/README.md\n+++ b/README.md\n@@ -42,7 +42,7 @@ This tool is for preventing this cognitive surrender while trying to maintain mo\n \n `pnpm sync:skill` copies the JSON Schema into `skills-next/comprehende/`, pins `npx comprehende@<version>` there, and mirrors that tree into `.agents/skills/comprehende` so agents in this checkout use the next skill. It does not touch `skills/comprehende/`.\n \n-`comprehende serve` and `comprehende export` share one UI and one git payload. Serve resolves refs to commit SHAs when it starts, then computes those payloads from the objects on each request. Export writes the same JSON (and image bytes) next to the UI so any static file server can host the review.\n+`comprehende serve` and `comprehende export` share one UI and one git payload. Serve resolves refs to commit SHAs when it starts, then computes those payloads from the objects on each request. Export writes the same JSON (and image bytes) next to the UI so any static file server can host the review. The URL hash is the UI location (`#overview`, `#group/<id>`, `#unassigned`, `#lockfiles`), so a refresh or a shared link opens the same view in either mode.\n \n This repository publishes those exports to GitHub Pages. A pull request review lives at `https://matemolnar8.github.io/comprehende/pr/<number>/`. A named site lives at `https://matemolnar8.github.io/comprehende/site/<name>/`. Enable Pages once. In the GitHub repo, open Settings, Pages, Deploy from a branch, branch `gh-pages`, folder `/`. A workflow removes the pull request folder when that pull request closes, and drops sites older than 30 days.\n \n","complete":true,"hunks":[{"path":"README.md","oldStart":42,"oldLines":7,"newStart":42,"newLines":7,"header":"@@ -42,7 +42,7 @@ This tool is for preventing this cognitive surrender while trying to maintain mo","language":"markdown","lines":[{"kind":"ctx","oldNumber":42,"newNumber":42,"text":""},{"kind":"ctx","oldNumber":43,"newNumber":43,"text":"`pnpm sync:skill` copies the JSON Schema into `skills-next/comprehende/`, pins `npx comprehende@<version>` there, and mirrors that tree into `.agents/skills/comprehende` so agents in this checkout use the next skill. It does not touch `skills/comprehende/`."},{"kind":"ctx","oldNumber":44,"newNumber":44,"text":""},{"kind":"del","oldNumber":45,"newNumber":null,"text":"`comprehende serve` and `comprehende export` share one UI and one git payload. Serve resolves refs to commit SHAs when it starts, then computes those payloads from the objects on each request. Export writes the same JSON (and image bytes) next to the UI so any static file server can host the review."},{"kind":"add","oldNumber":null,"newNumber":45,"text":"`comprehende serve` and `comprehende export` share one UI and one git payload. Serve resolves refs to commit SHAs when it starts, then computes those payloads from the objects on each request. Export writes the same JSON (and image bytes) next to the UI so any static file server can host the review. The URL hash is the UI location (`#overview`, `#group/<id>`, `#unassigned`, `#lockfiles`), so a refresh or a shared link opens the same view in either mode."},{"kind":"ctx","oldNumber":46,"newNumber":46,"text":""},{"kind":"ctx","oldNumber":47,"newNumber":47,"text":"This repository publishes those exports to GitHub Pages. A pull request review lives at `https://matemolnar8.github.io/comprehende/pr/<number>/`. A named site lives at `https://matemolnar8.github.io/comprehende/site/<name>/`. Enable Pages once. In the GitHub repo, open Settings, Pages, Deploy from a branch, branch `gh-pages`, folder `/`. A workflow removes the pull request folder when that pull request closes, and drops sites older than 30 days."},{"kind":"ctx","oldNumber":48,"newNumber":48,"text":""}]}]},{"path":"src/api/export.test.ts","kind":"text","status":"modified","patch":"diff --git a/src/api/export.test.ts b/src/api/export.test.ts\nindex 341af4d..907d525 100644\n--- a/src/api/export.test.ts\n+++ b/src/api/export.test.ts\n@@ -58,6 +58,12 @@ describe(\"export static site\", () => {\n     const frozen = await startStaticSite(outDir, 0);\n     servers.push(frozen.server);\n \n+    const liveShell = await fetch(`${live.url}/`);\n+    const frozenShell = await fetch(`${frozen.url}/`);\n+    assert.equal(liveShell.status, 200);\n+    assert.equal(frozenShell.status, 200);\n+    assert.equal(await liveShell.text(), await frozenShell.text());\n+\n     const resources = listResources(ctx);\n     assert.ok(resources.length > 4);\n \n","complete":true,"hunks":[{"path":"src/api/export.test.ts","oldStart":58,"oldLines":6,"newStart":58,"newLines":12,"header":"@@ -58,6 +58,12 @@ describe(\"export static site\", () => {","language":"typescript","lines":[{"kind":"ctx","oldNumber":58,"newNumber":58,"text":"    const frozen = await startStaticSite(outDir, 0);"},{"kind":"ctx","oldNumber":59,"newNumber":59,"text":"    servers.push(frozen.server);"},{"kind":"ctx","oldNumber":60,"newNumber":60,"text":""},{"kind":"add","oldNumber":null,"newNumber":61,"text":"    const liveShell = await fetch(`${live.url}/`);"},{"kind":"add","oldNumber":null,"newNumber":62,"text":"    const frozenShell = await fetch(`${frozen.url}/`);"},{"kind":"add","oldNumber":null,"newNumber":63,"text":"    assert.equal(liveShell.status, 200);"},{"kind":"add","oldNumber":null,"newNumber":64,"text":"    assert.equal(frozenShell.status, 200);"},{"kind":"add","oldNumber":null,"newNumber":65,"text":"    assert.equal(await liveShell.text(), await frozenShell.text());"},{"kind":"add","oldNumber":null,"newNumber":66,"text":""},{"kind":"ctx","oldNumber":61,"newNumber":67,"text":"    const resources = listResources(ctx);"},{"kind":"ctx","oldNumber":62,"newNumber":68,"text":"    assert.ok(resources.length > 4);"},{"kind":"ctx","oldNumber":63,"newNumber":69,"text":""}]}]}]}
