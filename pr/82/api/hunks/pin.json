{"hunks":[{"path":"package.json","oldStart":1,"oldLines":6,"newStart":1,"newLines":6,"header":"@@ -1,6 +1,6 @@","language":"json","lines":[{"kind":"ctx","oldNumber":1,"newNumber":1,"text":"{"},{"kind":"ctx","oldNumber":2,"newNumber":2,"text":"  \"name\": \"comprehende\","},{"kind":"del","oldNumber":3,"newNumber":null,"text":"  \"version\": \"0.7.0\","},{"kind":"add","oldNumber":null,"newNumber":3,"text":"  \"version\": \"0.8.0\","},{"kind":"ctx","oldNumber":4,"newNumber":4,"text":"  \"description\": \"Local review assistant that groups git diffs so humans can comprehend AI code changes.\","},{"kind":"ctx","oldNumber":5,"newNumber":5,"text":"  \"type\": \"module\","},{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"  \"bin\": {"}]},{"path":"README.md","oldStart":46,"oldLines":7,"newStart":46,"newLines":7,"header":"@@ -46,7 +46,7 @@ This tool is for preventing this cognitive surrender while trying to maintain mo","language":"markdown","lines":[{"kind":"ctx","oldNumber":46,"newNumber":46,"text":""},{"kind":"ctx","oldNumber":47,"newNumber":47,"text":"This repository publishes those exports to GitHub Pages. A pull request review lives at `https://matemolnar8.github.io/comprehende/pr/<number>/`. A named site lives at `https://matemolnar8.github.io/comprehende/site/<name>/`. Enable Pages once. In the GitHub repo, open Settings, Pages, Deploy from a branch, branch `gh-pages`, folder `/`. A workflow removes the pull request folder when that pull request closes, and drops sites older than 30 days."},{"kind":"ctx","oldNumber":48,"newNumber":48,"text":""},{"kind":"del","oldNumber":49,"newNumber":null,"text":"`pnpm dev` and `pnpm exec` run with this package as cwd, so they only make sense when _this_ repo is the one under review. To review a different project from a checkout, `cd` into it and run `npx comprehende@0.7.0` (or `node /path/to/comprehende/dist/cli/main.js` after `pnpm build`)."},{"kind":"add","oldNumber":null,"newNumber":49,"text":"`pnpm dev` and `pnpm exec` run with this package as cwd, so they only make sense when _this_ repo is the one under review. To review a different project from a checkout, `cd` into it and run `npx comprehende@0.8.0` (or `node /path/to/comprehende/dist/cli/main.js` after `pnpm build`)."},{"kind":"ctx","oldNumber":50,"newNumber":50,"text":""},{"kind":"ctx","oldNumber":51,"newNumber":51,"text":"## Release"},{"kind":"ctx","oldNumber":52,"newNumber":52,"text":""}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":12,"oldLines":14,"newStart":12,"newLines":14,"header":"@@ -12,14 +12,14 @@ Group a git diff into review concerns and serve a local UI. The point is cogniti","language":"markdown","lines":[{"kind":"ctx","oldNumber":12,"newNumber":12,"text":"Run every command inside the repository under review. Cwd is the repo; there is no `--repo` flag. Use the pinned CLI:"},{"kind":"ctx","oldNumber":13,"newNumber":13,"text":""},{"kind":"ctx","oldNumber":14,"newNumber":14,"text":"```sh"},{"kind":"del","oldNumber":15,"newNumber":null,"text":"npx comprehende@0.7.0 <command>"},{"kind":"add","oldNumber":null,"newNumber":15,"text":"npx comprehende@0.8.0 <command>"},{"kind":"ctx","oldNumber":16,"newNumber":16,"text":"```"},{"kind":"ctx","oldNumber":17,"newNumber":17,"text":""},{"kind":"ctx","oldNumber":18,"newNumber":18,"text":"The review document is interpretation only. It holds a title, groups, summaries, and hunk pointers. Do not copy patch text into it. `serve` reads the diff from git."},{"kind":"ctx","oldNumber":19,"newNumber":19,"text":""},{"kind":"ctx","oldNumber":20,"newNumber":20,"text":"## Workflow"},{"kind":"ctx","oldNumber":21,"newNumber":21,"text":""},{"kind":"del","oldNumber":22,"newNumber":null,"text":"1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.7.0`), stop and tell the user. Show this command as an option they can run:"},{"kind":"add","oldNumber":null,"newNumber":22,"text":"1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show this command as an option they can run:"},{"kind":"ctx","oldNumber":23,"newNumber":23,"text":""},{"kind":"ctx","oldNumber":24,"newNumber":24,"text":"   ```sh"},{"kind":"ctx","oldNumber":25,"newNumber":25,"text":"   npx skills update"}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":27,"oldLines":19,"newStart":27,"newLines":19,"header":"@@ -27,19 +27,19 @@ The review document is interpretation only. It holds a title, groups, summaries,","language":"markdown","lines":[{"kind":"ctx","oldNumber":27,"newNumber":27,"text":""},{"kind":"ctx","oldNumber":28,"newNumber":28,"text":"   Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue."},{"kind":"ctx","oldNumber":29,"newNumber":29,"text":"2. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"del","oldNumber":30,"newNumber":null,"text":"3. Run `npx comprehende@0.7.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"add","oldNumber":null,"newNumber":30,"text":"3. Run `npx comprehende@0.8.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"ctx","oldNumber":31,"newNumber":31,"text":"4. Recover the why, write the title, write the what, and compare the sources with the diff. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"ctx","oldNumber":32,"newNumber":32,"text":"5. Group the hunks by review concern, following the Grouping rules. Done when every hunk ref from the index appears in at least one group and every group has its `why`."},{"kind":"ctx","oldNumber":33,"newNumber":33,"text":"6. Write `review.json` in a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries). Shape per [references/review.schema.json](./references/review.schema.json); worked example in [references/example.md](./references/example.md). Copy hunk objects verbatim from the index. Set document `size` from review burden, not `git diff --stat`."},{"kind":"del","oldNumber":34,"newNumber":null,"text":"7. Run `npx comprehende@0.7.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"del","oldNumber":35,"newNumber":null,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.7.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"add","oldNumber":null,"newNumber":34,"text":"7. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"add","oldNumber":null,"newNumber":35,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":36,"newNumber":36,"text":""},{"kind":"ctx","oldNumber":37,"newNumber":37,"text":"## Export"},{"kind":"ctx","oldNumber":38,"newNumber":38,"text":""},{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"Write a static site and put that folder where they asked."},{"kind":"ctx","oldNumber":40,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":41,"text":"```sh"},{"kind":"del","oldNumber":42,"newNumber":null,"text":"npx comprehende@0.7.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"add","oldNumber":null,"newNumber":42,"text":"npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"ctx","oldNumber":43,"newNumber":43,"text":"```"},{"kind":"ctx","oldNumber":44,"newNumber":44,"text":""},{"kind":"ctx","oldNumber":45,"newNumber":45,"text":"`$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is not written yet, finish the Workflow through validate, then export."}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":12,"oldLines":14,"newStart":12,"newLines":14,"header":"@@ -12,14 +12,14 @@ Group a git diff into review concerns and serve a local UI. The point is cogniti","language":"markdown","lines":[{"kind":"ctx","oldNumber":12,"newNumber":12,"text":"Run every command inside the repository under review. Cwd is the repo; there is no `--repo` flag. Use the pinned CLI:"},{"kind":"ctx","oldNumber":13,"newNumber":13,"text":""},{"kind":"ctx","oldNumber":14,"newNumber":14,"text":"```sh"},{"kind":"del","oldNumber":15,"newNumber":null,"text":"npx comprehende@0.7.0 <command>"},{"kind":"add","oldNumber":null,"newNumber":15,"text":"npx comprehende@0.8.0 <command>"},{"kind":"ctx","oldNumber":16,"newNumber":16,"text":"```"},{"kind":"ctx","oldNumber":17,"newNumber":17,"text":""},{"kind":"ctx","oldNumber":18,"newNumber":18,"text":"The review document is interpretation only. It holds a title, groups, summaries, and hunk pointers. Do not copy patch text into it. `serve` reads the diff from git."},{"kind":"ctx","oldNumber":19,"newNumber":19,"text":""},{"kind":"ctx","oldNumber":20,"newNumber":20,"text":"## Workflow"},{"kind":"ctx","oldNumber":21,"newNumber":21,"text":""},{"kind":"del","oldNumber":22,"newNumber":null,"text":"1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.7.0`), stop and tell the user. Show this command as an option they can run:"},{"kind":"add","oldNumber":null,"newNumber":22,"text":"1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show this command as an option they can run:"},{"kind":"ctx","oldNumber":23,"newNumber":23,"text":""},{"kind":"ctx","oldNumber":24,"newNumber":24,"text":"   ```sh"},{"kind":"ctx","oldNumber":25,"newNumber":25,"text":"   npx skills update"}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":27,"oldLines":19,"newStart":27,"newLines":19,"header":"@@ -27,19 +27,19 @@ The review document is interpretation only. It holds a title, groups, summaries,","language":"markdown","lines":[{"kind":"ctx","oldNumber":27,"newNumber":27,"text":""},{"kind":"ctx","oldNumber":28,"newNumber":28,"text":"   Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue."},{"kind":"ctx","oldNumber":29,"newNumber":29,"text":"2. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"del","oldNumber":30,"newNumber":null,"text":"3. Run `npx comprehende@0.7.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"add","oldNumber":null,"newNumber":30,"text":"3. Run `npx comprehende@0.8.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"ctx","oldNumber":31,"newNumber":31,"text":"4. Recover the why, write the title, write the what, and compare the sources with the diff. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"ctx","oldNumber":32,"newNumber":32,"text":"5. Group the hunks by review concern, following the Grouping rules. Done when every hunk ref from the index appears in at least one group and every group has its `why`."},{"kind":"ctx","oldNumber":33,"newNumber":33,"text":"6. Write `review.json` in a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries). Shape per [references/review.schema.json](./references/review.schema.json); worked example in [references/example.md](./references/example.md). Copy hunk objects verbatim from the index. Set document `size` from review burden, not `git diff --stat`."},{"kind":"del","oldNumber":34,"newNumber":null,"text":"7. Run `npx comprehende@0.7.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"del","oldNumber":35,"newNumber":null,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.7.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"add","oldNumber":null,"newNumber":34,"text":"7. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"add","oldNumber":null,"newNumber":35,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":36,"newNumber":36,"text":""},{"kind":"ctx","oldNumber":37,"newNumber":37,"text":"## Export"},{"kind":"ctx","oldNumber":38,"newNumber":38,"text":""},{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"Write a static site and put that folder where they asked."},{"kind":"ctx","oldNumber":40,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":41,"text":"```sh"},{"kind":"del","oldNumber":42,"newNumber":null,"text":"npx comprehende@0.7.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"add","oldNumber":null,"newNumber":42,"text":"npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"ctx","oldNumber":43,"newNumber":43,"text":"```"},{"kind":"ctx","oldNumber":44,"newNumber":44,"text":""},{"kind":"ctx","oldNumber":45,"newNumber":45,"text":"`$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is not written yet, finish the Workflow through validate, then export."}]},{"path":"skills/comprehende/SKILL.md","oldStart":12,"oldLines":14,"newStart":12,"newLines":14,"header":"@@ -12,14 +12,14 @@ Group a git diff into review concerns and serve a local UI. The point is cogniti","language":"markdown","lines":[{"kind":"ctx","oldNumber":12,"newNumber":12,"text":"Run every command inside the repository under review. Cwd is the repo; there is no `--repo` flag. Use the pinned CLI:"},{"kind":"ctx","oldNumber":13,"newNumber":13,"text":""},{"kind":"ctx","oldNumber":14,"newNumber":14,"text":"```sh"},{"kind":"del","oldNumber":15,"newNumber":null,"text":"npx comprehende@0.7.0 <command>"},{"kind":"add","oldNumber":null,"newNumber":15,"text":"npx comprehende@0.8.0 <command>"},{"kind":"ctx","oldNumber":16,"newNumber":16,"text":"```"},{"kind":"ctx","oldNumber":17,"newNumber":17,"text":""},{"kind":"ctx","oldNumber":18,"newNumber":18,"text":"The review document is interpretation only. It holds a title, groups, summaries, and hunk pointers. Do not copy patch text into it. `serve` reads the diff from git."},{"kind":"ctx","oldNumber":19,"newNumber":19,"text":""},{"kind":"ctx","oldNumber":20,"newNumber":20,"text":"## Workflow"},{"kind":"ctx","oldNumber":21,"newNumber":21,"text":""},{"kind":"del","oldNumber":22,"newNumber":null,"text":"1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.7.0`), stop and tell the user. Show this command as an option they can run:"},{"kind":"add","oldNumber":null,"newNumber":22,"text":"1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show this command as an option they can run:"},{"kind":"ctx","oldNumber":23,"newNumber":23,"text":""},{"kind":"ctx","oldNumber":24,"newNumber":24,"text":"   ```sh"},{"kind":"ctx","oldNumber":25,"newNumber":25,"text":"   npx skills update"}]},{"path":"skills/comprehende/SKILL.md","oldStart":27,"oldLines":19,"newStart":27,"newLines":19,"header":"@@ -27,19 +27,19 @@ The review document is interpretation only. It holds a title, groups, summaries,","language":"markdown","lines":[{"kind":"ctx","oldNumber":27,"newNumber":27,"text":""},{"kind":"ctx","oldNumber":28,"newNumber":28,"text":"   Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue."},{"kind":"ctx","oldNumber":29,"newNumber":29,"text":"2. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"del","oldNumber":30,"newNumber":null,"text":"3. Run `npx comprehende@0.7.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"del","oldNumber":31,"newNumber":null,"text":"4. Recover the why, write the title, and write the what. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), and document `why` (only when a source names the motive). Summaries come from the code, not the log."},{"kind":"add","oldNumber":null,"newNumber":30,"text":"3. Run `npx comprehende@0.8.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"add","oldNumber":null,"newNumber":31,"text":"4. Recover the why, write the title, write the what, and compare the sources with the diff. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"ctx","oldNumber":32,"newNumber":32,"text":"5. Group the hunks by review concern, following the Grouping rules. Done when every hunk ref from the index appears in at least one group and every group has its `why`."},{"kind":"ctx","oldNumber":33,"newNumber":33,"text":"6. Write `review.json` in a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries). Shape per [references/review.schema.json](./references/review.schema.json); worked example in [references/example.md](./references/example.md). Copy hunk objects verbatim from the index. Set document `size` from review burden, not `git diff --stat`."},{"kind":"del","oldNumber":34,"newNumber":null,"text":"7. Run `npx comprehende@0.7.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"del","oldNumber":35,"newNumber":null,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.7.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"add","oldNumber":null,"newNumber":34,"text":"7. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"add","oldNumber":null,"newNumber":35,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":36,"newNumber":36,"text":""},{"kind":"ctx","oldNumber":37,"newNumber":37,"text":"## Export"},{"kind":"ctx","oldNumber":38,"newNumber":38,"text":""},{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"Write a static site and put that folder where they asked."},{"kind":"ctx","oldNumber":40,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":41,"text":"```sh"},{"kind":"del","oldNumber":42,"newNumber":null,"text":"npx comprehende@0.7.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"add","oldNumber":null,"newNumber":42,"text":"npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"ctx","oldNumber":43,"newNumber":43,"text":"```"},{"kind":"ctx","oldNumber":44,"newNumber":44,"text":""},{"kind":"ctx","oldNumber":45,"newNumber":45,"text":"`$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is not written yet, finish the Workflow through validate, then export."}]}],"files":[{"path":"package.json","kind":"text","status":"modified","patch":"diff --git a/package.json b/package.json\nindex b486510..21289ed 100644\n--- a/package.json\n+++ b/package.json\n@@ -1,6 +1,6 @@\n {\n   \"name\": \"comprehende\",\n-  \"version\": \"0.7.0\",\n+  \"version\": \"0.8.0\",\n   \"description\": \"Local review assistant that groups git diffs so humans can comprehend AI code changes.\",\n   \"type\": \"module\",\n   \"bin\": {\n","complete":true,"hunks":[{"path":"package.json","oldStart":1,"oldLines":6,"newStart":1,"newLines":6,"header":"@@ -1,6 +1,6 @@","language":"json","lines":[{"kind":"ctx","oldNumber":1,"newNumber":1,"text":"{"},{"kind":"ctx","oldNumber":2,"newNumber":2,"text":"  \"name\": \"comprehende\","},{"kind":"del","oldNumber":3,"newNumber":null,"text":"  \"version\": \"0.7.0\","},{"kind":"add","oldNumber":null,"newNumber":3,"text":"  \"version\": \"0.8.0\","},{"kind":"ctx","oldNumber":4,"newNumber":4,"text":"  \"description\": \"Local review assistant that groups git diffs so humans can comprehend AI code changes.\","},{"kind":"ctx","oldNumber":5,"newNumber":5,"text":"  \"type\": \"module\","},{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"  \"bin\": {"}]}]},{"path":"README.md","kind":"text","status":"modified","patch":"diff --git a/README.md b/README.md\nindex 79d33cf..d7f1334 100644\n--- a/README.md\n+++ b/README.md\n@@ -46,7 +46,7 @@ This tool is for preventing this cognitive surrender while trying to maintain mo\n \n This repository publishes those exports to GitHub Pages. A pull request review lives at `https://matemolnar8.github.io/comprehende/pr/<number>/`. A named site lives at `https://matemolnar8.github.io/comprehende/site/<name>/`. Enable Pages once. In the GitHub repo, open Settings, Pages, Deploy from a branch, branch `gh-pages`, folder `/`. A workflow removes the pull request folder when that pull request closes, and drops sites older than 30 days.\n \n-`pnpm dev` and `pnpm exec` run with this package as cwd, so they only make sense when _this_ repo is the one under review. To review a different project from a checkout, `cd` into it and run `npx comprehende@0.7.0` (or `node /path/to/comprehende/dist/cli/main.js` after `pnpm build`).\n+`pnpm dev` and `pnpm exec` run with this package as cwd, so they only make sense when _this_ repo is the one under review. To review a different project from a checkout, `cd` into it and run `npx comprehende@0.8.0` (or `node /path/to/comprehende/dist/cli/main.js` after `pnpm build`).\n \n ## Release\n \n","complete":true,"hunks":[{"path":"README.md","oldStart":46,"oldLines":7,"newStart":46,"newLines":7,"header":"@@ -46,7 +46,7 @@ This tool is for preventing this cognitive surrender while trying to maintain mo","language":"markdown","lines":[{"kind":"ctx","oldNumber":46,"newNumber":46,"text":""},{"kind":"ctx","oldNumber":47,"newNumber":47,"text":"This repository publishes those exports to GitHub Pages. A pull request review lives at `https://matemolnar8.github.io/comprehende/pr/<number>/`. A named site lives at `https://matemolnar8.github.io/comprehende/site/<name>/`. Enable Pages once. In the GitHub repo, open Settings, Pages, Deploy from a branch, branch `gh-pages`, folder `/`. A workflow removes the pull request folder when that pull request closes, and drops sites older than 30 days."},{"kind":"ctx","oldNumber":48,"newNumber":48,"text":""},{"kind":"del","oldNumber":49,"newNumber":null,"text":"`pnpm dev` and `pnpm exec` run with this package as cwd, so they only make sense when _this_ repo is the one under review. To review a different project from a checkout, `cd` into it and run `npx comprehende@0.7.0` (or `node /path/to/comprehende/dist/cli/main.js` after `pnpm build`)."},{"kind":"add","oldNumber":null,"newNumber":49,"text":"`pnpm dev` and `pnpm exec` run with this package as cwd, so they only make sense when _this_ repo is the one under review. To review a different project from a checkout, `cd` into it and run `npx comprehende@0.8.0` (or `node /path/to/comprehende/dist/cli/main.js` after `pnpm build`)."},{"kind":"ctx","oldNumber":50,"newNumber":50,"text":""},{"kind":"ctx","oldNumber":51,"newNumber":51,"text":"## Release"},{"kind":"ctx","oldNumber":52,"newNumber":52,"text":""}]}]},{"path":".agents/skills/comprehende/SKILL.md","kind":"text","status":"modified","patch":"diff --git a/.agents/skills/comprehende/SKILL.md b/.agents/skills/comprehende/SKILL.md\nindex 4115a15..c52468d 100644\n--- a/.agents/skills/comprehende/SKILL.md\n+++ b/.agents/skills/comprehende/SKILL.md\n@@ -12,14 +12,14 @@ Group a git diff into review concerns and serve a local UI. The point is cogniti\n Run every command inside the repository under review. Cwd is the repo; there is no `--repo` flag. Use the pinned CLI:\n \n ```sh\n-npx comprehende@0.7.0 <command>\n+npx comprehende@0.8.0 <command>\n ```\n \n The review document is interpretation only. It holds a title, groups, summaries, and hunk pointers. Do not copy patch text into it. `serve` reads the diff from git.\n \n ## Workflow\n \n-1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.7.0`), stop and tell the user. Show this command as an option they can run:\n+1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show this command as an option they can run:\n \n    ```sh\n    npx skills update\n@@ -27,19 +27,19 @@ The review document is interpretation only. It holds a title, groups, summaries,\n \n    Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue.\n 2. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref.\n-3. Run `npx comprehende@0.7.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content.\n+3. Run `npx comprehende@0.8.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content.\n 4. Recover the why, write the title, write the what, and compare the sources with the diff. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet.\n 5. Group the hunks by review concern, following the Grouping rules. Done when every hunk ref from the index appears in at least one group and every group has its `why`.\n 6. Write `review.json` in a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries). Shape per [references/review.schema.json](./references/review.schema.json); worked example in [references/example.md](./references/example.md). Copy hunk objects verbatim from the index. Set document `size` from review burden, not `git diff --stat`.\n-7. Run `npx comprehende@0.7.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0.\n-8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.7.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n+7. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0.\n+8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n \n ## Export\n \n Write a static site and put that folder where they asked.\n \n ```sh\n-npx comprehende@0.7.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\"\n+npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\"\n ```\n \n `$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is not written yet, finish the Workflow through validate, then export.\n","complete":true,"hunks":[{"path":".agents/skills/comprehende/SKILL.md","oldStart":12,"oldLines":14,"newStart":12,"newLines":14,"header":"@@ -12,14 +12,14 @@ Group a git diff into review concerns and serve a local UI. The point is cogniti","language":"markdown","lines":[{"kind":"ctx","oldNumber":12,"newNumber":12,"text":"Run every command inside the repository under review. Cwd is the repo; there is no `--repo` flag. Use the pinned CLI:"},{"kind":"ctx","oldNumber":13,"newNumber":13,"text":""},{"kind":"ctx","oldNumber":14,"newNumber":14,"text":"```sh"},{"kind":"del","oldNumber":15,"newNumber":null,"text":"npx comprehende@0.7.0 <command>"},{"kind":"add","oldNumber":null,"newNumber":15,"text":"npx comprehende@0.8.0 <command>"},{"kind":"ctx","oldNumber":16,"newNumber":16,"text":"```"},{"kind":"ctx","oldNumber":17,"newNumber":17,"text":""},{"kind":"ctx","oldNumber":18,"newNumber":18,"text":"The review document is interpretation only. It holds a title, groups, summaries, and hunk pointers. Do not copy patch text into it. `serve` reads the diff from git."},{"kind":"ctx","oldNumber":19,"newNumber":19,"text":""},{"kind":"ctx","oldNumber":20,"newNumber":20,"text":"## Workflow"},{"kind":"ctx","oldNumber":21,"newNumber":21,"text":""},{"kind":"del","oldNumber":22,"newNumber":null,"text":"1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.7.0`), stop and tell the user. Show this command as an option they can run:"},{"kind":"add","oldNumber":null,"newNumber":22,"text":"1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show this command as an option they can run:"},{"kind":"ctx","oldNumber":23,"newNumber":23,"text":""},{"kind":"ctx","oldNumber":24,"newNumber":24,"text":"   ```sh"},{"kind":"ctx","oldNumber":25,"newNumber":25,"text":"   npx skills update"}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":27,"oldLines":19,"newStart":27,"newLines":19,"header":"@@ -27,19 +27,19 @@ The review document is interpretation only. It holds a title, groups, summaries,","language":"markdown","lines":[{"kind":"ctx","oldNumber":27,"newNumber":27,"text":""},{"kind":"ctx","oldNumber":28,"newNumber":28,"text":"   Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue."},{"kind":"ctx","oldNumber":29,"newNumber":29,"text":"2. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"del","oldNumber":30,"newNumber":null,"text":"3. Run `npx comprehende@0.7.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"add","oldNumber":null,"newNumber":30,"text":"3. Run `npx comprehende@0.8.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"ctx","oldNumber":31,"newNumber":31,"text":"4. Recover the why, write the title, write the what, and compare the sources with the diff. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"ctx","oldNumber":32,"newNumber":32,"text":"5. Group the hunks by review concern, following the Grouping rules. Done when every hunk ref from the index appears in at least one group and every group has its `why`."},{"kind":"ctx","oldNumber":33,"newNumber":33,"text":"6. Write `review.json` in a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries). Shape per [references/review.schema.json](./references/review.schema.json); worked example in [references/example.md](./references/example.md). Copy hunk objects verbatim from the index. Set document `size` from review burden, not `git diff --stat`."},{"kind":"del","oldNumber":34,"newNumber":null,"text":"7. Run `npx comprehende@0.7.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"del","oldNumber":35,"newNumber":null,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.7.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"add","oldNumber":null,"newNumber":34,"text":"7. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"add","oldNumber":null,"newNumber":35,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":36,"newNumber":36,"text":""},{"kind":"ctx","oldNumber":37,"newNumber":37,"text":"## Export"},{"kind":"ctx","oldNumber":38,"newNumber":38,"text":""},{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"Write a static site and put that folder where they asked."},{"kind":"ctx","oldNumber":40,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":41,"text":"```sh"},{"kind":"del","oldNumber":42,"newNumber":null,"text":"npx comprehende@0.7.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"add","oldNumber":null,"newNumber":42,"text":"npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"ctx","oldNumber":43,"newNumber":43,"text":"```"},{"kind":"ctx","oldNumber":44,"newNumber":44,"text":""},{"kind":"ctx","oldNumber":45,"newNumber":45,"text":"`$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is not written yet, finish the Workflow through validate, then export."}]}]},{"path":"skills-next/comprehende/SKILL.md","kind":"text","status":"modified","patch":"diff --git a/skills-next/comprehende/SKILL.md b/skills-next/comprehende/SKILL.md\nindex 4115a15..c52468d 100644\n--- a/skills-next/comprehende/SKILL.md\n+++ b/skills-next/comprehende/SKILL.md\n@@ -12,14 +12,14 @@ Group a git diff into review concerns and serve a local UI. The point is cogniti\n Run every command inside the repository under review. Cwd is the repo; there is no `--repo` flag. Use the pinned CLI:\n \n ```sh\n-npx comprehende@0.7.0 <command>\n+npx comprehende@0.8.0 <command>\n ```\n \n The review document is interpretation only. It holds a title, groups, summaries, and hunk pointers. Do not copy patch text into it. `serve` reads the diff from git.\n \n ## Workflow\n \n-1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.7.0`), stop and tell the user. Show this command as an option they can run:\n+1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show this command as an option they can run:\n \n    ```sh\n    npx skills update\n@@ -27,19 +27,19 @@ The review document is interpretation only. It holds a title, groups, summaries,\n \n    Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue.\n 2. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref.\n-3. Run `npx comprehende@0.7.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content.\n+3. Run `npx comprehende@0.8.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content.\n 4. Recover the why, write the title, write the what, and compare the sources with the diff. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet.\n 5. Group the hunks by review concern, following the Grouping rules. Done when every hunk ref from the index appears in at least one group and every group has its `why`.\n 6. Write `review.json` in a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries). Shape per [references/review.schema.json](./references/review.schema.json); worked example in [references/example.md](./references/example.md). Copy hunk objects verbatim from the index. Set document `size` from review burden, not `git diff --stat`.\n-7. Run `npx comprehende@0.7.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0.\n-8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.7.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n+7. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0.\n+8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n \n ## Export\n \n Write a static site and put that folder where they asked.\n \n ```sh\n-npx comprehende@0.7.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\"\n+npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\"\n ```\n \n `$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is not written yet, finish the Workflow through validate, then export.\n","complete":true,"hunks":[{"path":"skills-next/comprehende/SKILL.md","oldStart":12,"oldLines":14,"newStart":12,"newLines":14,"header":"@@ -12,14 +12,14 @@ Group a git diff into review concerns and serve a local UI. The point is cogniti","language":"markdown","lines":[{"kind":"ctx","oldNumber":12,"newNumber":12,"text":"Run every command inside the repository under review. Cwd is the repo; there is no `--repo` flag. Use the pinned CLI:"},{"kind":"ctx","oldNumber":13,"newNumber":13,"text":""},{"kind":"ctx","oldNumber":14,"newNumber":14,"text":"```sh"},{"kind":"del","oldNumber":15,"newNumber":null,"text":"npx comprehende@0.7.0 <command>"},{"kind":"add","oldNumber":null,"newNumber":15,"text":"npx comprehende@0.8.0 <command>"},{"kind":"ctx","oldNumber":16,"newNumber":16,"text":"```"},{"kind":"ctx","oldNumber":17,"newNumber":17,"text":""},{"kind":"ctx","oldNumber":18,"newNumber":18,"text":"The review document is interpretation only. It holds a title, groups, summaries, and hunk pointers. Do not copy patch text into it. `serve` reads the diff from git."},{"kind":"ctx","oldNumber":19,"newNumber":19,"text":""},{"kind":"ctx","oldNumber":20,"newNumber":20,"text":"## Workflow"},{"kind":"ctx","oldNumber":21,"newNumber":21,"text":""},{"kind":"del","oldNumber":22,"newNumber":null,"text":"1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.7.0`), stop and tell the user. Show this command as an option they can run:"},{"kind":"add","oldNumber":null,"newNumber":22,"text":"1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show this command as an option they can run:"},{"kind":"ctx","oldNumber":23,"newNumber":23,"text":""},{"kind":"ctx","oldNumber":24,"newNumber":24,"text":"   ```sh"},{"kind":"ctx","oldNumber":25,"newNumber":25,"text":"   npx skills update"}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":27,"oldLines":19,"newStart":27,"newLines":19,"header":"@@ -27,19 +27,19 @@ The review document is interpretation only. It holds a title, groups, summaries,","language":"markdown","lines":[{"kind":"ctx","oldNumber":27,"newNumber":27,"text":""},{"kind":"ctx","oldNumber":28,"newNumber":28,"text":"   Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue."},{"kind":"ctx","oldNumber":29,"newNumber":29,"text":"2. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"del","oldNumber":30,"newNumber":null,"text":"3. Run `npx comprehende@0.7.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"add","oldNumber":null,"newNumber":30,"text":"3. Run `npx comprehende@0.8.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"ctx","oldNumber":31,"newNumber":31,"text":"4. Recover the why, write the title, write the what, and compare the sources with the diff. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"ctx","oldNumber":32,"newNumber":32,"text":"5. Group the hunks by review concern, following the Grouping rules. Done when every hunk ref from the index appears in at least one group and every group has its `why`."},{"kind":"ctx","oldNumber":33,"newNumber":33,"text":"6. Write `review.json` in a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries). Shape per [references/review.schema.json](./references/review.schema.json); worked example in [references/example.md](./references/example.md). Copy hunk objects verbatim from the index. Set document `size` from review burden, not `git diff --stat`."},{"kind":"del","oldNumber":34,"newNumber":null,"text":"7. Run `npx comprehende@0.7.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"del","oldNumber":35,"newNumber":null,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.7.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"add","oldNumber":null,"newNumber":34,"text":"7. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"add","oldNumber":null,"newNumber":35,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":36,"newNumber":36,"text":""},{"kind":"ctx","oldNumber":37,"newNumber":37,"text":"## Export"},{"kind":"ctx","oldNumber":38,"newNumber":38,"text":""},{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"Write a static site and put that folder where they asked."},{"kind":"ctx","oldNumber":40,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":41,"text":"```sh"},{"kind":"del","oldNumber":42,"newNumber":null,"text":"npx comprehende@0.7.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"add","oldNumber":null,"newNumber":42,"text":"npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"ctx","oldNumber":43,"newNumber":43,"text":"```"},{"kind":"ctx","oldNumber":44,"newNumber":44,"text":""},{"kind":"ctx","oldNumber":45,"newNumber":45,"text":"`$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is not written yet, finish the Workflow through validate, then export."}]}]},{"path":"skills/comprehende/SKILL.md","kind":"text","status":"modified","patch":"diff --git a/skills/comprehende/SKILL.md b/skills/comprehende/SKILL.md\nindex 0561b54..c52468d 100644\n--- a/skills/comprehende/SKILL.md\n+++ b/skills/comprehende/SKILL.md\n@@ -12,14 +12,14 @@ Group a git diff into review concerns and serve a local UI. The point is cogniti\n Run every command inside the repository under review. Cwd is the repo; there is no `--repo` flag. Use the pinned CLI:\n \n ```sh\n-npx comprehende@0.7.0 <command>\n+npx comprehende@0.8.0 <command>\n ```\n \n The review document is interpretation only. It holds a title, groups, summaries, and hunk pointers. Do not copy patch text into it. `serve` reads the diff from git.\n \n ## Workflow\n \n-1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.7.0`), stop and tell the user. Show this command as an option they can run:\n+1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show this command as an option they can run:\n \n    ```sh\n    npx skills update\n@@ -27,19 +27,19 @@ The review document is interpretation only. It holds a title, groups, summaries,\n \n    Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue.\n 2. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref.\n-3. Run `npx comprehende@0.7.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content.\n-4. Recover the why, write the title, and write the what. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), and document `why` (only when a source names the motive). Summaries come from the code, not the log.\n+3. Run `npx comprehende@0.8.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content.\n+4. Recover the why, write the title, write the what, and compare the sources with the diff. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet.\n 5. Group the hunks by review concern, following the Grouping rules. Done when every hunk ref from the index appears in at least one group and every group has its `why`.\n 6. Write `review.json` in a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries). Shape per [references/review.schema.json](./references/review.schema.json); worked example in [references/example.md](./references/example.md). Copy hunk objects verbatim from the index. Set document `size` from review burden, not `git diff --stat`.\n-7. Run `npx comprehende@0.7.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0.\n-8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.7.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n+7. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0.\n+8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n \n ## Export\n \n Write a static site and put that folder where they asked.\n \n ```sh\n-npx comprehende@0.7.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\"\n+npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\"\n ```\n \n `$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is not written yet, finish the Workflow through validate, then export.\n","complete":false,"hunks":[{"path":"skills/comprehende/SKILL.md","oldStart":12,"oldLines":14,"newStart":12,"newLines":14,"header":"@@ -12,14 +12,14 @@ Group a git diff into review concerns and serve a local UI. The point is cogniti","language":"markdown","lines":[{"kind":"ctx","oldNumber":12,"newNumber":12,"text":"Run every command inside the repository under review. Cwd is the repo; there is no `--repo` flag. Use the pinned CLI:"},{"kind":"ctx","oldNumber":13,"newNumber":13,"text":""},{"kind":"ctx","oldNumber":14,"newNumber":14,"text":"```sh"},{"kind":"del","oldNumber":15,"newNumber":null,"text":"npx comprehende@0.7.0 <command>"},{"kind":"add","oldNumber":null,"newNumber":15,"text":"npx comprehende@0.8.0 <command>"},{"kind":"ctx","oldNumber":16,"newNumber":16,"text":"```"},{"kind":"ctx","oldNumber":17,"newNumber":17,"text":""},{"kind":"ctx","oldNumber":18,"newNumber":18,"text":"The review document is interpretation only. It holds a title, groups, summaries, and hunk pointers. Do not copy patch text into it. `serve` reads the diff from git."},{"kind":"ctx","oldNumber":19,"newNumber":19,"text":""},{"kind":"ctx","oldNumber":20,"newNumber":20,"text":"## Workflow"},{"kind":"ctx","oldNumber":21,"newNumber":21,"text":""},{"kind":"del","oldNumber":22,"newNumber":null,"text":"1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.7.0`), stop and tell the user. Show this command as an option they can run:"},{"kind":"add","oldNumber":null,"newNumber":22,"text":"1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show this command as an option they can run:"},{"kind":"ctx","oldNumber":23,"newNumber":23,"text":""},{"kind":"ctx","oldNumber":24,"newNumber":24,"text":"   ```sh"},{"kind":"ctx","oldNumber":25,"newNumber":25,"text":"   npx skills update"}]},{"path":"skills/comprehende/SKILL.md","oldStart":27,"oldLines":19,"newStart":27,"newLines":19,"header":"@@ -27,19 +27,19 @@ The review document is interpretation only. It holds a title, groups, summaries,","language":"markdown","lines":[{"kind":"ctx","oldNumber":27,"newNumber":27,"text":""},{"kind":"ctx","oldNumber":28,"newNumber":28,"text":"   Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue."},{"kind":"ctx","oldNumber":29,"newNumber":29,"text":"2. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"del","oldNumber":30,"newNumber":null,"text":"3. Run `npx comprehende@0.7.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"del","oldNumber":31,"newNumber":null,"text":"4. Recover the why, write the title, and write the what. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), and document `why` (only when a source names the motive). Summaries come from the code, not the log."},{"kind":"add","oldNumber":null,"newNumber":30,"text":"3. Run `npx comprehende@0.8.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"add","oldNumber":null,"newNumber":31,"text":"4. Recover the why, write the title, write the what, and compare the sources with the diff. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"ctx","oldNumber":32,"newNumber":32,"text":"5. Group the hunks by review concern, following the Grouping rules. Done when every hunk ref from the index appears in at least one group and every group has its `why`."},{"kind":"ctx","oldNumber":33,"newNumber":33,"text":"6. Write `review.json` in a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries). Shape per [references/review.schema.json](./references/review.schema.json); worked example in [references/example.md](./references/example.md). Copy hunk objects verbatim from the index. Set document `size` from review burden, not `git diff --stat`."},{"kind":"del","oldNumber":34,"newNumber":null,"text":"7. Run `npx comprehende@0.7.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"del","oldNumber":35,"newNumber":null,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.7.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"add","oldNumber":null,"newNumber":34,"text":"7. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"add","oldNumber":null,"newNumber":35,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":36,"newNumber":36,"text":""},{"kind":"ctx","oldNumber":37,"newNumber":37,"text":"## Export"},{"kind":"ctx","oldNumber":38,"newNumber":38,"text":""},{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"Write a static site and put that folder where they asked."},{"kind":"ctx","oldNumber":40,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":41,"text":"```sh"},{"kind":"del","oldNumber":42,"newNumber":null,"text":"npx comprehende@0.7.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"add","oldNumber":null,"newNumber":42,"text":"npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"ctx","oldNumber":43,"newNumber":43,"text":"```"},{"kind":"ctx","oldNumber":44,"newNumber":44,"text":""},{"kind":"ctx","oldNumber":45,"newNumber":45,"text":"`$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is not written yet, finish the Workflow through validate, then export."}]}]}]}
