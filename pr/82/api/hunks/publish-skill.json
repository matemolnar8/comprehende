{"hunks":[{"path":"skills/comprehende/SKILL.md","oldStart":27,"oldLines":19,"newStart":27,"newLines":19,"header":"@@ -27,19 +27,19 @@ The review document is interpretation only. It holds a title, groups, summaries,","language":"markdown","lines":[{"kind":"ctx","oldNumber":27,"newNumber":27,"text":""},{"kind":"ctx","oldNumber":28,"newNumber":28,"text":"   Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue."},{"kind":"ctx","oldNumber":29,"newNumber":29,"text":"2. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"del","oldNumber":30,"newNumber":null,"text":"3. Run `npx comprehende@0.7.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"del","oldNumber":31,"newNumber":null,"text":"4. Recover the why, write the title, and write the what. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), and document `why` (only when a source names the motive). Summaries come from the code, not the log."},{"kind":"add","oldNumber":null,"newNumber":30,"text":"3. Run `npx comprehende@0.8.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"add","oldNumber":null,"newNumber":31,"text":"4. Recover the why, write the title, write the what, and compare the sources with the diff. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"ctx","oldNumber":32,"newNumber":32,"text":"5. Group the hunks by review concern, following the Grouping rules. Done when every hunk ref from the index appears in at least one group and every group has its `why`."},{"kind":"ctx","oldNumber":33,"newNumber":33,"text":"6. Write `review.json` in a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries). Shape per [references/review.schema.json](./references/review.schema.json); worked example in [references/example.md](./references/example.md). Copy hunk objects verbatim from the index. Set document `size` from review burden, not `git diff --stat`."},{"kind":"del","oldNumber":34,"newNumber":null,"text":"7. Run `npx comprehende@0.7.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"del","oldNumber":35,"newNumber":null,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.7.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"add","oldNumber":null,"newNumber":34,"text":"7. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"add","oldNumber":null,"newNumber":35,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":36,"newNumber":36,"text":""},{"kind":"ctx","oldNumber":37,"newNumber":37,"text":"## Export"},{"kind":"ctx","oldNumber":38,"newNumber":38,"text":""},{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"Write a static site and put that folder where they asked."},{"kind":"ctx","oldNumber":40,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":41,"text":"```sh"},{"kind":"del","oldNumber":42,"newNumber":null,"text":"npx comprehende@0.7.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"add","oldNumber":null,"newNumber":42,"text":"npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"ctx","oldNumber":43,"newNumber":43,"text":"```"},{"kind":"ctx","oldNumber":44,"newNumber":44,"text":""},{"kind":"ctx","oldNumber":45,"newNumber":45,"text":"`$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is not written yet, finish the Workflow through validate, then export."}]},{"path":"skills/comprehende/SKILL.md","oldStart":91,"oldLines":9,"newStart":91,"newLines":11,"header":"@@ -91,9 +91,11 @@ Hunk identity is `(path, oldStart, newStart)` plus `oldPath` when renamed. Copy","language":"markdown","lines":[{"kind":"ctx","oldNumber":91,"newNumber":91,"text":""},{"kind":"ctx","oldNumber":92,"newNumber":92,"text":"## lookFor"},{"kind":"ctx","oldNumber":93,"newNumber":93,"text":""},{"kind":"del","oldNumber":94,"newNumber":null,"text":"`lookFor` is for what the live diff does not make obvious. Empty is correct for a rename, a formatting change, or a wording-only docs group; leave it empty rather than fill it."},{"kind":"add","oldNumber":null,"newNumber":94,"text":"`lookFor` is for what the live diff does not make obvious. Each bullet is one inspectable claim: optionally a short risk tag (Subtle, Breaking, Race, Perf), then one sentence. The bullet states the claim; the human checks it against the live diff and keeps the verdict. Pseudocode, diagrams, collapsed diffs, commit lists, hunk counts, and pass/fail marks stay out."},{"kind":"ctx","oldNumber":95,"newNumber":95,"text":""},{"kind":"del","oldNumber":96,"newNumber":null,"text":"Each bullet is one inspectable claim: optionally a short risk tag (Subtle, Breaking, Race, Perf), then one sentence. When a behavior change hides in the hunk, a predicted trace earns a bullet: a small realistic input, where the old and new paths diverge, and the observable result. The human checks the claim against the live diff; pseudocode, diagrams, collapsed diffs, commit lists, and hunk counts stay out."},{"kind":"add","oldNumber":null,"newNumber":96,"text":"Group `lookFor` holds claims that live in those hunks. When a behavior change hides in the hunk, a predicted trace earns a bullet: a small realistic input, where the old and new paths diverge, and the observable result. Empty is correct for a rename, a formatting change, or a wording-only docs group; leave it empty rather than fill it."},{"kind":"add","oldNumber":null,"newNumber":97,"text":""},{"kind":"add","oldNumber":null,"newNumber":98,"text":"Document `lookFor` holds claims about the whole change, and work a source names that no hunk does. Compare each source with the diff. When a ticket, request, comment, or transcript names work, the bullet says what it asks for and what the diff does about it. Cite the source with `[text](source:s1)`; the id goes in `sources`, and `validate` fails on an unknown id. Omit the field when no source names expected work, or when the bullet would only restate document `summary`."},{"kind":"ctx","oldNumber":97,"newNumber":99,"text":""},{"kind":"ctx","oldNumber":98,"newNumber":100,"text":"## Write the prose"},{"kind":"ctx","oldNumber":99,"newNumber":101,"text":""}]},{"path":"skills/comprehende/references/example.md","oldStart":6,"oldLines":6,"newStart":6,"newLines":7,"header":"@@ -6,6 +6,7 @@ Pointers and prose only. The `@@` numbers must come from `comprehende index`, no","language":"markdown","lines":[{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"- `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request."},{"kind":"ctx","oldNumber":7,"newNumber":7,"text":"- Ticket #12 names why this work exists, so document `why` is present. Document `title` keeps the ticket title. Document `summary` names both stories. The why cites the ticket with `[#12](source:s1)`."},{"kind":"ctx","oldNumber":8,"newNumber":8,"text":"- `login` `summary` names how those hunks meet. `login` `lookFor` is a predicted trace. `docs` has no `lookFor`."},{"kind":"add","oldNumber":null,"newNumber":9,"text":"- Document `lookFor` compares ticket #12 with the diff: work the ticket asks for that no hunk does, and one claim about the whole change. Both cite `[#12](source:s1)`. Group `lookFor` stays inside its hunks."},{"kind":"ctx","oldNumber":9,"newNumber":10,"text":""},{"kind":"ctx","oldNumber":10,"newNumber":11,"text":"```json"},{"kind":"ctx","oldNumber":11,"newNumber":12,"text":"{"}]},{"path":"skills/comprehende/references/example.md","oldStart":29,"oldLines":6,"newStart":30,"newLines":10,"header":"@@ -29,6 +30,10 @@ Pointers and prose only. The `@@` numbers must come from `comprehende index`, no","language":"markdown","lines":[{"kind":"ctx","oldNumber":29,"newNumber":30,"text":"      \"part\": \"Session cookie\""},{"kind":"ctx","oldNumber":30,"newNumber":31,"text":"    }"},{"kind":"ctx","oldNumber":31,"newNumber":32,"text":"  ],"},{"kind":"add","oldNumber":null,"newNumber":33,"text":"  \"lookFor\": ["},{"kind":"add","oldNumber":null,"newNumber":34,"text":"    \"[#12](source:s1) also asks logout to clear the session cookie. No hunk touches `src/api/logout.ts`.\","},{"kind":"add","oldNumber":null,"newNumber":35,"text":"    \"Subtle. [#12](source:s1) wants sessions that client scripts cannot read. Sessions issued before this change keep their old cookie until it expires; no hunk rotates them.\""},{"kind":"add","oldNumber":null,"newNumber":36,"text":"  ],"},{"kind":"ctx","oldNumber":32,"newNumber":37,"text":"  \"groups\": ["},{"kind":"ctx","oldNumber":33,"newNumber":38,"text":"    {"},{"kind":"ctx","oldNumber":34,"newNumber":39,"text":"      \"id\": \"cookie\","}]},{"path":"skills/comprehende/references/review.schema.json","oldStart":62,"oldLines":6,"newStart":62,"newLines":14,"header":"@@ -62,6 +62,14 @@","language":"json","lines":[{"kind":"ctx","oldNumber":62,"newNumber":62,"text":"        \"$ref\": \"#/$defs/source\""},{"kind":"ctx","oldNumber":63,"newNumber":63,"text":"      }"},{"kind":"ctx","oldNumber":64,"newNumber":64,"text":"    },"},{"kind":"add","oldNumber":null,"newNumber":65,"text":"    \"lookFor\": {"},{"kind":"add","oldNumber":null,"newNumber":66,"text":"      \"description\": \"Whole-change and missing-work claims. Cite the source. Do not store a pass/fail.\","},{"kind":"add","oldNumber":null,"newNumber":67,"text":"      \"type\": \"array\","},{"kind":"add","oldNumber":null,"newNumber":68,"text":"      \"items\": {"},{"kind":"add","oldNumber":null,"newNumber":69,"text":"        \"type\": \"string\","},{"kind":"add","oldNumber":null,"newNumber":70,"text":"        \"minLength\": 1"},{"kind":"add","oldNumber":null,"newNumber":71,"text":"      }"},{"kind":"add","oldNumber":null,"newNumber":72,"text":"    },"},{"kind":"ctx","oldNumber":65,"newNumber":73,"text":"    \"groups\": {"},{"kind":"ctx","oldNumber":66,"newNumber":74,"text":"      \"type\": \"array\","},{"kind":"ctx","oldNumber":67,"newNumber":75,"text":"      \"items\": {"}]}],"files":[{"path":"skills/comprehende/SKILL.md","kind":"text","status":"modified","patch":"diff --git a/skills/comprehende/SKILL.md b/skills/comprehende/SKILL.md\nindex 0561b54..c52468d 100644\n--- a/skills/comprehende/SKILL.md\n+++ b/skills/comprehende/SKILL.md\n@@ -27,19 +27,19 @@ The review document is interpretation only. It holds a title, groups, summaries,\n \n    Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue.\n 2. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref.\n-3. Run `npx comprehende@0.7.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content.\n-4. Recover the why, write the title, and write the what. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), and document `why` (only when a source names the motive). Summaries come from the code, not the log.\n+3. Run `npx comprehende@0.8.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content.\n+4. Recover the why, write the title, write the what, and compare the sources with the diff. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet.\n 5. Group the hunks by review concern, following the Grouping rules. Done when every hunk ref from the index appears in at least one group and every group has its `why`.\n 6. Write `review.json` in a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries). Shape per [references/review.schema.json](./references/review.schema.json); worked example in [references/example.md](./references/example.md). Copy hunk objects verbatim from the index. Set document `size` from review burden, not `git diff --stat`.\n-7. Run `npx comprehende@0.7.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0.\n-8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.7.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n+7. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0.\n+8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n \n ## Export\n \n Write a static site and put that folder where they asked.\n \n ```sh\n-npx comprehende@0.7.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\"\n+npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\"\n ```\n \n `$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is not written yet, finish the Workflow through validate, then export.\n@@ -91,9 +91,11 @@ Hunk identity is `(path, oldStart, newStart)` plus `oldPath` when renamed. Copy\n \n ## lookFor\n \n-`lookFor` is for what the live diff does not make obvious. Empty is correct for a rename, a formatting change, or a wording-only docs group; leave it empty rather than fill it.\n+`lookFor` is for what the live diff does not make obvious. Each bullet is one inspectable claim: optionally a short risk tag (Subtle, Breaking, Race, Perf), then one sentence. The bullet states the claim; the human checks it against the live diff and keeps the verdict. Pseudocode, diagrams, collapsed diffs, commit lists, hunk counts, and pass/fail marks stay out.\n \n-Each bullet is one inspectable claim: optionally a short risk tag (Subtle, Breaking, Race, Perf), then one sentence. When a behavior change hides in the hunk, a predicted trace earns a bullet: a small realistic input, where the old and new paths diverge, and the observable result. The human checks the claim against the live diff; pseudocode, diagrams, collapsed diffs, commit lists, and hunk counts stay out.\n+Group `lookFor` holds claims that live in those hunks. When a behavior change hides in the hunk, a predicted trace earns a bullet: a small realistic input, where the old and new paths diverge, and the observable result. Empty is correct for a rename, a formatting change, or a wording-only docs group; leave it empty rather than fill it.\n+\n+Document `lookFor` holds claims about the whole change, and work a source names that no hunk does. Compare each source with the diff. When a ticket, request, comment, or transcript names work, the bullet says what it asks for and what the diff does about it. Cite the source with `[text](source:s1)`; the id goes in `sources`, and `validate` fails on an unknown id. Omit the field when no source names expected work, or when the bullet would only restate document `summary`.\n \n ## Write the prose\n \n","complete":false,"hunks":[{"path":"skills/comprehende/SKILL.md","oldStart":27,"oldLines":19,"newStart":27,"newLines":19,"header":"@@ -27,19 +27,19 @@ The review document is interpretation only. It holds a title, groups, summaries,","language":"markdown","lines":[{"kind":"ctx","oldNumber":27,"newNumber":27,"text":""},{"kind":"ctx","oldNumber":28,"newNumber":28,"text":"   Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue."},{"kind":"ctx","oldNumber":29,"newNumber":29,"text":"2. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"del","oldNumber":30,"newNumber":null,"text":"3. Run `npx comprehende@0.7.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"del","oldNumber":31,"newNumber":null,"text":"4. Recover the why, write the title, and write the what. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), and document `why` (only when a source names the motive). Summaries come from the code, not the log."},{"kind":"add","oldNumber":null,"newNumber":30,"text":"3. Run `npx comprehende@0.8.0 index [--base <ref>] [--head <ref>]` and keep the JSON outside the work tree (stdout or a temp file). Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The index lists hunk refs (path plus `@@` ranges), image files (`oldStart` and `newStart` 0), and `skipped` (lockfiles and non-image binaries). It carries no line content."},{"kind":"add","oldNumber":null,"newNumber":31,"text":"4. Recover the why, write the title, write the what, and compare the sources with the diff. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"ctx","oldNumber":32,"newNumber":32,"text":"5. Group the hunks by review concern, following the Grouping rules. Done when every hunk ref from the index appears in at least one group and every group has its `why`."},{"kind":"ctx","oldNumber":33,"newNumber":33,"text":"6. Write `review.json` in a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries). Shape per [references/review.schema.json](./references/review.schema.json); worked example in [references/example.md](./references/example.md). Copy hunk objects verbatim from the index. Set document `size` from review burden, not `git diff --stat`."},{"kind":"del","oldNumber":34,"newNumber":null,"text":"7. Run `npx comprehende@0.7.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"del","oldNumber":35,"newNumber":null,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.7.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"add","oldNumber":null,"newNumber":34,"text":"7. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"add","oldNumber":null,"newNumber":35,"text":"8. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":36,"newNumber":36,"text":""},{"kind":"ctx","oldNumber":37,"newNumber":37,"text":"## Export"},{"kind":"ctx","oldNumber":38,"newNumber":38,"text":""},{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"Write a static site and put that folder where they asked."},{"kind":"ctx","oldNumber":40,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":41,"text":"```sh"},{"kind":"del","oldNumber":42,"newNumber":null,"text":"npx comprehende@0.7.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"add","oldNumber":null,"newNumber":42,"text":"npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"ctx","oldNumber":43,"newNumber":43,"text":"```"},{"kind":"ctx","oldNumber":44,"newNumber":44,"text":""},{"kind":"ctx","oldNumber":45,"newNumber":45,"text":"`$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is not written yet, finish the Workflow through validate, then export."}]},{"path":"skills/comprehende/SKILL.md","oldStart":91,"oldLines":9,"newStart":91,"newLines":11,"header":"@@ -91,9 +91,11 @@ Hunk identity is `(path, oldStart, newStart)` plus `oldPath` when renamed. Copy","language":"markdown","lines":[{"kind":"ctx","oldNumber":91,"newNumber":91,"text":""},{"kind":"ctx","oldNumber":92,"newNumber":92,"text":"## lookFor"},{"kind":"ctx","oldNumber":93,"newNumber":93,"text":""},{"kind":"del","oldNumber":94,"newNumber":null,"text":"`lookFor` is for what the live diff does not make obvious. Empty is correct for a rename, a formatting change, or a wording-only docs group; leave it empty rather than fill it."},{"kind":"add","oldNumber":null,"newNumber":94,"text":"`lookFor` is for what the live diff does not make obvious. Each bullet is one inspectable claim: optionally a short risk tag (Subtle, Breaking, Race, Perf), then one sentence. The bullet states the claim; the human checks it against the live diff and keeps the verdict. Pseudocode, diagrams, collapsed diffs, commit lists, hunk counts, and pass/fail marks stay out."},{"kind":"ctx","oldNumber":95,"newNumber":95,"text":""},{"kind":"del","oldNumber":96,"newNumber":null,"text":"Each bullet is one inspectable claim: optionally a short risk tag (Subtle, Breaking, Race, Perf), then one sentence. When a behavior change hides in the hunk, a predicted trace earns a bullet: a small realistic input, where the old and new paths diverge, and the observable result. The human checks the claim against the live diff; pseudocode, diagrams, collapsed diffs, commit lists, and hunk counts stay out."},{"kind":"add","oldNumber":null,"newNumber":96,"text":"Group `lookFor` holds claims that live in those hunks. When a behavior change hides in the hunk, a predicted trace earns a bullet: a small realistic input, where the old and new paths diverge, and the observable result. Empty is correct for a rename, a formatting change, or a wording-only docs group; leave it empty rather than fill it."},{"kind":"add","oldNumber":null,"newNumber":97,"text":""},{"kind":"add","oldNumber":null,"newNumber":98,"text":"Document `lookFor` holds claims about the whole change, and work a source names that no hunk does. Compare each source with the diff. When a ticket, request, comment, or transcript names work, the bullet says what it asks for and what the diff does about it. Cite the source with `[text](source:s1)`; the id goes in `sources`, and `validate` fails on an unknown id. Omit the field when no source names expected work, or when the bullet would only restate document `summary`."},{"kind":"ctx","oldNumber":97,"newNumber":99,"text":""},{"kind":"ctx","oldNumber":98,"newNumber":100,"text":"## Write the prose"},{"kind":"ctx","oldNumber":99,"newNumber":101,"text":""}]}]},{"path":"skills/comprehende/references/example.md","kind":"text","status":"modified","patch":"diff --git a/skills/comprehende/references/example.md b/skills/comprehende/references/example.md\nindex b0129ed..fa8d413 100644\n--- a/skills/comprehende/references/example.md\n+++ b/skills/comprehende/references/example.md\n@@ -6,6 +6,7 @@ Pointers and prose only. The `@@` numbers must come from `comprehende index`, no\n - `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request.\n - Ticket #12 names why this work exists, so document `why` is present. Document `title` keeps the ticket title. Document `summary` names both stories. The why cites the ticket with `[#12](source:s1)`.\n - `login` `summary` names how those hunks meet. `login` `lookFor` is a predicted trace. `docs` has no `lookFor`.\n+- Document `lookFor` compares ticket #12 with the diff: work the ticket asks for that no hunk does, and one claim about the whole change. Both cite `[#12](source:s1)`. Group `lookFor` stays inside its hunks.\n \n ```json\n {\n@@ -29,6 +30,10 @@ Pointers and prose only. The `@@` numbers must come from `comprehende index`, no\n       \"part\": \"Session cookie\"\n     }\n   ],\n+  \"lookFor\": [\n+    \"[#12](source:s1) also asks logout to clear the session cookie. No hunk touches `src/api/logout.ts`.\",\n+    \"Subtle. [#12](source:s1) wants sessions that client scripts cannot read. Sessions issued before this change keep their old cookie until it expires; no hunk rotates them.\"\n+  ],\n   \"groups\": [\n     {\n       \"id\": \"cookie\",\n","complete":true,"hunks":[{"path":"skills/comprehende/references/example.md","oldStart":6,"oldLines":6,"newStart":6,"newLines":7,"header":"@@ -6,6 +6,7 @@ Pointers and prose only. The `@@` numbers must come from `comprehende index`, no","language":"markdown","lines":[{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"- `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request."},{"kind":"ctx","oldNumber":7,"newNumber":7,"text":"- Ticket #12 names why this work exists, so document `why` is present. Document `title` keeps the ticket title. Document `summary` names both stories. The why cites the ticket with `[#12](source:s1)`."},{"kind":"ctx","oldNumber":8,"newNumber":8,"text":"- `login` `summary` names how those hunks meet. `login` `lookFor` is a predicted trace. `docs` has no `lookFor`."},{"kind":"add","oldNumber":null,"newNumber":9,"text":"- Document `lookFor` compares ticket #12 with the diff: work the ticket asks for that no hunk does, and one claim about the whole change. Both cite `[#12](source:s1)`. Group `lookFor` stays inside its hunks."},{"kind":"ctx","oldNumber":9,"newNumber":10,"text":""},{"kind":"ctx","oldNumber":10,"newNumber":11,"text":"```json"},{"kind":"ctx","oldNumber":11,"newNumber":12,"text":"{"}]},{"path":"skills/comprehende/references/example.md","oldStart":29,"oldLines":6,"newStart":30,"newLines":10,"header":"@@ -29,6 +30,10 @@ Pointers and prose only. The `@@` numbers must come from `comprehende index`, no","language":"markdown","lines":[{"kind":"ctx","oldNumber":29,"newNumber":30,"text":"      \"part\": \"Session cookie\""},{"kind":"ctx","oldNumber":30,"newNumber":31,"text":"    }"},{"kind":"ctx","oldNumber":31,"newNumber":32,"text":"  ],"},{"kind":"add","oldNumber":null,"newNumber":33,"text":"  \"lookFor\": ["},{"kind":"add","oldNumber":null,"newNumber":34,"text":"    \"[#12](source:s1) also asks logout to clear the session cookie. No hunk touches `src/api/logout.ts`.\","},{"kind":"add","oldNumber":null,"newNumber":35,"text":"    \"Subtle. [#12](source:s1) wants sessions that client scripts cannot read. Sessions issued before this change keep their old cookie until it expires; no hunk rotates them.\""},{"kind":"add","oldNumber":null,"newNumber":36,"text":"  ],"},{"kind":"ctx","oldNumber":32,"newNumber":37,"text":"  \"groups\": ["},{"kind":"ctx","oldNumber":33,"newNumber":38,"text":"    {"},{"kind":"ctx","oldNumber":34,"newNumber":39,"text":"      \"id\": \"cookie\","}]}]},{"path":"skills/comprehende/references/review.schema.json","kind":"text","status":"modified","patch":"diff --git a/skills/comprehende/references/review.schema.json b/skills/comprehende/references/review.schema.json\nindex e76476b..6886bf5 100644\n--- a/skills/comprehende/references/review.schema.json\n+++ b/skills/comprehende/references/review.schema.json\n@@ -62,6 +62,14 @@\n         \"$ref\": \"#/$defs/source\"\n       }\n     },\n+    \"lookFor\": {\n+      \"description\": \"Whole-change and missing-work claims. Cite the source. Do not store a pass/fail.\",\n+      \"type\": \"array\",\n+      \"items\": {\n+        \"type\": \"string\",\n+        \"minLength\": 1\n+      }\n+    },\n     \"groups\": {\n       \"type\": \"array\",\n       \"items\": {\n","complete":true,"hunks":[{"path":"skills/comprehende/references/review.schema.json","oldStart":62,"oldLines":6,"newStart":62,"newLines":14,"header":"@@ -62,6 +62,14 @@","language":"json","lines":[{"kind":"ctx","oldNumber":62,"newNumber":62,"text":"        \"$ref\": \"#/$defs/source\""},{"kind":"ctx","oldNumber":63,"newNumber":63,"text":"      }"},{"kind":"ctx","oldNumber":64,"newNumber":64,"text":"    },"},{"kind":"add","oldNumber":null,"newNumber":65,"text":"    \"lookFor\": {"},{"kind":"add","oldNumber":null,"newNumber":66,"text":"      \"description\": \"Whole-change and missing-work claims. Cite the source. Do not store a pass/fail.\","},{"kind":"add","oldNumber":null,"newNumber":67,"text":"      \"type\": \"array\","},{"kind":"add","oldNumber":null,"newNumber":68,"text":"      \"items\": {"},{"kind":"add","oldNumber":null,"newNumber":69,"text":"        \"type\": \"string\","},{"kind":"add","oldNumber":null,"newNumber":70,"text":"        \"minLength\": 1"},{"kind":"add","oldNumber":null,"newNumber":71,"text":"      }"},{"kind":"add","oldNumber":null,"newNumber":72,"text":"    },"},{"kind":"ctx","oldNumber":65,"newNumber":73,"text":"    \"groups\": {"},{"kind":"ctx","oldNumber":66,"newNumber":74,"text":"      \"type\": \"array\","},{"kind":"ctx","oldNumber":67,"newNumber":75,"text":"      \"items\": {"}]}]}]}
