{"hunks":[{"path":"skills-next/comprehende/SKILL.md","oldStart":19,"oldLines":18,"newStart":19,"newLines":29,"header":"@@ -19,18 +19,29 @@ The review document is interpretation only. It holds a title, groups, summaries,","language":"markdown","lines":[{"kind":"ctx","oldNumber":19,"newNumber":19,"text":""},{"kind":"ctx","oldNumber":20,"newNumber":20,"text":"## Workflow"},{"kind":"ctx","oldNumber":21,"newNumber":21,"text":""},{"kind":"del","oldNumber":22,"newNumber":null,"text":"1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show this command as an option they can run:"},{"kind":"add","oldNumber":null,"newNumber":22,"text":"Each step is one tool call where the step says so. Batch the commands as written; every extra round trip re-sends the whole context."},{"kind":"add","oldNumber":null,"newNumber":23,"text":""},{"kind":"add","oldNumber":null,"newNumber":24,"text":"1. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"add","oldNumber":null,"newNumber":25,"text":"2. One shell call: version check, covering skeleton, log, and stat. `$REVIEW_DIR` is a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries)."},{"kind":"add","oldNumber":null,"newNumber":26,"text":""},{"kind":"add","oldNumber":null,"newNumber":27,"text":"   ```sh"},{"kind":"add","oldNumber":null,"newNumber":28,"text":"   npm view comprehende version"},{"kind":"add","oldNumber":null,"newNumber":29,"text":"   npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"add","oldNumber":null,"newNumber":30,"text":"   git log --format='%s%n%n%b' --end-of-options <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":31,"text":"   git diff --stat <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":32,"text":"   ```"},{"kind":"add","oldNumber":null,"newNumber":33,"text":""},{"kind":"add","oldNumber":null,"newNumber":34,"text":"   If `npm view` reports a version newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes every hunk ref into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those hunk refs; do not invent replacements."},{"kind":"add","oldNumber":null,"newNumber":35,"text":"3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec."},{"kind":"ctx","oldNumber":23,"newNumber":36,"text":""},{"kind":"ctx","oldNumber":24,"newNumber":37,"text":"   ```sh"},{"kind":"del","oldNumber":25,"newNumber":null,"text":"   npx skills update"},{"kind":"add","oldNumber":null,"newNumber":38,"text":"   git diff <base>...<head> -- . ':(exclude)<path>'"},{"kind":"ctx","oldNumber":26,"newNumber":39,"text":"   ```"},{"kind":"ctx","oldNumber":27,"newNumber":40,"text":""},{"kind":"del","oldNumber":28,"newNumber":null,"text":"   Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue."},{"kind":"del","oldNumber":29,"newNumber":null,"text":"2. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"del","oldNumber":30,"newNumber":null,"text":"3. Write a covering skeleton. `$REVIEW_DIR` is a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries). Run `npx comprehende@0.8.0 review [--base <ref>] [--head <ref>] --data \"$REVIEW_DIR/review.json\"`. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The command indexes live git and writes every hunk ref into one group, with stub prose. It does not invent a review. Done when that file exists. Keep those hunk refs; do not invent replacements."},{"kind":"del","oldNumber":31,"newNumber":null,"text":"4. Recover the why, write the title, write the what, and compare the sources with the diff. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"del","oldNumber":32,"newNumber":null,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy hunk objects from the skeleton; do not invent refs. Set document `size` from review burden, not `git diff --stat`. Shape per [references/review.schema.json](./references/review.schema.json); worked example in [references/example.md](./references/example.md). Done when every hunk ref from the skeleton appears in at least one group, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"del","oldNumber":33,"newNumber":null,"text":"6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"add","oldNumber":null,"newNumber":41,"text":"   Sources are listed under The why and The title. Done when the diff and those sources are in context."},{"kind":"add","oldNumber":null,"newNumber":42,"text":"4. Read the skeleton once, for its hunk refs. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"add","oldNumber":null,"newNumber":43,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy hunk objects from the skeleton; do not invent refs. Set document `size` from review burden, not `git diff --stat`. Shape and field names: [references/example.md](./references/example.md) shows every field. Write the whole `review.json` in one write; the skeleton's `ungrouped` group is replaced, not kept. Done when every hunk ref from the skeleton appears in at least one group, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"add","oldNumber":null,"newNumber":44,"text":"6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"ctx","oldNumber":34,"newNumber":45,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":35,"newNumber":46,"text":""},{"kind":"ctx","oldNumber":36,"newNumber":47,"text":"## Export"}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":53,"oldLines":14,"newStart":64,"newLines":14,"header":"@@ -53,14 +64,14 @@ Invent a title when those sources are missing, vague, or name something else. A","language":"markdown","lines":[{"kind":"ctx","oldNumber":53,"newNumber":64,"text":""},{"kind":"ctx","oldNumber":54,"newNumber":65,"text":"## The why"},{"kind":"ctx","oldNumber":55,"newNumber":66,"text":""},{"kind":"del","oldNumber":56,"newNumber":null,"text":"Document `why` names why the work exists, in one or two sentences. This should be inferred only from the sources below. If the sources don't tell us why the change exists, or they are missing, omit it, and let the summary be the main source for getting an idea of the changes. If sources are available and they tell the story, don't derive document `why` from the diff, from group structure, or from a motive you infer from the patch. A request naming two unrelated product stories with no unifying source also gets no document `why`; each story keeps its own group `why`s."},{"kind":"add","oldNumber":null,"newNumber":67,"text":"Document `why` names why the work exists, in one or two sentences, inferred only from the sources below. If those sources are silent or mixed, omit it. Do not derive document `why` from the diff, from group structure, or from a motive you infer from the patch. A request naming two unrelated product stories with no unifying source also gets no document `why`; each story keeps its own group `why`s."},{"kind":"ctx","oldNumber":57,"newNumber":68,"text":""},{"kind":"ctx","oldNumber":58,"newNumber":69,"text":"Sources are read, never copied into the review document, except PR review comments: copy `author` and `body` so the UI can show the quote. Keep ticket bodies, request descriptions, commit text, and transcript text out of `review.json`. Cite a source in prose with `[text](source:s1)` when that sentence leans on it."},{"kind":"ctx","oldNumber":59,"newNumber":70,"text":""},{"kind":"ctx","oldNumber":60,"newNumber":71,"text":"- Tickets and issues. Emit a `sources` item: short `id` (`s1`), `kind` `ticket`, `label` (`#24`), plus `url`, `title`, `gist`, and `part` when you have them. Read the ticket (host CLI, tools or MCP, whichever is available) when document `why` needs it. `part` goes on a ticket that belongs to one story, matching that story's group `part`; omit it only when the request or ticket title says the ticket covers the whole review. Commit count proves nothing about ticket scope. Do not copy the ticket body."},{"kind":"ctx","oldNumber":61,"newNumber":72,"text":"- Pull request. When the user named a pull request or merge request, emit `kind` `pr` with a label like `PR #32` and a gist from the description. Do not copy the description. PRs can also refer to tickets and issues in their description or branch names."},{"kind":"ctx","oldNumber":62,"newNumber":73,"text":"- PR comments. Emit `kind` `pr-comment`. Label like `alice on PR #32`. Copy `author` and `body`. For a review comment on a line, also copy `path`, `side` (`old` or `new`, git-shaped, not GitHub LEFT/RIGHT), and `line`. A conversation comment has no pin. Flatten threads: one source per comment."},{"kind":"del","oldNumber":63,"newNumber":null,"text":"- Commit messages. `git log --format='%s%n%n%b' --end-of-options <base>...<head>`. Emit `kind` `commit` when a commit names the motive. They can clarify group `why`; document `why` needs one of the stronger sources above. These can also contain issues and tickets."},{"kind":"add","oldNumber":null,"newNumber":74,"text":"- Commit messages. `git log --format='%s%n%n%b' --end-of-options <base>...<head>`. Emit `kind` `commit` when a commit names the motive. `label` is that subject line, or a SHA from the same log. They can clarify group `why`; document `why` needs one of the stronger sources above. These can also contain issues and tickets."},{"kind":"ctx","oldNumber":64,"newNumber":75,"text":"- Transcripts. A coding-agent (like Cursor, Claude Code, Codex, etc.) transcript you already have (this session, or a historical one) supplies the human's stated reason. Emit `kind` `transcript`, a label like `Cursor session · Aug 12`, and a gist. Omit `url`. Do not copy transcript text."},{"kind":"ctx","oldNumber":65,"newNumber":76,"text":""},{"kind":"ctx","oldNumber":66,"newNumber":77,"text":"Put those ids on the group `sources` array when the group uses them, even if the prose does not cite them inline. `validate` fails on a `source:` href whose id is missing from `sources`."}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":85,"oldLines":7,"newStart":96,"newLines":7,"header":"@@ -85,7 +96,7 @@ Group `summary` is one sentence saying what the group is, describing how its hun","language":"markdown","lines":[{"kind":"ctx","oldNumber":85,"newNumber":96,"text":"- `suggestedOrder` walks the whole review, independent parts included. Mechanical parts, independent documentation, and test-only cleanup go last."},{"kind":"ctx","oldNumber":86,"newNumber":97,"text":"- Document `size` is human review burden: `trivial`, `small`, `medium`, `large`, `very-large`. Forty files changing one import in one group are `small`; three files rewriting a contract the rest of the system hangs on can be `large`."},{"kind":"ctx","oldNumber":87,"newNumber":98,"text":"- Coverage: every hunk from the skeleton sits in at least one group; duplicate refs across groups are allowed. Unreferenced hunks fail `validate` and show as Unassigned in the UI."},{"kind":"del","oldNumber":88,"newNumber":null,"text":"- Lockfiles stay in `skipped`; the UI gives them their own closed bucket. Hunk refs exist only for hunks the skeleton lists."},{"kind":"add","oldNumber":null,"newNumber":99,"text":"- Lockfiles have no hunk refs; the CLI leaves them out of the skeleton and the UI gives them their own closed bucket. Hunk refs exist only for hunks the skeleton lists."},{"kind":"ctx","oldNumber":89,"newNumber":100,"text":"- Stale refs (rebase of the pinned commits) fail `validate`; `serve` still starts, shows git at those SHAs, and flags the broken pointer. Re-run `review` and copy fresh refs. A dirty work tree does not make refs stale."},{"kind":"ctx","oldNumber":90,"newNumber":101,"text":""},{"kind":"ctx","oldNumber":91,"newNumber":102,"text":"Hunk identity is `(path, oldStart, newStart)` plus `oldPath` when renamed. Copy `oldStart`, `oldLines`, `newStart`, and `newLines` from the skeleton. Image files are hunks; copy their refs into groups. Git LFS images are read from `.git/lfs/objects` in the clone; a missing object leaves the image slot empty."}]},{"path":"skills-next/comprehende/references/example.md","oldStart":1,"oldLines":6,"newStart":1,"newLines":6,"header":"@@ -1,6 +1,6 @@","language":"markdown","lines":[{"kind":"ctx","oldNumber":1,"newNumber":1,"text":"# Example review.json"},{"kind":"ctx","oldNumber":2,"newNumber":2,"text":""},{"kind":"del","oldNumber":3,"newNumber":null,"text":"Pointers and prose only. The `@@` numbers must come from `comprehende review`, not from reading the patch. Shape: [review.schema.json](./review.schema.json)."},{"kind":"add","oldNumber":null,"newNumber":3,"text":"Pointers and prose only. The `@@` numbers must come from `comprehende review`, not from reading the patch."},{"kind":"ctx","oldNumber":4,"newNumber":4,"text":""},{"kind":"ctx","oldNumber":5,"newNumber":5,"text":"- `login` depends on `cookie`. Both use `part` \"Session cookie\"."},{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"- `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request."}]}],"files":[{"path":"skills-next/comprehende/SKILL.md","kind":"text","status":"modified","patch":"diff --git a/skills-next/comprehende/SKILL.md b/skills-next/comprehende/SKILL.md\nindex e2babf4..a11c141 100644\n--- a/skills-next/comprehende/SKILL.md\n+++ b/skills-next/comprehende/SKILL.md\n@@ -19,18 +19,29 @@ The review document is interpretation only. It holds a title, groups, summaries,\n \n ## Workflow\n \n-1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show this command as an option they can run:\n+Each step is one tool call where the step says so. Batch the commands as written; every extra round trip re-sends the whole context.\n+\n+1. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref.\n+2. One shell call: version check, covering skeleton, log, and stat. `$REVIEW_DIR` is a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries).\n+\n+   ```sh\n+   npm view comprehende version\n+   npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\"\n+   git log --format='%s%n%n%b' --end-of-options <base>...<head>\n+   git diff --stat <base>...<head>\n+   ```\n+\n+   If `npm view` reports a version newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes every hunk ref into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those hunk refs; do not invent replacements.\n+3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec.\n \n    ```sh\n-   npx skills update\n+   git diff <base>...<head> -- . ':(exclude)<path>'\n    ```\n \n-   Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue.\n-2. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref.\n-3. Write a covering skeleton. `$REVIEW_DIR` is a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries). Run `npx comprehende@0.8.0 review [--base <ref>] [--head <ref>] --data \"$REVIEW_DIR/review.json\"`. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The command indexes live git and writes every hunk ref into one group, with stub prose. It does not invent a review. Done when that file exists. Keep those hunk refs; do not invent replacements.\n-4. Recover the why, write the title, write the what, and compare the sources with the diff. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet.\n-5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy hunk objects from the skeleton; do not invent refs. Set document `size` from review burden, not `git diff --stat`. Shape per [references/review.schema.json](./references/review.schema.json); worked example in [references/example.md](./references/example.md). Done when every hunk ref from the skeleton appears in at least one group, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry.\n-6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0.\n+   Sources are listed under The why and The title. Done when the diff and those sources are in context.\n+4. Read the skeleton once, for its hunk refs. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet.\n+5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy hunk objects from the skeleton; do not invent refs. Set document `size` from review burden, not `git diff --stat`. Shape and field names: [references/example.md](./references/example.md) shows every field. Write the whole `review.json` in one write; the skeleton's `ungrouped` group is replaced, not kept. Done when every hunk ref from the skeleton appears in at least one group, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry.\n+6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0.\n 7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n \n ## Export\n@@ -53,14 +64,14 @@ Invent a title when those sources are missing, vague, or name something else. A\n \n ## The why\n \n-Document `why` names why the work exists, in one or two sentences. This should be inferred only from the sources below. If the sources don't tell us why the change exists, or they are missing, omit it, and let the summary be the main source for getting an idea of the changes. If sources are available and they tell the story, don't derive document `why` from the diff, from group structure, or from a motive you infer from the patch. A request naming two unrelated product stories with no unifying source also gets no document `why`; each story keeps its own group `why`s.\n+Document `why` names why the work exists, in one or two sentences, inferred only from the sources below. If those sources are silent or mixed, omit it. Do not derive document `why` from the diff, from group structure, or from a motive you infer from the patch. A request naming two unrelated product stories with no unifying source also gets no document `why`; each story keeps its own group `why`s.\n \n Sources are read, never copied into the review document, except PR review comments: copy `author` and `body` so the UI can show the quote. Keep ticket bodies, request descriptions, commit text, and transcript text out of `review.json`. Cite a source in prose with `[text](source:s1)` when that sentence leans on it.\n \n - Tickets and issues. Emit a `sources` item: short `id` (`s1`), `kind` `ticket`, `label` (`#24`), plus `url`, `title`, `gist`, and `part` when you have them. Read the ticket (host CLI, tools or MCP, whichever is available) when document `why` needs it. `part` goes on a ticket that belongs to one story, matching that story's group `part`; omit it only when the request or ticket title says the ticket covers the whole review. Commit count proves nothing about ticket scope. Do not copy the ticket body.\n - Pull request. When the user named a pull request or merge request, emit `kind` `pr` with a label like `PR #32` and a gist from the description. Do not copy the description. PRs can also refer to tickets and issues in their description or branch names.\n - PR comments. Emit `kind` `pr-comment`. Label like `alice on PR #32`. Copy `author` and `body`. For a review comment on a line, also copy `path`, `side` (`old` or `new`, git-shaped, not GitHub LEFT/RIGHT), and `line`. A conversation comment has no pin. Flatten threads: one source per comment.\n-- Commit messages. `git log --format='%s%n%n%b' --end-of-options <base>...<head>`. Emit `kind` `commit` when a commit names the motive. They can clarify group `why`; document `why` needs one of the stronger sources above. These can also contain issues and tickets.\n+- Commit messages. `git log --format='%s%n%n%b' --end-of-options <base>...<head>`. Emit `kind` `commit` when a commit names the motive. `label` is that subject line, or a SHA from the same log. They can clarify group `why`; document `why` needs one of the stronger sources above. These can also contain issues and tickets.\n - Transcripts. A coding-agent (like Cursor, Claude Code, Codex, etc.) transcript you already have (this session, or a historical one) supplies the human's stated reason. Emit `kind` `transcript`, a label like `Cursor session · Aug 12`, and a gist. Omit `url`. Do not copy transcript text.\n \n Put those ids on the group `sources` array when the group uses them, even if the prose does not cite them inline. `validate` fails on a `source:` href whose id is missing from `sources`.\n@@ -85,7 +96,7 @@ Group `summary` is one sentence saying what the group is, describing how its hun\n - `suggestedOrder` walks the whole review, independent parts included. Mechanical parts, independent documentation, and test-only cleanup go last.\n - Document `size` is human review burden: `trivial`, `small`, `medium`, `large`, `very-large`. Forty files changing one import in one group are `small`; three files rewriting a contract the rest of the system hangs on can be `large`.\n - Coverage: every hunk from the skeleton sits in at least one group; duplicate refs across groups are allowed. Unreferenced hunks fail `validate` and show as Unassigned in the UI.\n-- Lockfiles stay in `skipped`; the UI gives them their own closed bucket. Hunk refs exist only for hunks the skeleton lists.\n+- Lockfiles have no hunk refs; the CLI leaves them out of the skeleton and the UI gives them their own closed bucket. Hunk refs exist only for hunks the skeleton lists.\n - Stale refs (rebase of the pinned commits) fail `validate`; `serve` still starts, shows git at those SHAs, and flags the broken pointer. Re-run `review` and copy fresh refs. A dirty work tree does not make refs stale.\n \n Hunk identity is `(path, oldStart, newStart)` plus `oldPath` when renamed. Copy `oldStart`, `oldLines`, `newStart`, and `newLines` from the skeleton. Image files are hunks; copy their refs into groups. Git LFS images are read from `.git/lfs/objects` in the clone; a missing object leaves the image slot empty.\n","complete":true,"hunks":[{"path":"skills-next/comprehende/SKILL.md","oldStart":19,"oldLines":18,"newStart":19,"newLines":29,"header":"@@ -19,18 +19,29 @@ The review document is interpretation only. It holds a title, groups, summaries,","language":"markdown","lines":[{"kind":"ctx","oldNumber":19,"newNumber":19,"text":""},{"kind":"ctx","oldNumber":20,"newNumber":20,"text":"## Workflow"},{"kind":"ctx","oldNumber":21,"newNumber":21,"text":""},{"kind":"del","oldNumber":22,"newNumber":null,"text":"1. Check npm for a newer package. Run `npm view comprehende version`. If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show this command as an option they can run:"},{"kind":"add","oldNumber":null,"newNumber":22,"text":"Each step is one tool call where the step says so. Batch the commands as written; every extra round trip re-sends the whole context."},{"kind":"add","oldNumber":null,"newNumber":23,"text":""},{"kind":"add","oldNumber":null,"newNumber":24,"text":"1. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"add","oldNumber":null,"newNumber":25,"text":"2. One shell call: version check, covering skeleton, log, and stat. `$REVIEW_DIR` is a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries)."},{"kind":"add","oldNumber":null,"newNumber":26,"text":""},{"kind":"add","oldNumber":null,"newNumber":27,"text":"   ```sh"},{"kind":"add","oldNumber":null,"newNumber":28,"text":"   npm view comprehende version"},{"kind":"add","oldNumber":null,"newNumber":29,"text":"   npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"add","oldNumber":null,"newNumber":30,"text":"   git log --format='%s%n%n%b' --end-of-options <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":31,"text":"   git diff --stat <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":32,"text":"   ```"},{"kind":"add","oldNumber":null,"newNumber":33,"text":""},{"kind":"add","oldNumber":null,"newNumber":34,"text":"   If `npm view` reports a version newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes every hunk ref into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those hunk refs; do not invent replacements."},{"kind":"add","oldNumber":null,"newNumber":35,"text":"3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec."},{"kind":"ctx","oldNumber":23,"newNumber":36,"text":""},{"kind":"ctx","oldNumber":24,"newNumber":37,"text":"   ```sh"},{"kind":"del","oldNumber":25,"newNumber":null,"text":"   npx skills update"},{"kind":"add","oldNumber":null,"newNumber":38,"text":"   git diff <base>...<head> -- . ':(exclude)<path>'"},{"kind":"ctx","oldNumber":26,"newNumber":39,"text":"   ```"},{"kind":"ctx","oldNumber":27,"newNumber":40,"text":""},{"kind":"del","oldNumber":28,"newNumber":null,"text":"   Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue."},{"kind":"del","oldNumber":29,"newNumber":null,"text":"2. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"del","oldNumber":30,"newNumber":null,"text":"3. Write a covering skeleton. `$REVIEW_DIR` is a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries). Run `npx comprehende@0.8.0 review [--base <ref>] [--head <ref>] --data \"$REVIEW_DIR/review.json\"`. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. The command indexes live git and writes every hunk ref into one group, with stub prose. It does not invent a review. Done when that file exists. Keep those hunk refs; do not invent replacements."},{"kind":"del","oldNumber":31,"newNumber":null,"text":"4. Recover the why, write the title, write the what, and compare the sources with the diff. Read the sources listed under The why and The title, read `git diff --stat <base>...<head>` and the diffs themselves, then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"del","oldNumber":32,"newNumber":null,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy hunk objects from the skeleton; do not invent refs. Set document `size` from review burden, not `git diff --stat`. Shape per [references/review.schema.json](./references/review.schema.json); worked example in [references/example.md](./references/example.md). Done when every hunk ref from the skeleton appears in at least one group, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"del","oldNumber":33,"newNumber":null,"text":"6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. On failure, fix groups or coverage; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"add","oldNumber":null,"newNumber":41,"text":"   Sources are listed under The why and The title. Done when the diff and those sources are in context."},{"kind":"add","oldNumber":null,"newNumber":42,"text":"4. Read the skeleton once, for its hunk refs. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"add","oldNumber":null,"newNumber":43,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy hunk objects from the skeleton; do not invent refs. Set document `size` from review burden, not `git diff --stat`. Shape and field names: [references/example.md](./references/example.md) shows every field. Write the whole `review.json` in one write; the skeleton's `ungrouped` group is replaced, not kept. Done when every hunk ref from the skeleton appears in at least one group, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"add","oldNumber":null,"newNumber":44,"text":"6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"ctx","oldNumber":34,"newNumber":45,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":35,"newNumber":46,"text":""},{"kind":"ctx","oldNumber":36,"newNumber":47,"text":"## Export"}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":53,"oldLines":14,"newStart":64,"newLines":14,"header":"@@ -53,14 +64,14 @@ Invent a title when those sources are missing, vague, or name something else. A","language":"markdown","lines":[{"kind":"ctx","oldNumber":53,"newNumber":64,"text":""},{"kind":"ctx","oldNumber":54,"newNumber":65,"text":"## The why"},{"kind":"ctx","oldNumber":55,"newNumber":66,"text":""},{"kind":"del","oldNumber":56,"newNumber":null,"text":"Document `why` names why the work exists, in one or two sentences. This should be inferred only from the sources below. If the sources don't tell us why the change exists, or they are missing, omit it, and let the summary be the main source for getting an idea of the changes. If sources are available and they tell the story, don't derive document `why` from the diff, from group structure, or from a motive you infer from the patch. A request naming two unrelated product stories with no unifying source also gets no document `why`; each story keeps its own group `why`s."},{"kind":"add","oldNumber":null,"newNumber":67,"text":"Document `why` names why the work exists, in one or two sentences, inferred only from the sources below. If those sources are silent or mixed, omit it. Do not derive document `why` from the diff, from group structure, or from a motive you infer from the patch. A request naming two unrelated product stories with no unifying source also gets no document `why`; each story keeps its own group `why`s."},{"kind":"ctx","oldNumber":57,"newNumber":68,"text":""},{"kind":"ctx","oldNumber":58,"newNumber":69,"text":"Sources are read, never copied into the review document, except PR review comments: copy `author` and `body` so the UI can show the quote. Keep ticket bodies, request descriptions, commit text, and transcript text out of `review.json`. Cite a source in prose with `[text](source:s1)` when that sentence leans on it."},{"kind":"ctx","oldNumber":59,"newNumber":70,"text":""},{"kind":"ctx","oldNumber":60,"newNumber":71,"text":"- Tickets and issues. Emit a `sources` item: short `id` (`s1`), `kind` `ticket`, `label` (`#24`), plus `url`, `title`, `gist`, and `part` when you have them. Read the ticket (host CLI, tools or MCP, whichever is available) when document `why` needs it. `part` goes on a ticket that belongs to one story, matching that story's group `part`; omit it only when the request or ticket title says the ticket covers the whole review. Commit count proves nothing about ticket scope. Do not copy the ticket body."},{"kind":"ctx","oldNumber":61,"newNumber":72,"text":"- Pull request. When the user named a pull request or merge request, emit `kind` `pr` with a label like `PR #32` and a gist from the description. Do not copy the description. PRs can also refer to tickets and issues in their description or branch names."},{"kind":"ctx","oldNumber":62,"newNumber":73,"text":"- PR comments. Emit `kind` `pr-comment`. Label like `alice on PR #32`. Copy `author` and `body`. For a review comment on a line, also copy `path`, `side` (`old` or `new`, git-shaped, not GitHub LEFT/RIGHT), and `line`. A conversation comment has no pin. Flatten threads: one source per comment."},{"kind":"del","oldNumber":63,"newNumber":null,"text":"- Commit messages. `git log --format='%s%n%n%b' --end-of-options <base>...<head>`. Emit `kind` `commit` when a commit names the motive. They can clarify group `why`; document `why` needs one of the stronger sources above. These can also contain issues and tickets."},{"kind":"add","oldNumber":null,"newNumber":74,"text":"- Commit messages. `git log --format='%s%n%n%b' --end-of-options <base>...<head>`. Emit `kind` `commit` when a commit names the motive. `label` is that subject line, or a SHA from the same log. They can clarify group `why`; document `why` needs one of the stronger sources above. These can also contain issues and tickets."},{"kind":"ctx","oldNumber":64,"newNumber":75,"text":"- Transcripts. A coding-agent (like Cursor, Claude Code, Codex, etc.) transcript you already have (this session, or a historical one) supplies the human's stated reason. Emit `kind` `transcript`, a label like `Cursor session · Aug 12`, and a gist. Omit `url`. Do not copy transcript text."},{"kind":"ctx","oldNumber":65,"newNumber":76,"text":""},{"kind":"ctx","oldNumber":66,"newNumber":77,"text":"Put those ids on the group `sources` array when the group uses them, even if the prose does not cite them inline. `validate` fails on a `source:` href whose id is missing from `sources`."}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":85,"oldLines":7,"newStart":96,"newLines":7,"header":"@@ -85,7 +96,7 @@ Group `summary` is one sentence saying what the group is, describing how its hun","language":"markdown","lines":[{"kind":"ctx","oldNumber":85,"newNumber":96,"text":"- `suggestedOrder` walks the whole review, independent parts included. Mechanical parts, independent documentation, and test-only cleanup go last."},{"kind":"ctx","oldNumber":86,"newNumber":97,"text":"- Document `size` is human review burden: `trivial`, `small`, `medium`, `large`, `very-large`. Forty files changing one import in one group are `small`; three files rewriting a contract the rest of the system hangs on can be `large`."},{"kind":"ctx","oldNumber":87,"newNumber":98,"text":"- Coverage: every hunk from the skeleton sits in at least one group; duplicate refs across groups are allowed. Unreferenced hunks fail `validate` and show as Unassigned in the UI."},{"kind":"del","oldNumber":88,"newNumber":null,"text":"- Lockfiles stay in `skipped`; the UI gives them their own closed bucket. Hunk refs exist only for hunks the skeleton lists."},{"kind":"add","oldNumber":null,"newNumber":99,"text":"- Lockfiles have no hunk refs; the CLI leaves them out of the skeleton and the UI gives them their own closed bucket. Hunk refs exist only for hunks the skeleton lists."},{"kind":"ctx","oldNumber":89,"newNumber":100,"text":"- Stale refs (rebase of the pinned commits) fail `validate`; `serve` still starts, shows git at those SHAs, and flags the broken pointer. Re-run `review` and copy fresh refs. A dirty work tree does not make refs stale."},{"kind":"ctx","oldNumber":90,"newNumber":101,"text":""},{"kind":"ctx","oldNumber":91,"newNumber":102,"text":"Hunk identity is `(path, oldStart, newStart)` plus `oldPath` when renamed. Copy `oldStart`, `oldLines`, `newStart`, and `newLines` from the skeleton. Image files are hunks; copy their refs into groups. Git LFS images are read from `.git/lfs/objects` in the clone; a missing object leaves the image slot empty."}]}]},{"path":"skills-next/comprehende/references/example.md","kind":"text","status":"modified","patch":"diff --git a/skills-next/comprehende/references/example.md b/skills-next/comprehende/references/example.md\nindex 2a25c8b..2b051d0 100644\n--- a/skills-next/comprehende/references/example.md\n+++ b/skills-next/comprehende/references/example.md\n@@ -1,6 +1,6 @@\n # Example review.json\n \n-Pointers and prose only. The `@@` numbers must come from `comprehende review`, not from reading the patch. Shape: [review.schema.json](./review.schema.json).\n+Pointers and prose only. The `@@` numbers must come from `comprehende review`, not from reading the patch.\n \n - `login` depends on `cookie`. Both use `part` \"Session cookie\".\n - `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request.\n","complete":true,"hunks":[{"path":"skills-next/comprehende/references/example.md","oldStart":1,"oldLines":6,"newStart":1,"newLines":6,"header":"@@ -1,6 +1,6 @@","language":"markdown","lines":[{"kind":"ctx","oldNumber":1,"newNumber":1,"text":"# Example review.json"},{"kind":"ctx","oldNumber":2,"newNumber":2,"text":""},{"kind":"del","oldNumber":3,"newNumber":null,"text":"Pointers and prose only. The `@@` numbers must come from `comprehende review`, not from reading the patch. Shape: [review.schema.json](./review.schema.json)."},{"kind":"add","oldNumber":null,"newNumber":3,"text":"Pointers and prose only. The `@@` numbers must come from `comprehende review`, not from reading the patch."},{"kind":"ctx","oldNumber":4,"newNumber":4,"text":""},{"kind":"ctx","oldNumber":5,"newNumber":5,"text":"- `login` depends on `cookie`. Both use `part` \"Session cookie\"."},{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"- `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request."}]}]}]}
