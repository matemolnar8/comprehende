{"hunks":[{"path":"package.json","oldStart":1,"oldLines":6,"newStart":1,"newLines":6,"header":"@@ -1,6 +1,6 @@","language":"json","lines":[{"kind":"ctx","oldNumber":1,"newNumber":1,"text":"{"},{"kind":"ctx","oldNumber":2,"newNumber":2,"text":"  \"name\": \"comprehende\","},{"kind":"del","oldNumber":3,"newNumber":null,"text":"  \"version\": \"0.8.0\","},{"kind":"add","oldNumber":null,"newNumber":3,"text":"  \"version\": \"0.9.0\","},{"kind":"ctx","oldNumber":4,"newNumber":4,"text":"  \"description\": \"Local review assistant that groups git diffs so humans can comprehend AI code changes.\","},{"kind":"ctx","oldNumber":5,"newNumber":5,"text":"  \"type\": \"module\","},{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"  \"bin\": {"}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":12,"oldLines":7,"newStart":12,"newLines":7,"header":"@@ -12,7 +12,7 @@ Group a git diff into review concerns and serve a local UI. The point is cogniti","language":"markdown","lines":[{"kind":"ctx","oldNumber":12,"newNumber":12,"text":"Run every command inside the repository under review. Cwd is the repo; there is no `--repo` flag. Use the pinned CLI:"},{"kind":"ctx","oldNumber":13,"newNumber":13,"text":""},{"kind":"ctx","oldNumber":14,"newNumber":14,"text":"```sh"},{"kind":"del","oldNumber":15,"newNumber":null,"text":"npx comprehende@0.8.0 <command>"},{"kind":"add","oldNumber":null,"newNumber":15,"text":"npx comprehende@0.9.0 <command>"},{"kind":"ctx","oldNumber":16,"newNumber":16,"text":"```"},{"kind":"ctx","oldNumber":17,"newNumber":17,"text":""},{"kind":"ctx","oldNumber":18,"newNumber":18,"text":"The review document is interpretation only. It holds a title, groups, summaries, and hunk pointers. Do not copy patch text into it. `serve` reads the diff from git."}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":27,"oldLines":15,"newStart":27,"newLines":15,"header":"@@ -27,15 +27,15 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":27,"newNumber":27,"text":"   ```sh"},{"kind":"ctx","oldNumber":28,"newNumber":28,"text":"   REVIEW_DIR=$(mktemp -d) && git rev-parse --verify --end-of-options \"<base>^{commit}\" && git rev-parse --verify --end-of-options \"<head>^{commit}\" && {"},{"kind":"ctx","oldNumber":29,"newNumber":29,"text":"     npm view comprehende version || true"},{"kind":"del","oldNumber":30,"newNumber":null,"text":"     npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"add","oldNumber":null,"newNumber":30,"text":"     npx comprehende@0.9.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"ctx","oldNumber":31,"newNumber":31,"text":"     git log --format='%s%n%n%b' --end-of-options <base>...<head>"},{"kind":"ctx","oldNumber":32,"newNumber":32,"text":"     git diff --stat <base>...<head>"},{"kind":"ctx","oldNumber":33,"newNumber":33,"text":"   }"},{"kind":"ctx","oldNumber":34,"newNumber":34,"text":"   ```"},{"kind":"ctx","oldNumber":35,"newNumber":35,"text":""},{"kind":"del","oldNumber":36,"newNumber":null,"text":"   When the user names a local executable, use that command as written in place of `npx comprehende@0.8.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version."},{"kind":"add","oldNumber":null,"newNumber":36,"text":"   When the user names a local executable, use that command as written in place of `npx comprehende@0.9.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version."},{"kind":"ctx","oldNumber":37,"newNumber":37,"text":""},{"kind":"del","oldNumber":38,"newNumber":null,"text":"   If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"add","oldNumber":null,"newNumber":38,"text":"   If that version is newer than this pin (`npx comprehende@0.9.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec."},{"kind":"ctx","oldNumber":40,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":41,"text":"   ```sh"}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":69,"oldLines":8,"newStart":69,"newLines":8,"header":"@@ -69,8 +69,8 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":69,"newNumber":69,"text":"   ```"},{"kind":"ctx","oldNumber":70,"newNumber":70,"text":""},{"kind":"ctx","oldNumber":71,"newNumber":71,"text":"   Add a field when the section that defines it says to write it: document `why`, `lookFor`, and `parts` (`name`, `summary`); `sources` (`id`, `kind` of `ticket`, `pr`, `pr-comment`, `commit`, or `transcript`, `label`, plus `url`, `title`, `gist`, and `part` when you have them). On a group: `part`, `sources` (those ids), `lookFor`, `dependsOn` (group ids). A `pr-comment` source also has `author` and `body`. A line pin adds `path`, `side` (`old` or `new`), and `line` together. `size` is `trivial`, `small`, `medium`, `large`, or `very-large`."},{"kind":"del","oldNumber":72,"newNumber":null,"text":"6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"del","oldNumber":73,"newNumber":null,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"add","oldNumber":null,"newNumber":72,"text":"6. Run `npx comprehende@0.9.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"add","oldNumber":null,"newNumber":73,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.9.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":74,"newNumber":74,"text":""},{"kind":"ctx","oldNumber":75,"newNumber":75,"text":"`references/example.md` is an optional filled sample of the field shape. The shape in step 5 is enough to write the document."},{"kind":"ctx","oldNumber":76,"newNumber":76,"text":""}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":79,"oldLines":7,"newStart":79,"newLines":7,"header":"@@ -79,7 +79,7 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":79,"newNumber":79,"text":"Write a static site and put that folder where they asked."},{"kind":"ctx","oldNumber":80,"newNumber":80,"text":""},{"kind":"ctx","oldNumber":81,"newNumber":81,"text":"```sh"},{"kind":"del","oldNumber":82,"newNumber":null,"text":"npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"add","oldNumber":null,"newNumber":82,"text":"npx comprehende@0.9.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"ctx","oldNumber":83,"newNumber":83,"text":"```"},{"kind":"ctx","oldNumber":84,"newNumber":84,"text":""},{"kind":"ctx","oldNumber":85,"newNumber":85,"text":"`$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is still a skeleton, finish the Workflow through validate, then export."}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":12,"oldLines":7,"newStart":12,"newLines":7,"header":"@@ -12,7 +12,7 @@ Group a git diff into review concerns and serve a local UI. The point is cogniti","language":"markdown","lines":[{"kind":"ctx","oldNumber":12,"newNumber":12,"text":"Run every command inside the repository under review. Cwd is the repo; there is no `--repo` flag. Use the pinned CLI:"},{"kind":"ctx","oldNumber":13,"newNumber":13,"text":""},{"kind":"ctx","oldNumber":14,"newNumber":14,"text":"```sh"},{"kind":"del","oldNumber":15,"newNumber":null,"text":"npx comprehende@0.8.0 <command>"},{"kind":"add","oldNumber":null,"newNumber":15,"text":"npx comprehende@0.9.0 <command>"},{"kind":"ctx","oldNumber":16,"newNumber":16,"text":"```"},{"kind":"ctx","oldNumber":17,"newNumber":17,"text":""},{"kind":"ctx","oldNumber":18,"newNumber":18,"text":"The review document is interpretation only. It holds a title, groups, summaries, and hunk pointers. Do not copy patch text into it. `serve` reads the diff from git."}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":27,"oldLines":15,"newStart":27,"newLines":15,"header":"@@ -27,15 +27,15 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":27,"newNumber":27,"text":"   ```sh"},{"kind":"ctx","oldNumber":28,"newNumber":28,"text":"   REVIEW_DIR=$(mktemp -d) && git rev-parse --verify --end-of-options \"<base>^{commit}\" && git rev-parse --verify --end-of-options \"<head>^{commit}\" && {"},{"kind":"ctx","oldNumber":29,"newNumber":29,"text":"     npm view comprehende version || true"},{"kind":"del","oldNumber":30,"newNumber":null,"text":"     npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"add","oldNumber":null,"newNumber":30,"text":"     npx comprehende@0.9.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"ctx","oldNumber":31,"newNumber":31,"text":"     git log --format='%s%n%n%b' --end-of-options <base>...<head>"},{"kind":"ctx","oldNumber":32,"newNumber":32,"text":"     git diff --stat <base>...<head>"},{"kind":"ctx","oldNumber":33,"newNumber":33,"text":"   }"},{"kind":"ctx","oldNumber":34,"newNumber":34,"text":"   ```"},{"kind":"ctx","oldNumber":35,"newNumber":35,"text":""},{"kind":"del","oldNumber":36,"newNumber":null,"text":"   When the user names a local executable, use that command as written in place of `npx comprehende@0.8.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version."},{"kind":"add","oldNumber":null,"newNumber":36,"text":"   When the user names a local executable, use that command as written in place of `npx comprehende@0.9.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version."},{"kind":"ctx","oldNumber":37,"newNumber":37,"text":""},{"kind":"del","oldNumber":38,"newNumber":null,"text":"   If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"add","oldNumber":null,"newNumber":38,"text":"   If that version is newer than this pin (`npx comprehende@0.9.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec."},{"kind":"ctx","oldNumber":40,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":41,"text":"   ```sh"}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":69,"oldLines":8,"newStart":69,"newLines":8,"header":"@@ -69,8 +69,8 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":69,"newNumber":69,"text":"   ```"},{"kind":"ctx","oldNumber":70,"newNumber":70,"text":""},{"kind":"ctx","oldNumber":71,"newNumber":71,"text":"   Add a field when the section that defines it says to write it: document `why`, `lookFor`, and `parts` (`name`, `summary`); `sources` (`id`, `kind` of `ticket`, `pr`, `pr-comment`, `commit`, or `transcript`, `label`, plus `url`, `title`, `gist`, and `part` when you have them). On a group: `part`, `sources` (those ids), `lookFor`, `dependsOn` (group ids). A `pr-comment` source also has `author` and `body`. A line pin adds `path`, `side` (`old` or `new`), and `line` together. `size` is `trivial`, `small`, `medium`, `large`, or `very-large`."},{"kind":"del","oldNumber":72,"newNumber":null,"text":"6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"del","oldNumber":73,"newNumber":null,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"add","oldNumber":null,"newNumber":72,"text":"6. Run `npx comprehende@0.9.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"add","oldNumber":null,"newNumber":73,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.9.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":74,"newNumber":74,"text":""},{"kind":"ctx","oldNumber":75,"newNumber":75,"text":"`references/example.md` is an optional filled sample of the field shape. The shape in step 5 is enough to write the document."},{"kind":"ctx","oldNumber":76,"newNumber":76,"text":""}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":79,"oldLines":7,"newStart":79,"newLines":7,"header":"@@ -79,7 +79,7 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":79,"newNumber":79,"text":"Write a static site and put that folder where they asked."},{"kind":"ctx","oldNumber":80,"newNumber":80,"text":""},{"kind":"ctx","oldNumber":81,"newNumber":81,"text":"```sh"},{"kind":"del","oldNumber":82,"newNumber":null,"text":"npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"add","oldNumber":null,"newNumber":82,"text":"npx comprehende@0.9.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"ctx","oldNumber":83,"newNumber":83,"text":"```"},{"kind":"ctx","oldNumber":84,"newNumber":84,"text":""},{"kind":"ctx","oldNumber":85,"newNumber":85,"text":"`$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is still a skeleton, finish the Workflow through validate, then export."}]}],"files":[{"path":"package.json","kind":"text","status":"modified","patch":"diff --git a/package.json b/package.json\nindex 2b0ce2a..07f96ee 100644\n--- a/package.json\n+++ b/package.json\n@@ -1,6 +1,6 @@\n {\n   \"name\": \"comprehende\",\n-  \"version\": \"0.8.0\",\n+  \"version\": \"0.9.0\",\n   \"description\": \"Local review assistant that groups git diffs so humans can comprehend AI code changes.\",\n   \"type\": \"module\",\n   \"bin\": {\n","complete":true,"hunks":[{"path":"package.json","oldStart":1,"oldLines":6,"newStart":1,"newLines":6,"header":"@@ -1,6 +1,6 @@","language":"json","lines":[{"kind":"ctx","oldNumber":1,"newNumber":1,"text":"{"},{"kind":"ctx","oldNumber":2,"newNumber":2,"text":"  \"name\": \"comprehende\","},{"kind":"del","oldNumber":3,"newNumber":null,"text":"  \"version\": \"0.8.0\","},{"kind":"add","oldNumber":null,"newNumber":3,"text":"  \"version\": \"0.9.0\","},{"kind":"ctx","oldNumber":4,"newNumber":4,"text":"  \"description\": \"Local review assistant that groups git diffs so humans can comprehend AI code changes.\","},{"kind":"ctx","oldNumber":5,"newNumber":5,"text":"  \"type\": \"module\","},{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"  \"bin\": {"}]}]},{"path":"skills-next/comprehende/SKILL.md","kind":"text","status":"modified","patch":"diff --git a/skills-next/comprehende/SKILL.md b/skills-next/comprehende/SKILL.md\nindex 9f77e85..ce6cc8f 100644\n--- a/skills-next/comprehende/SKILL.md\n+++ b/skills-next/comprehende/SKILL.md\n@@ -12,7 +12,7 @@ Group a git diff into review concerns and serve a local UI. The point is cogniti\n Run every command inside the repository under review. Cwd is the repo; there is no `--repo` flag. Use the pinned CLI:\n \n ```sh\n-npx comprehende@0.8.0 <command>\n+npx comprehende@0.9.0 <command>\n ```\n \n The review document is interpretation only. It holds a title, groups, summaries, and hunk pointers. Do not copy patch text into it. `serve` reads the diff from git.\n@@ -27,15 +27,15 @@ Each step is one tool call where the step says so. Batch the commands as written\n    ```sh\n    REVIEW_DIR=$(mktemp -d) && git rev-parse --verify --end-of-options \"<base>^{commit}\" && git rev-parse --verify --end-of-options \"<head>^{commit}\" && {\n      npm view comprehende version || true\n-     npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\"\n+     npx comprehende@0.9.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\"\n      git log --format='%s%n%n%b' --end-of-options <base>...<head>\n      git diff --stat <base>...<head>\n    }\n    ```\n \n-   When the user names a local executable, use that command as written in place of `npx comprehende@0.8.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version.\n+   When the user names a local executable, use that command as written in place of `npx comprehende@0.9.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version.\n \n-   If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file.\n+   If that version is newer than this pin (`npx comprehende@0.9.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file.\n 3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec.\n \n    ```sh\n@@ -69,8 +69,8 @@ Each step is one tool call where the step says so. Batch the commands as written\n    ```\n \n    Add a field when the section that defines it says to write it: document `why`, `lookFor`, and `parts` (`name`, `summary`); `sources` (`id`, `kind` of `ticket`, `pr`, `pr-comment`, `commit`, or `transcript`, `label`, plus `url`, `title`, `gist`, and `part` when you have them). On a group: `part`, `sources` (those ids), `lookFor`, `dependsOn` (group ids). A `pr-comment` source also has `author` and `body`. A line pin adds `path`, `side` (`old` or `new`), and `line` together. `size` is `trivial`, `small`, `medium`, `large`, or `very-large`.\n-6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0.\n-7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n+6. Run `npx comprehende@0.9.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0.\n+7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.9.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n \n `references/example.md` is an optional filled sample of the field shape. The shape in step 5 is enough to write the document.\n \n@@ -79,7 +79,7 @@ Each step is one tool call where the step says so. Batch the commands as written\n Write a static site and put that folder where they asked.\n \n ```sh\n-npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\"\n+npx comprehende@0.9.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\"\n ```\n \n `$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is still a skeleton, finish the Workflow through validate, then export.\n","complete":true,"hunks":[{"path":"skills-next/comprehende/SKILL.md","oldStart":12,"oldLines":7,"newStart":12,"newLines":7,"header":"@@ -12,7 +12,7 @@ Group a git diff into review concerns and serve a local UI. The point is cogniti","language":"markdown","lines":[{"kind":"ctx","oldNumber":12,"newNumber":12,"text":"Run every command inside the repository under review. Cwd is the repo; there is no `--repo` flag. Use the pinned CLI:"},{"kind":"ctx","oldNumber":13,"newNumber":13,"text":""},{"kind":"ctx","oldNumber":14,"newNumber":14,"text":"```sh"},{"kind":"del","oldNumber":15,"newNumber":null,"text":"npx comprehende@0.8.0 <command>"},{"kind":"add","oldNumber":null,"newNumber":15,"text":"npx comprehende@0.9.0 <command>"},{"kind":"ctx","oldNumber":16,"newNumber":16,"text":"```"},{"kind":"ctx","oldNumber":17,"newNumber":17,"text":""},{"kind":"ctx","oldNumber":18,"newNumber":18,"text":"The review document is interpretation only. It holds a title, groups, summaries, and hunk pointers. Do not copy patch text into it. `serve` reads the diff from git."}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":27,"oldLines":15,"newStart":27,"newLines":15,"header":"@@ -27,15 +27,15 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":27,"newNumber":27,"text":"   ```sh"},{"kind":"ctx","oldNumber":28,"newNumber":28,"text":"   REVIEW_DIR=$(mktemp -d) && git rev-parse --verify --end-of-options \"<base>^{commit}\" && git rev-parse --verify --end-of-options \"<head>^{commit}\" && {"},{"kind":"ctx","oldNumber":29,"newNumber":29,"text":"     npm view comprehende version || true"},{"kind":"del","oldNumber":30,"newNumber":null,"text":"     npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"add","oldNumber":null,"newNumber":30,"text":"     npx comprehende@0.9.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"ctx","oldNumber":31,"newNumber":31,"text":"     git log --format='%s%n%n%b' --end-of-options <base>...<head>"},{"kind":"ctx","oldNumber":32,"newNumber":32,"text":"     git diff --stat <base>...<head>"},{"kind":"ctx","oldNumber":33,"newNumber":33,"text":"   }"},{"kind":"ctx","oldNumber":34,"newNumber":34,"text":"   ```"},{"kind":"ctx","oldNumber":35,"newNumber":35,"text":""},{"kind":"del","oldNumber":36,"newNumber":null,"text":"   When the user names a local executable, use that command as written in place of `npx comprehende@0.8.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version."},{"kind":"add","oldNumber":null,"newNumber":36,"text":"   When the user names a local executable, use that command as written in place of `npx comprehende@0.9.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version."},{"kind":"ctx","oldNumber":37,"newNumber":37,"text":""},{"kind":"del","oldNumber":38,"newNumber":null,"text":"   If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"add","oldNumber":null,"newNumber":38,"text":"   If that version is newer than this pin (`npx comprehende@0.9.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec."},{"kind":"ctx","oldNumber":40,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":41,"text":"   ```sh"}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":69,"oldLines":8,"newStart":69,"newLines":8,"header":"@@ -69,8 +69,8 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":69,"newNumber":69,"text":"   ```"},{"kind":"ctx","oldNumber":70,"newNumber":70,"text":""},{"kind":"ctx","oldNumber":71,"newNumber":71,"text":"   Add a field when the section that defines it says to write it: document `why`, `lookFor`, and `parts` (`name`, `summary`); `sources` (`id`, `kind` of `ticket`, `pr`, `pr-comment`, `commit`, or `transcript`, `label`, plus `url`, `title`, `gist`, and `part` when you have them). On a group: `part`, `sources` (those ids), `lookFor`, `dependsOn` (group ids). A `pr-comment` source also has `author` and `body`. A line pin adds `path`, `side` (`old` or `new`), and `line` together. `size` is `trivial`, `small`, `medium`, `large`, or `very-large`."},{"kind":"del","oldNumber":72,"newNumber":null,"text":"6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"del","oldNumber":73,"newNumber":null,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"add","oldNumber":null,"newNumber":72,"text":"6. Run `npx comprehende@0.9.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"add","oldNumber":null,"newNumber":73,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.9.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":74,"newNumber":74,"text":""},{"kind":"ctx","oldNumber":75,"newNumber":75,"text":"`references/example.md` is an optional filled sample of the field shape. The shape in step 5 is enough to write the document."},{"kind":"ctx","oldNumber":76,"newNumber":76,"text":""}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":79,"oldLines":7,"newStart":79,"newLines":7,"header":"@@ -79,7 +79,7 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":79,"newNumber":79,"text":"Write a static site and put that folder where they asked."},{"kind":"ctx","oldNumber":80,"newNumber":80,"text":""},{"kind":"ctx","oldNumber":81,"newNumber":81,"text":"```sh"},{"kind":"del","oldNumber":82,"newNumber":null,"text":"npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"add","oldNumber":null,"newNumber":82,"text":"npx comprehende@0.9.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"ctx","oldNumber":83,"newNumber":83,"text":"```"},{"kind":"ctx","oldNumber":84,"newNumber":84,"text":""},{"kind":"ctx","oldNumber":85,"newNumber":85,"text":"`$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is still a skeleton, finish the Workflow through validate, then export."}]}]},{"path":".agents/skills/comprehende/SKILL.md","kind":"text","status":"modified","patch":"diff --git a/.agents/skills/comprehende/SKILL.md b/.agents/skills/comprehende/SKILL.md\nindex 9f77e85..ce6cc8f 100644\n--- a/.agents/skills/comprehende/SKILL.md\n+++ b/.agents/skills/comprehende/SKILL.md\n@@ -12,7 +12,7 @@ Group a git diff into review concerns and serve a local UI. The point is cogniti\n Run every command inside the repository under review. Cwd is the repo; there is no `--repo` flag. Use the pinned CLI:\n \n ```sh\n-npx comprehende@0.8.0 <command>\n+npx comprehende@0.9.0 <command>\n ```\n \n The review document is interpretation only. It holds a title, groups, summaries, and hunk pointers. Do not copy patch text into it. `serve` reads the diff from git.\n@@ -27,15 +27,15 @@ Each step is one tool call where the step says so. Batch the commands as written\n    ```sh\n    REVIEW_DIR=$(mktemp -d) && git rev-parse --verify --end-of-options \"<base>^{commit}\" && git rev-parse --verify --end-of-options \"<head>^{commit}\" && {\n      npm view comprehende version || true\n-     npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\"\n+     npx comprehende@0.9.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\"\n      git log --format='%s%n%n%b' --end-of-options <base>...<head>\n      git diff --stat <base>...<head>\n    }\n    ```\n \n-   When the user names a local executable, use that command as written in place of `npx comprehende@0.8.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version.\n+   When the user names a local executable, use that command as written in place of `npx comprehende@0.9.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version.\n \n-   If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file.\n+   If that version is newer than this pin (`npx comprehende@0.9.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file.\n 3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec.\n \n    ```sh\n@@ -69,8 +69,8 @@ Each step is one tool call where the step says so. Batch the commands as written\n    ```\n \n    Add a field when the section that defines it says to write it: document `why`, `lookFor`, and `parts` (`name`, `summary`); `sources` (`id`, `kind` of `ticket`, `pr`, `pr-comment`, `commit`, or `transcript`, `label`, plus `url`, `title`, `gist`, and `part` when you have them). On a group: `part`, `sources` (those ids), `lookFor`, `dependsOn` (group ids). A `pr-comment` source also has `author` and `body`. A line pin adds `path`, `side` (`old` or `new`), and `line` together. `size` is `trivial`, `small`, `medium`, `large`, or `very-large`.\n-6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0.\n-7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n+6. Run `npx comprehende@0.9.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0.\n+7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.9.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n \n `references/example.md` is an optional filled sample of the field shape. The shape in step 5 is enough to write the document.\n \n@@ -79,7 +79,7 @@ Each step is one tool call where the step says so. Batch the commands as written\n Write a static site and put that folder where they asked.\n \n ```sh\n-npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\"\n+npx comprehende@0.9.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\"\n ```\n \n `$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is still a skeleton, finish the Workflow through validate, then export.\n","complete":true,"hunks":[{"path":".agents/skills/comprehende/SKILL.md","oldStart":12,"oldLines":7,"newStart":12,"newLines":7,"header":"@@ -12,7 +12,7 @@ Group a git diff into review concerns and serve a local UI. The point is cogniti","language":"markdown","lines":[{"kind":"ctx","oldNumber":12,"newNumber":12,"text":"Run every command inside the repository under review. Cwd is the repo; there is no `--repo` flag. Use the pinned CLI:"},{"kind":"ctx","oldNumber":13,"newNumber":13,"text":""},{"kind":"ctx","oldNumber":14,"newNumber":14,"text":"```sh"},{"kind":"del","oldNumber":15,"newNumber":null,"text":"npx comprehende@0.8.0 <command>"},{"kind":"add","oldNumber":null,"newNumber":15,"text":"npx comprehende@0.9.0 <command>"},{"kind":"ctx","oldNumber":16,"newNumber":16,"text":"```"},{"kind":"ctx","oldNumber":17,"newNumber":17,"text":""},{"kind":"ctx","oldNumber":18,"newNumber":18,"text":"The review document is interpretation only. It holds a title, groups, summaries, and hunk pointers. Do not copy patch text into it. `serve` reads the diff from git."}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":27,"oldLines":15,"newStart":27,"newLines":15,"header":"@@ -27,15 +27,15 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":27,"newNumber":27,"text":"   ```sh"},{"kind":"ctx","oldNumber":28,"newNumber":28,"text":"   REVIEW_DIR=$(mktemp -d) && git rev-parse --verify --end-of-options \"<base>^{commit}\" && git rev-parse --verify --end-of-options \"<head>^{commit}\" && {"},{"kind":"ctx","oldNumber":29,"newNumber":29,"text":"     npm view comprehende version || true"},{"kind":"del","oldNumber":30,"newNumber":null,"text":"     npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"add","oldNumber":null,"newNumber":30,"text":"     npx comprehende@0.9.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"ctx","oldNumber":31,"newNumber":31,"text":"     git log --format='%s%n%n%b' --end-of-options <base>...<head>"},{"kind":"ctx","oldNumber":32,"newNumber":32,"text":"     git diff --stat <base>...<head>"},{"kind":"ctx","oldNumber":33,"newNumber":33,"text":"   }"},{"kind":"ctx","oldNumber":34,"newNumber":34,"text":"   ```"},{"kind":"ctx","oldNumber":35,"newNumber":35,"text":""},{"kind":"del","oldNumber":36,"newNumber":null,"text":"   When the user names a local executable, use that command as written in place of `npx comprehende@0.8.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version."},{"kind":"add","oldNumber":null,"newNumber":36,"text":"   When the user names a local executable, use that command as written in place of `npx comprehende@0.9.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version."},{"kind":"ctx","oldNumber":37,"newNumber":37,"text":""},{"kind":"del","oldNumber":38,"newNumber":null,"text":"   If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"add","oldNumber":null,"newNumber":38,"text":"   If that version is newer than this pin (`npx comprehende@0.9.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec."},{"kind":"ctx","oldNumber":40,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":41,"text":"   ```sh"}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":69,"oldLines":8,"newStart":69,"newLines":8,"header":"@@ -69,8 +69,8 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":69,"newNumber":69,"text":"   ```"},{"kind":"ctx","oldNumber":70,"newNumber":70,"text":""},{"kind":"ctx","oldNumber":71,"newNumber":71,"text":"   Add a field when the section that defines it says to write it: document `why`, `lookFor`, and `parts` (`name`, `summary`); `sources` (`id`, `kind` of `ticket`, `pr`, `pr-comment`, `commit`, or `transcript`, `label`, plus `url`, `title`, `gist`, and `part` when you have them). On a group: `part`, `sources` (those ids), `lookFor`, `dependsOn` (group ids). A `pr-comment` source also has `author` and `body`. A line pin adds `path`, `side` (`old` or `new`), and `line` together. `size` is `trivial`, `small`, `medium`, `large`, or `very-large`."},{"kind":"del","oldNumber":72,"newNumber":null,"text":"6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"del","oldNumber":73,"newNumber":null,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"add","oldNumber":null,"newNumber":72,"text":"6. Run `npx comprehende@0.9.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"add","oldNumber":null,"newNumber":73,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.9.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":74,"newNumber":74,"text":""},{"kind":"ctx","oldNumber":75,"newNumber":75,"text":"`references/example.md` is an optional filled sample of the field shape. The shape in step 5 is enough to write the document."},{"kind":"ctx","oldNumber":76,"newNumber":76,"text":""}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":79,"oldLines":7,"newStart":79,"newLines":7,"header":"@@ -79,7 +79,7 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":79,"newNumber":79,"text":"Write a static site and put that folder where they asked."},{"kind":"ctx","oldNumber":80,"newNumber":80,"text":""},{"kind":"ctx","oldNumber":81,"newNumber":81,"text":"```sh"},{"kind":"del","oldNumber":82,"newNumber":null,"text":"npx comprehende@0.8.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"add","oldNumber":null,"newNumber":82,"text":"npx comprehende@0.9.0 export --data \"$REVIEW_DIR/review.json\" --out \"$EXPORT_DIR\""},{"kind":"ctx","oldNumber":83,"newNumber":83,"text":"```"},{"kind":"ctx","oldNumber":84,"newNumber":84,"text":""},{"kind":"ctx","oldNumber":85,"newNumber":85,"text":"`$EXPORT_DIR` is a fresh directory outside the work tree, not a git repository. The folder is the UI plus frozen git payloads. There is no git in it. Done when they have the URL or path they named. If `review.json` is still a skeleton, finish the Workflow through validate, then export."}]}]}]}
