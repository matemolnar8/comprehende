{"hunks":[{"path":".agents/skills/comprehende/SKILL.md","oldStart":43,"oldLines":8,"newStart":43,"newLines":8,"header":"@@ -43,8 +43,8 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":43,"newNumber":43,"text":"   ```"},{"kind":"ctx","oldNumber":44,"newNumber":44,"text":""},{"kind":"ctx","oldNumber":45,"newNumber":45,"text":"   Sources are listed under The why and The title. Done when the diff and those sources are in context."},{"kind":"del","oldNumber":46,"newNumber":null,"text":"4. Read the skeleton once, for its paths. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"del","oldNumber":47,"newNumber":null,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Write the whole `review.json` in one write from the field shape below. Keep `version` and `source` from the skeleton. The skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"add","oldNumber":null,"newNumber":46,"text":"4. Read the skeleton once, for its paths. Recover the why, write the title, write the what, and check each source item against the code at head (see lookFor). Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when an item diverges; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every source item has a verdict from the code at head and every divergence is a document `lookFor` bullet."},{"kind":"add","oldNumber":null,"newNumber":47,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Write the whole `review.json` in one write from the field shape below. Keep `version` and `source` from the skeleton. The skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry. Also done when each test file sits in the group of the code it checks (unless tests are the concern), each one-line call site or declaration sits with the code it serves, work no source asks for has its own `part`, and each `dependsOn` id names a group in the same `part`. When a story builds on another part, `suggestedOrder` puts that part first."},{"kind":"ctx","oldNumber":48,"newNumber":48,"text":""},{"kind":"ctx","oldNumber":49,"newNumber":49,"text":"   Field shape. The words are placeholders. This shape is enough to write a valid document."},{"kind":"ctx","oldNumber":50,"newNumber":50,"text":""}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":94,"oldLines":7,"newStart":94,"newLines":7,"header":"@@ -94,7 +94,7 @@ Invent a title when those sources are missing, vague, or name something else. A","language":"markdown","lines":[{"kind":"ctx","oldNumber":94,"newNumber":94,"text":""},{"kind":"ctx","oldNumber":95,"newNumber":95,"text":"## The why"},{"kind":"ctx","oldNumber":96,"newNumber":96,"text":""},{"kind":"del","oldNumber":97,"newNumber":null,"text":"Document `why` names why the work exists, in one or two sentences, inferred only from the sources below. If those sources are silent or mixed, omit it. Do not derive document `why` from the diff, from group structure, or from a motive you infer from the patch. A request naming two unrelated product stories with no unifying source also gets no document `why`; each story keeps its own group `why`s."},{"kind":"add","oldNumber":null,"newNumber":97,"text":"Document `why` names why the work exists, in one or two sentences, inferred only from the sources below. It is the problem or goal a source states. Before writing it, find the source sentence that states that problem or goal: \"Support SSO login\", \"so admins can export audit logs\", \"retries flood the API\". A sentence that only says what the change does (\"the settings page becomes a modal\", \"uploads move to S3\") belongs in the summary. When no source sentence states a problem or goal, or the sources are mixed, omit the why. Write it in the source's own terms, with no cause, benefit, or reader need the source does not name. The same holds for every group `why`: a citation carries only what that source says, and a source is the reason for a choice only when it states that reason. Do not derive document `why` from the diff, from group structure, or from a motive you infer from the patch. A request naming two unrelated product stories with no unifying source also gets no document `why`; each story keeps its own group `why`s."},{"kind":"ctx","oldNumber":98,"newNumber":98,"text":""},{"kind":"ctx","oldNumber":99,"newNumber":99,"text":"Sources are read, never copied into the review document, except PR review comments: copy `author` and `body` so the UI can show the quote. Keep ticket bodies, request descriptions, commit text, and transcript text out of `review.json`. Cite a source in prose with `[text](source:s1)` when that sentence leans on it."},{"kind":"ctx","oldNumber":100,"newNumber":100,"text":""}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":119,"oldLines":10,"newStart":119,"newLines":11,"header":"@@ -119,10 +119,11 @@ Group `summary` is one sentence saying what the group is, describing how its hun","language":"markdown","lines":[{"kind":"ctx","oldNumber":119,"newNumber":119,"text":"## Grouping rules"},{"kind":"ctx","oldNumber":120,"newNumber":120,"text":""},{"kind":"ctx","oldNumber":121,"newNumber":121,"text":"- Group by review concern: why these hunks are read together. A directory qualifies only when that directory is the concern. Grouping needs the diffs; the index and the log alone are not enough."},{"kind":"del","oldNumber":122,"newNumber":null,"text":"- A hunk may appear in several groups when it matters in more than one story."},{"kind":"del","oldNumber":123,"newNumber":null,"text":"- `dependsOn` marks a real dependency inside one story: the reader needs the earlier group to understand this one. Reading order inside a story: contracts and foundations, then call sites, then tests, then mechanical work. Independent work stands alone with its own `part`, empty `dependsOn`, and nothing depending on it; a second feature or standalone documentation that could have been its own PR is independent work. When unsure whether two concerns depend on each other, keep them separate under different parts: a false split is easy to see, a false chain hides a mixed PR."},{"kind":"add","oldNumber":null,"newNumber":122,"text":"- A group is one concern a reviewer names in a phrase. The code, the tests that check it, and the one-line call sites, declarations, and re-exports that serve it read together in that group. Tests are their own group only when they are the concern, or when one test covers several groups. A new feature's code, fixtures, helpers, and tests are one group; split a concern only where a reader would stop and review the first piece alone. A small change has one to three groups."},{"kind":"add","oldNumber":null,"newNumber":123,"text":"- A hunk may appear in several groups when it matters in more than one story. When one hunk holds two concerns, the group that holds it names both."},{"kind":"add","oldNumber":null,"newNumber":124,"text":"- `dependsOn` marks a real dependency inside one story: the reader needs the earlier group to understand this one. It stays inside one part. Reading order inside a story: contracts and foundations, then call sites, then mechanical work. Independent work stands alone with its own `part`, empty `dependsOn`, and nothing depending on it; a second feature, work no source asks for (extra product events, a dependency refresh), or standalone documentation that could have been its own PR is independent work. When unsure whether two concerns depend on each other, keep them separate under different parts: a false split is easy to see, a false chain hides a mixed PR."},{"kind":"ctx","oldNumber":124,"newNumber":125,"text":"- `part` is a short name (a few words) for one independent story, the same on every group in that story; the UI colors shared parts together. One document `parts[]` entry per name."},{"kind":"del","oldNumber":125,"newNumber":null,"text":"- Mechanical work (import reordering, identifier-only renames, generated code, formatting, type re-exports) is its own group that keeps every hunk as refs, risk visible rather than folded into a file list. When it exists only because of a story, it is that part's last group with nothing depending on it. When it could have been its own PR, it is its own part."},{"kind":"add","oldNumber":null,"newNumber":126,"text":"- Mechanical work (import reordering, identifier-only renames, generated code, formatting, pure moves) is its own group that keeps every hunk as refs, risk visible rather than folded into a file list. Its summary tells the reader what they can skip and why that is safe: a pure move, regenerated output that means the same as before. An identical copy of a file in the diff sits with its source, and the summary says it is identical. When it exists only because of a story, it is that part's last group with nothing depending on it. When it could have been its own PR, it is its own part."},{"kind":"ctx","oldNumber":126,"newNumber":127,"text":"- `suggestedOrder` walks the whole review, independent parts included. Mechanical parts, independent documentation, and test-only cleanup go last."},{"kind":"ctx","oldNumber":127,"newNumber":128,"text":"- Document `size` is human review burden: `trivial`, `small`, `medium`, `large`, `very-large`. Forty files changing one import in one group are `small`; three files rewriting a contract the rest of the system hangs on can be `large`."},{"kind":"ctx","oldNumber":128,"newNumber":129,"text":"- Coverage: every live hunk sits in at least one group. A path covers every live hunk of that file. Duplicate refs across groups are allowed. Unreferenced hunks fail `validate` and show as Unassigned in the UI."}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":133,"oldLines":11,"newStart":134,"newLines":11,"header":"@@ -133,11 +134,11 @@ A `hunkRefs` entry is a path string when the group holds every live hunk of that","language":"markdown","lines":[{"kind":"ctx","oldNumber":133,"newNumber":134,"text":""},{"kind":"ctx","oldNumber":134,"newNumber":135,"text":"## lookFor"},{"kind":"ctx","oldNumber":135,"newNumber":136,"text":""},{"kind":"del","oldNumber":136,"newNumber":null,"text":"`lookFor` is for what the live diff does not make obvious. Each bullet is one inspectable claim: optionally a short risk tag (Subtle, Breaking, Race, Perf), then one sentence. The bullet states the claim; the human checks it against the live diff and keeps the verdict. Pseudocode, diagrams, collapsed diffs, commit lists, hunk counts, and pass/fail marks stay out."},{"kind":"add","oldNumber":null,"newNumber":137,"text":"`lookFor` is for what a careful engineer would miss on a first read of the live diff. Each bullet is one finding, stated as fact: optionally a short risk tag (Subtle, Breaking, Race, Perf), then one sentence on what the code at head does and what the reader would observe. The human checks it against the live diff and keeps the verdict. Pseudocode, diagrams, collapsed diffs, commit lists, hunk counts, and pass/fail marks stay out."},{"kind":"ctx","oldNumber":137,"newNumber":138,"text":""},{"kind":"del","oldNumber":138,"newNumber":null,"text":"Group `lookFor` holds claims that live in those hunks. When a behavior change hides in the hunk, a predicted trace earns a bullet: a small realistic input, where the old and new paths diverge, and the observable result. Empty is correct for a rename, a formatting change, or a wording-only docs group; leave it empty rather than fill it."},{"kind":"add","oldNumber":null,"newNumber":139,"text":"Group `lookFor` holds claims that live in those hunks. When a behavior change hides in the hunk, a predicted trace earns a bullet: a small realistic input, where the old and new paths diverge, and the observable result. When a hunk works around app behavior (a sleep, a forced write, a retry), the bullet says what the app does without the workaround. Production code inside a test or tooling change earns a bullet. So does a behavior change no source mentions, and a hunk that resolves a review comment in a way the hunk alone does not make obvious: the bullet says what the fix does. Empty is correct for a rename, a formatting change, or a wording-only docs group; leave it empty rather than fill it."},{"kind":"ctx","oldNumber":139,"newNumber":140,"text":""},{"kind":"del","oldNumber":140,"newNumber":null,"text":"Document `lookFor` holds claims about the whole change, and work a source names that no hunk does. Compare each source with the diff. When a ticket, request, comment, or transcript names work, the bullet says what it asks for and what the diff does about it. Cite the source with `[text](source:s1)`; the id goes in `sources`, and `validate` fails on an unknown id. Omit the field when no source names expected work, or when the bullet would only restate document `summary`."},{"kind":"add","oldNumber":null,"newNumber":141,"text":"Document `lookFor` holds claims about the whole change, and the divergences between the sources and the code at head. Source items are each requirement and acceptance item in a ticket, each behavior claim in the pull request description and commit messages, and each request in a review comment. Test plans, manual QA notes, and out-of-scope lists are context, not items. Read the code at head for each item and give it a verdict: done as asked, done differently, not done, or not true at head. Each verdict other than done-as-asked is one bullet: what the source asks or says, what head does, and the reason when a source or the code gives one, or that no source gives one. When a later source changes what an earlier one asked (a review comment overrides the ticket), the ticket item is done differently, and the bullet names both sources. A manual step a source says the change needs (turn on a setting, run a migration) is also a bullet. Cite with `[text](source:s1)`; the id goes in `sources`, and `validate` fails on an unknown id. Omit the field when nothing needs a bullet."},{"kind":"ctx","oldNumber":141,"newNumber":142,"text":""},{"kind":"ctx","oldNumber":142,"newNumber":143,"text":"## Write the prose"},{"kind":"ctx","oldNumber":143,"newNumber":144,"text":""}]},{"path":".agents/skills/comprehende/references/example.md","oldStart":2,"oldLines":12,"newStart":2,"newLines":12,"header":"@@ -2,12 +2,12 @@","language":"markdown","lines":[{"kind":"ctx","oldNumber":2,"newNumber":2,"text":""},{"kind":"ctx","oldNumber":3,"newNumber":3,"text":"Optional filled sample. The field shape in `SKILL.md` is enough to write a document. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header."},{"kind":"ctx","oldNumber":4,"newNumber":4,"text":""},{"kind":"del","oldNumber":5,"newNumber":null,"text":"- `login` depends on `cookie`. Both use `part` \"Session cookie\"."},{"kind":"add","oldNumber":null,"newNumber":5,"text":"- `login` depends on `cookie`. Both use `part` \"Session cookie\". The helper's test sits in `cookie` with the helper it checks."},{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"- `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request."},{"kind":"ctx","oldNumber":7,"newNumber":7,"text":"- Ticket #12 names why this work exists, so document `why` is present. Document `title` keeps the ticket title. Document `summary` names both stories. Document `parts` holds a one-sentence what per story. The why cites the ticket with `[#12](source:s1)`."},{"kind":"ctx","oldNumber":8,"newNumber":8,"text":"- `login` `summary` names how those hunks meet. `login` `lookFor` is a predicted trace. `docs` has no `lookFor`."},{"kind":"ctx","oldNumber":9,"newNumber":9,"text":"- Document `lookFor` compares ticket #12 with the diff: work the ticket asks for that no hunk does, and one claim about the whole change. Both cite `[#12](source:s1)`. Group `lookFor` stays inside its hunks."},{"kind":"del","oldNumber":10,"newNumber":null,"text":"- Each group holds every hunk of its file, so each `hunkRefs` entry is that path. A split file uses `path@oldStart+newStart` (`old/path -> new/path@oldStart+newStart` when renamed)."},{"kind":"add","oldNumber":null,"newNumber":10,"text":"- Each group holds every hunk of its files, so each `hunkRefs` entry is a path. A split file uses `path@oldStart+newStart` (`old/path -> new/path@oldStart+newStart` when renamed)."},{"kind":"ctx","oldNumber":11,"newNumber":11,"text":""},{"kind":"ctx","oldNumber":12,"newNumber":12,"text":"```json"},{"kind":"ctx","oldNumber":13,"newNumber":13,"text":"{"}]},{"path":".agents/skills/comprehende/references/example.md","oldStart":50,"oldLines":13,"newStart":50,"newLines":13,"header":"@@ -50,13 +50,13 @@ Optional filled sample. The field shape in `SKILL.md` is enough to write a docum","language":"markdown","lines":[{"kind":"ctx","oldNumber":50,"newNumber":50,"text":"      \"id\": \"cookie\","},{"kind":"ctx","oldNumber":51,"newNumber":51,"text":"      \"title\": \"Session cookie helper\","},{"kind":"ctx","oldNumber":52,"newNumber":52,"text":"      \"why\": \"The login route needs one helper to apply the session cookie options.\","},{"kind":"del","oldNumber":53,"newNumber":null,"text":"      \"summary\": \"`setSessionCookie` applies the required options to session cookies.\","},{"kind":"add","oldNumber":null,"newNumber":53,"text":"      \"summary\": \"`setSessionCookie` applies the required options to session cookies, and its test checks them.\","},{"kind":"ctx","oldNumber":54,"newNumber":54,"text":"      \"part\": \"Session cookie\","},{"kind":"ctx","oldNumber":55,"newNumber":55,"text":"      \"lookFor\": ["},{"kind":"ctx","oldNumber":56,"newNumber":56,"text":"        \"Breaking. `setSessionCookie` throws when the caller passes `httpOnly: false`.\""},{"kind":"ctx","oldNumber":57,"newNumber":57,"text":"      ],"},{"kind":"ctx","oldNumber":58,"newNumber":58,"text":"      \"suggestedOrder\": 0,"},{"kind":"del","oldNumber":59,"newNumber":null,"text":"      \"hunkRefs\": [\"src/auth/session.ts\"]"},{"kind":"add","oldNumber":null,"newNumber":59,"text":"      \"hunkRefs\": [\"src/auth/session.ts\", \"src/auth/session.test.ts\"]"},{"kind":"ctx","oldNumber":60,"newNumber":60,"text":"    },"},{"kind":"ctx","oldNumber":61,"newNumber":61,"text":"    {"},{"kind":"ctx","oldNumber":62,"newNumber":62,"text":"      \"id\": \"login\","}]}],"files":[{"path":".agents/skills/comprehende/SKILL.md","kind":"text","status":"modified","patch":"diff --git a/.agents/skills/comprehende/SKILL.md b/.agents/skills/comprehende/SKILL.md\nindex ce6cc8f..e11c7fe 100644\n--- a/.agents/skills/comprehende/SKILL.md\n+++ b/.agents/skills/comprehende/SKILL.md\n@@ -43,8 +43,8 @@ Each step is one tool call where the step says so. Batch the commands as written\n    ```\n \n    Sources are listed under The why and The title. Done when the diff and those sources are in context.\n-4. Read the skeleton once, for its paths. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet.\n-5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Write the whole `review.json` in one write from the field shape below. Keep `version` and `source` from the skeleton. The skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry.\n+4. Read the skeleton once, for its paths. Recover the why, write the title, write the what, and check each source item against the code at head (see lookFor). Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when an item diverges; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every source item has a verdict from the code at head and every divergence is a document `lookFor` bullet.\n+5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Write the whole `review.json` in one write from the field shape below. Keep `version` and `source` from the skeleton. The skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry. Also done when each test file sits in the group of the code it checks (unless tests are the concern), each one-line call site or declaration sits with the code it serves, work no source asks for has its own `part`, and each `dependsOn` id names a group in the same `part`. When a story builds on another part, `suggestedOrder` puts that part first.\n \n    Field shape. The words are placeholders. This shape is enough to write a valid document.\n \n@@ -94,7 +94,7 @@ Invent a title when those sources are missing, vague, or name something else. A\n \n ## The why\n \n-Document `why` names why the work exists, in one or two sentences, inferred only from the sources below. If those sources are silent or mixed, omit it. Do not derive document `why` from the diff, from group structure, or from a motive you infer from the patch. A request naming two unrelated product stories with no unifying source also gets no document `why`; each story keeps its own group `why`s.\n+Document `why` names why the work exists, in one or two sentences, inferred only from the sources below. It is the problem or goal a source states. Before writing it, find the source sentence that states that problem or goal: \"Support SSO login\", \"so admins can export audit logs\", \"retries flood the API\". A sentence that only says what the change does (\"the settings page becomes a modal\", \"uploads move to S3\") belongs in the summary. When no source sentence states a problem or goal, or the sources are mixed, omit the why. Write it in the source's own terms, with no cause, benefit, or reader need the source does not name. The same holds for every group `why`: a citation carries only what that source says, and a source is the reason for a choice only when it states that reason. Do not derive document `why` from the diff, from group structure, or from a motive you infer from the patch. A request naming two unrelated product stories with no unifying source also gets no document `why`; each story keeps its own group `why`s.\n \n Sources are read, never copied into the review document, except PR review comments: copy `author` and `body` so the UI can show the quote. Keep ticket bodies, request descriptions, commit text, and transcript text out of `review.json`. Cite a source in prose with `[text](source:s1)` when that sentence leans on it.\n \n@@ -119,10 +119,11 @@ Group `summary` is one sentence saying what the group is, describing how its hun\n ## Grouping rules\n \n - Group by review concern: why these hunks are read together. A directory qualifies only when that directory is the concern. Grouping needs the diffs; the index and the log alone are not enough.\n-- A hunk may appear in several groups when it matters in more than one story.\n-- `dependsOn` marks a real dependency inside one story: the reader needs the earlier group to understand this one. Reading order inside a story: contracts and foundations, then call sites, then tests, then mechanical work. Independent work stands alone with its own `part`, empty `dependsOn`, and nothing depending on it; a second feature or standalone documentation that could have been its own PR is independent work. When unsure whether two concerns depend on each other, keep them separate under different parts: a false split is easy to see, a false chain hides a mixed PR.\n+- A group is one concern a reviewer names in a phrase. The code, the tests that check it, and the one-line call sites, declarations, and re-exports that serve it read together in that group. Tests are their own group only when they are the concern, or when one test covers several groups. A new feature's code, fixtures, helpers, and tests are one group; split a concern only where a reader would stop and review the first piece alone. A small change has one to three groups.\n+- A hunk may appear in several groups when it matters in more than one story. When one hunk holds two concerns, the group that holds it names both.\n+- `dependsOn` marks a real dependency inside one story: the reader needs the earlier group to understand this one. It stays inside one part. Reading order inside a story: contracts and foundations, then call sites, then mechanical work. Independent work stands alone with its own `part`, empty `dependsOn`, and nothing depending on it; a second feature, work no source asks for (extra product events, a dependency refresh), or standalone documentation that could have been its own PR is independent work. When unsure whether two concerns depend on each other, keep them separate under different parts: a false split is easy to see, a false chain hides a mixed PR.\n - `part` is a short name (a few words) for one independent story, the same on every group in that story; the UI colors shared parts together. One document `parts[]` entry per name.\n-- Mechanical work (import reordering, identifier-only renames, generated code, formatting, type re-exports) is its own group that keeps every hunk as refs, risk visible rather than folded into a file list. When it exists only because of a story, it is that part's last group with nothing depending on it. When it could have been its own PR, it is its own part.\n+- Mechanical work (import reordering, identifier-only renames, generated code, formatting, pure moves) is its own group that keeps every hunk as refs, risk visible rather than folded into a file list. Its summary tells the reader what they can skip and why that is safe: a pure move, regenerated output that means the same as before. An identical copy of a file in the diff sits with its source, and the summary says it is identical. When it exists only because of a story, it is that part's last group with nothing depending on it. When it could have been its own PR, it is its own part.\n - `suggestedOrder` walks the whole review, independent parts included. Mechanical parts, independent documentation, and test-only cleanup go last.\n - Document `size` is human review burden: `trivial`, `small`, `medium`, `large`, `very-large`. Forty files changing one import in one group are `small`; three files rewriting a contract the rest of the system hangs on can be `large`.\n - Coverage: every live hunk sits in at least one group. A path covers every live hunk of that file. Duplicate refs across groups are allowed. Unreferenced hunks fail `validate` and show as Unassigned in the UI.\n@@ -133,11 +134,11 @@ A `hunkRefs` entry is a path string when the group holds every live hunk of that\n \n ## lookFor\n \n-`lookFor` is for what the live diff does not make obvious. Each bullet is one inspectable claim: optionally a short risk tag (Subtle, Breaking, Race, Perf), then one sentence. The bullet states the claim; the human checks it against the live diff and keeps the verdict. Pseudocode, diagrams, collapsed diffs, commit lists, hunk counts, and pass/fail marks stay out.\n+`lookFor` is for what a careful engineer would miss on a first read of the live diff. Each bullet is one finding, stated as fact: optionally a short risk tag (Subtle, Breaking, Race, Perf), then one sentence on what the code at head does and what the reader would observe. The human checks it against the live diff and keeps the verdict. Pseudocode, diagrams, collapsed diffs, commit lists, hunk counts, and pass/fail marks stay out.\n \n-Group `lookFor` holds claims that live in those hunks. When a behavior change hides in the hunk, a predicted trace earns a bullet: a small realistic input, where the old and new paths diverge, and the observable result. Empty is correct for a rename, a formatting change, or a wording-only docs group; leave it empty rather than fill it.\n+Group `lookFor` holds claims that live in those hunks. When a behavior change hides in the hunk, a predicted trace earns a bullet: a small realistic input, where the old and new paths diverge, and the observable result. When a hunk works around app behavior (a sleep, a forced write, a retry), the bullet says what the app does without the workaround. Production code inside a test or tooling change earns a bullet. So does a behavior change no source mentions, and a hunk that resolves a review comment in a way the hunk alone does not make obvious: the bullet says what the fix does. Empty is correct for a rename, a formatting change, or a wording-only docs group; leave it empty rather than fill it.\n \n-Document `lookFor` holds claims about the whole change, and work a source names that no hunk does. Compare each source with the diff. When a ticket, request, comment, or transcript names work, the bullet says what it asks for and what the diff does about it. Cite the source with `[text](source:s1)`; the id goes in `sources`, and `validate` fails on an unknown id. Omit the field when no source names expected work, or when the bullet would only restate document `summary`.\n+Document `lookFor` holds claims about the whole change, and the divergences between the sources and the code at head. Source items are each requirement and acceptance item in a ticket, each behavior claim in the pull request description and commit messages, and each request in a review comment. Test plans, manual QA notes, and out-of-scope lists are context, not items. Read the code at head for each item and give it a verdict: done as asked, done differently, not done, or not true at head. Each verdict other than done-as-asked is one bullet: what the source asks or says, what head does, and the reason when a source or the code gives one, or that no source gives one. When a later source changes what an earlier one asked (a review comment overrides the ticket), the ticket item is done differently, and the bullet names both sources. A manual step a source says the change needs (turn on a setting, run a migration) is also a bullet. Cite with `[text](source:s1)`; the id goes in `sources`, and `validate` fails on an unknown id. Omit the field when nothing needs a bullet.\n \n ## Write the prose\n \n","complete":true,"hunks":[{"path":".agents/skills/comprehende/SKILL.md","oldStart":43,"oldLines":8,"newStart":43,"newLines":8,"header":"@@ -43,8 +43,8 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":43,"newNumber":43,"text":"   ```"},{"kind":"ctx","oldNumber":44,"newNumber":44,"text":""},{"kind":"ctx","oldNumber":45,"newNumber":45,"text":"   Sources are listed under The why and The title. Done when the diff and those sources are in context."},{"kind":"del","oldNumber":46,"newNumber":null,"text":"4. Read the skeleton once, for its paths. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"del","oldNumber":47,"newNumber":null,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Write the whole `review.json` in one write from the field shape below. Keep `version` and `source` from the skeleton. The skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"add","oldNumber":null,"newNumber":46,"text":"4. Read the skeleton once, for its paths. Recover the why, write the title, write the what, and check each source item against the code at head (see lookFor). Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when an item diverges; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every source item has a verdict from the code at head and every divergence is a document `lookFor` bullet."},{"kind":"add","oldNumber":null,"newNumber":47,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Write the whole `review.json` in one write from the field shape below. Keep `version` and `source` from the skeleton. The skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry. Also done when each test file sits in the group of the code it checks (unless tests are the concern), each one-line call site or declaration sits with the code it serves, work no source asks for has its own `part`, and each `dependsOn` id names a group in the same `part`. When a story builds on another part, `suggestedOrder` puts that part first."},{"kind":"ctx","oldNumber":48,"newNumber":48,"text":""},{"kind":"ctx","oldNumber":49,"newNumber":49,"text":"   Field shape. The words are placeholders. This shape is enough to write a valid document."},{"kind":"ctx","oldNumber":50,"newNumber":50,"text":""}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":94,"oldLines":7,"newStart":94,"newLines":7,"header":"@@ -94,7 +94,7 @@ Invent a title when those sources are missing, vague, or name something else. A","language":"markdown","lines":[{"kind":"ctx","oldNumber":94,"newNumber":94,"text":""},{"kind":"ctx","oldNumber":95,"newNumber":95,"text":"## The why"},{"kind":"ctx","oldNumber":96,"newNumber":96,"text":""},{"kind":"del","oldNumber":97,"newNumber":null,"text":"Document `why` names why the work exists, in one or two sentences, inferred only from the sources below. If those sources are silent or mixed, omit it. Do not derive document `why` from the diff, from group structure, or from a motive you infer from the patch. A request naming two unrelated product stories with no unifying source also gets no document `why`; each story keeps its own group `why`s."},{"kind":"add","oldNumber":null,"newNumber":97,"text":"Document `why` names why the work exists, in one or two sentences, inferred only from the sources below. It is the problem or goal a source states. Before writing it, find the source sentence that states that problem or goal: \"Support SSO login\", \"so admins can export audit logs\", \"retries flood the API\". A sentence that only says what the change does (\"the settings page becomes a modal\", \"uploads move to S3\") belongs in the summary. When no source sentence states a problem or goal, or the sources are mixed, omit the why. Write it in the source's own terms, with no cause, benefit, or reader need the source does not name. The same holds for every group `why`: a citation carries only what that source says, and a source is the reason for a choice only when it states that reason. Do not derive document `why` from the diff, from group structure, or from a motive you infer from the patch. A request naming two unrelated product stories with no unifying source also gets no document `why`; each story keeps its own group `why`s."},{"kind":"ctx","oldNumber":98,"newNumber":98,"text":""},{"kind":"ctx","oldNumber":99,"newNumber":99,"text":"Sources are read, never copied into the review document, except PR review comments: copy `author` and `body` so the UI can show the quote. Keep ticket bodies, request descriptions, commit text, and transcript text out of `review.json`. Cite a source in prose with `[text](source:s1)` when that sentence leans on it."},{"kind":"ctx","oldNumber":100,"newNumber":100,"text":""}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":119,"oldLines":10,"newStart":119,"newLines":11,"header":"@@ -119,10 +119,11 @@ Group `summary` is one sentence saying what the group is, describing how its hun","language":"markdown","lines":[{"kind":"ctx","oldNumber":119,"newNumber":119,"text":"## Grouping rules"},{"kind":"ctx","oldNumber":120,"newNumber":120,"text":""},{"kind":"ctx","oldNumber":121,"newNumber":121,"text":"- Group by review concern: why these hunks are read together. A directory qualifies only when that directory is the concern. Grouping needs the diffs; the index and the log alone are not enough."},{"kind":"del","oldNumber":122,"newNumber":null,"text":"- A hunk may appear in several groups when it matters in more than one story."},{"kind":"del","oldNumber":123,"newNumber":null,"text":"- `dependsOn` marks a real dependency inside one story: the reader needs the earlier group to understand this one. Reading order inside a story: contracts and foundations, then call sites, then tests, then mechanical work. Independent work stands alone with its own `part`, empty `dependsOn`, and nothing depending on it; a second feature or standalone documentation that could have been its own PR is independent work. When unsure whether two concerns depend on each other, keep them separate under different parts: a false split is easy to see, a false chain hides a mixed PR."},{"kind":"add","oldNumber":null,"newNumber":122,"text":"- A group is one concern a reviewer names in a phrase. The code, the tests that check it, and the one-line call sites, declarations, and re-exports that serve it read together in that group. Tests are their own group only when they are the concern, or when one test covers several groups. A new feature's code, fixtures, helpers, and tests are one group; split a concern only where a reader would stop and review the first piece alone. A small change has one to three groups."},{"kind":"add","oldNumber":null,"newNumber":123,"text":"- A hunk may appear in several groups when it matters in more than one story. When one hunk holds two concerns, the group that holds it names both."},{"kind":"add","oldNumber":null,"newNumber":124,"text":"- `dependsOn` marks a real dependency inside one story: the reader needs the earlier group to understand this one. It stays inside one part. Reading order inside a story: contracts and foundations, then call sites, then mechanical work. Independent work stands alone with its own `part`, empty `dependsOn`, and nothing depending on it; a second feature, work no source asks for (extra product events, a dependency refresh), or standalone documentation that could have been its own PR is independent work. When unsure whether two concerns depend on each other, keep them separate under different parts: a false split is easy to see, a false chain hides a mixed PR."},{"kind":"ctx","oldNumber":124,"newNumber":125,"text":"- `part` is a short name (a few words) for one independent story, the same on every group in that story; the UI colors shared parts together. One document `parts[]` entry per name."},{"kind":"del","oldNumber":125,"newNumber":null,"text":"- Mechanical work (import reordering, identifier-only renames, generated code, formatting, type re-exports) is its own group that keeps every hunk as refs, risk visible rather than folded into a file list. When it exists only because of a story, it is that part's last group with nothing depending on it. When it could have been its own PR, it is its own part."},{"kind":"add","oldNumber":null,"newNumber":126,"text":"- Mechanical work (import reordering, identifier-only renames, generated code, formatting, pure moves) is its own group that keeps every hunk as refs, risk visible rather than folded into a file list. Its summary tells the reader what they can skip and why that is safe: a pure move, regenerated output that means the same as before. An identical copy of a file in the diff sits with its source, and the summary says it is identical. When it exists only because of a story, it is that part's last group with nothing depending on it. When it could have been its own PR, it is its own part."},{"kind":"ctx","oldNumber":126,"newNumber":127,"text":"- `suggestedOrder` walks the whole review, independent parts included. Mechanical parts, independent documentation, and test-only cleanup go last."},{"kind":"ctx","oldNumber":127,"newNumber":128,"text":"- Document `size` is human review burden: `trivial`, `small`, `medium`, `large`, `very-large`. Forty files changing one import in one group are `small`; three files rewriting a contract the rest of the system hangs on can be `large`."},{"kind":"ctx","oldNumber":128,"newNumber":129,"text":"- Coverage: every live hunk sits in at least one group. A path covers every live hunk of that file. Duplicate refs across groups are allowed. Unreferenced hunks fail `validate` and show as Unassigned in the UI."}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":133,"oldLines":11,"newStart":134,"newLines":11,"header":"@@ -133,11 +134,11 @@ A `hunkRefs` entry is a path string when the group holds every live hunk of that","language":"markdown","lines":[{"kind":"ctx","oldNumber":133,"newNumber":134,"text":""},{"kind":"ctx","oldNumber":134,"newNumber":135,"text":"## lookFor"},{"kind":"ctx","oldNumber":135,"newNumber":136,"text":""},{"kind":"del","oldNumber":136,"newNumber":null,"text":"`lookFor` is for what the live diff does not make obvious. Each bullet is one inspectable claim: optionally a short risk tag (Subtle, Breaking, Race, Perf), then one sentence. The bullet states the claim; the human checks it against the live diff and keeps the verdict. Pseudocode, diagrams, collapsed diffs, commit lists, hunk counts, and pass/fail marks stay out."},{"kind":"add","oldNumber":null,"newNumber":137,"text":"`lookFor` is for what a careful engineer would miss on a first read of the live diff. Each bullet is one finding, stated as fact: optionally a short risk tag (Subtle, Breaking, Race, Perf), then one sentence on what the code at head does and what the reader would observe. The human checks it against the live diff and keeps the verdict. Pseudocode, diagrams, collapsed diffs, commit lists, hunk counts, and pass/fail marks stay out."},{"kind":"ctx","oldNumber":137,"newNumber":138,"text":""},{"kind":"del","oldNumber":138,"newNumber":null,"text":"Group `lookFor` holds claims that live in those hunks. When a behavior change hides in the hunk, a predicted trace earns a bullet: a small realistic input, where the old and new paths diverge, and the observable result. Empty is correct for a rename, a formatting change, or a wording-only docs group; leave it empty rather than fill it."},{"kind":"add","oldNumber":null,"newNumber":139,"text":"Group `lookFor` holds claims that live in those hunks. When a behavior change hides in the hunk, a predicted trace earns a bullet: a small realistic input, where the old and new paths diverge, and the observable result. When a hunk works around app behavior (a sleep, a forced write, a retry), the bullet says what the app does without the workaround. Production code inside a test or tooling change earns a bullet. So does a behavior change no source mentions, and a hunk that resolves a review comment in a way the hunk alone does not make obvious: the bullet says what the fix does. Empty is correct for a rename, a formatting change, or a wording-only docs group; leave it empty rather than fill it."},{"kind":"ctx","oldNumber":139,"newNumber":140,"text":""},{"kind":"del","oldNumber":140,"newNumber":null,"text":"Document `lookFor` holds claims about the whole change, and work a source names that no hunk does. Compare each source with the diff. When a ticket, request, comment, or transcript names work, the bullet says what it asks for and what the diff does about it. Cite the source with `[text](source:s1)`; the id goes in `sources`, and `validate` fails on an unknown id. Omit the field when no source names expected work, or when the bullet would only restate document `summary`."},{"kind":"add","oldNumber":null,"newNumber":141,"text":"Document `lookFor` holds claims about the whole change, and the divergences between the sources and the code at head. Source items are each requirement and acceptance item in a ticket, each behavior claim in the pull request description and commit messages, and each request in a review comment. Test plans, manual QA notes, and out-of-scope lists are context, not items. Read the code at head for each item and give it a verdict: done as asked, done differently, not done, or not true at head. Each verdict other than done-as-asked is one bullet: what the source asks or says, what head does, and the reason when a source or the code gives one, or that no source gives one. When a later source changes what an earlier one asked (a review comment overrides the ticket), the ticket item is done differently, and the bullet names both sources. A manual step a source says the change needs (turn on a setting, run a migration) is also a bullet. Cite with `[text](source:s1)`; the id goes in `sources`, and `validate` fails on an unknown id. Omit the field when nothing needs a bullet."},{"kind":"ctx","oldNumber":141,"newNumber":142,"text":""},{"kind":"ctx","oldNumber":142,"newNumber":143,"text":"## Write the prose"},{"kind":"ctx","oldNumber":143,"newNumber":144,"text":""}]}]},{"path":".agents/skills/comprehende/references/example.md","kind":"text","status":"modified","patch":"diff --git a/.agents/skills/comprehende/references/example.md b/.agents/skills/comprehende/references/example.md\nindex b4e0059..90a8f38 100644\n--- a/.agents/skills/comprehende/references/example.md\n+++ b/.agents/skills/comprehende/references/example.md\n@@ -2,12 +2,12 @@\n \n Optional filled sample. The field shape in `SKILL.md` is enough to write a document. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header.\n \n-- `login` depends on `cookie`. Both use `part` \"Session cookie\".\n+- `login` depends on `cookie`. Both use `part` \"Session cookie\". The helper's test sits in `cookie` with the helper it checks.\n - `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request.\n - Ticket #12 names why this work exists, so document `why` is present. Document `title` keeps the ticket title. Document `summary` names both stories. Document `parts` holds a one-sentence what per story. The why cites the ticket with `[#12](source:s1)`.\n - `login` `summary` names how those hunks meet. `login` `lookFor` is a predicted trace. `docs` has no `lookFor`.\n - Document `lookFor` compares ticket #12 with the diff: work the ticket asks for that no hunk does, and one claim about the whole change. Both cite `[#12](source:s1)`. Group `lookFor` stays inside its hunks.\n-- Each group holds every hunk of its file, so each `hunkRefs` entry is that path. A split file uses `path@oldStart+newStart` (`old/path -> new/path@oldStart+newStart` when renamed).\n+- Each group holds every hunk of its files, so each `hunkRefs` entry is a path. A split file uses `path@oldStart+newStart` (`old/path -> new/path@oldStart+newStart` when renamed).\n \n ```json\n {\n@@ -50,13 +50,13 @@ Optional filled sample. The field shape in `SKILL.md` is enough to write a docum\n       \"id\": \"cookie\",\n       \"title\": \"Session cookie helper\",\n       \"why\": \"The login route needs one helper to apply the session cookie options.\",\n-      \"summary\": \"`setSessionCookie` applies the required options to session cookies.\",\n+      \"summary\": \"`setSessionCookie` applies the required options to session cookies, and its test checks them.\",\n       \"part\": \"Session cookie\",\n       \"lookFor\": [\n         \"Breaking. `setSessionCookie` throws when the caller passes `httpOnly: false`.\"\n       ],\n       \"suggestedOrder\": 0,\n-      \"hunkRefs\": [\"src/auth/session.ts\"]\n+      \"hunkRefs\": [\"src/auth/session.ts\", \"src/auth/session.test.ts\"]\n     },\n     {\n       \"id\": \"login\",\n","complete":true,"hunks":[{"path":".agents/skills/comprehende/references/example.md","oldStart":2,"oldLines":12,"newStart":2,"newLines":12,"header":"@@ -2,12 +2,12 @@","language":"markdown","lines":[{"kind":"ctx","oldNumber":2,"newNumber":2,"text":""},{"kind":"ctx","oldNumber":3,"newNumber":3,"text":"Optional filled sample. The field shape in `SKILL.md` is enough to write a document. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header."},{"kind":"ctx","oldNumber":4,"newNumber":4,"text":""},{"kind":"del","oldNumber":5,"newNumber":null,"text":"- `login` depends on `cookie`. Both use `part` \"Session cookie\"."},{"kind":"add","oldNumber":null,"newNumber":5,"text":"- `login` depends on `cookie`. Both use `part` \"Session cookie\". The helper's test sits in `cookie` with the helper it checks."},{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"- `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request."},{"kind":"ctx","oldNumber":7,"newNumber":7,"text":"- Ticket #12 names why this work exists, so document `why` is present. Document `title` keeps the ticket title. Document `summary` names both stories. Document `parts` holds a one-sentence what per story. The why cites the ticket with `[#12](source:s1)`."},{"kind":"ctx","oldNumber":8,"newNumber":8,"text":"- `login` `summary` names how those hunks meet. `login` `lookFor` is a predicted trace. `docs` has no `lookFor`."},{"kind":"ctx","oldNumber":9,"newNumber":9,"text":"- Document `lookFor` compares ticket #12 with the diff: work the ticket asks for that no hunk does, and one claim about the whole change. Both cite `[#12](source:s1)`. Group `lookFor` stays inside its hunks."},{"kind":"del","oldNumber":10,"newNumber":null,"text":"- Each group holds every hunk of its file, so each `hunkRefs` entry is that path. A split file uses `path@oldStart+newStart` (`old/path -> new/path@oldStart+newStart` when renamed)."},{"kind":"add","oldNumber":null,"newNumber":10,"text":"- Each group holds every hunk of its files, so each `hunkRefs` entry is a path. A split file uses `path@oldStart+newStart` (`old/path -> new/path@oldStart+newStart` when renamed)."},{"kind":"ctx","oldNumber":11,"newNumber":11,"text":""},{"kind":"ctx","oldNumber":12,"newNumber":12,"text":"```json"},{"kind":"ctx","oldNumber":13,"newNumber":13,"text":"{"}]},{"path":".agents/skills/comprehende/references/example.md","oldStart":50,"oldLines":13,"newStart":50,"newLines":13,"header":"@@ -50,13 +50,13 @@ Optional filled sample. The field shape in `SKILL.md` is enough to write a docum","language":"markdown","lines":[{"kind":"ctx","oldNumber":50,"newNumber":50,"text":"      \"id\": \"cookie\","},{"kind":"ctx","oldNumber":51,"newNumber":51,"text":"      \"title\": \"Session cookie helper\","},{"kind":"ctx","oldNumber":52,"newNumber":52,"text":"      \"why\": \"The login route needs one helper to apply the session cookie options.\","},{"kind":"del","oldNumber":53,"newNumber":null,"text":"      \"summary\": \"`setSessionCookie` applies the required options to session cookies.\","},{"kind":"add","oldNumber":null,"newNumber":53,"text":"      \"summary\": \"`setSessionCookie` applies the required options to session cookies, and its test checks them.\","},{"kind":"ctx","oldNumber":54,"newNumber":54,"text":"      \"part\": \"Session cookie\","},{"kind":"ctx","oldNumber":55,"newNumber":55,"text":"      \"lookFor\": ["},{"kind":"ctx","oldNumber":56,"newNumber":56,"text":"        \"Breaking. `setSessionCookie` throws when the caller passes `httpOnly: false`.\""},{"kind":"ctx","oldNumber":57,"newNumber":57,"text":"      ],"},{"kind":"ctx","oldNumber":58,"newNumber":58,"text":"      \"suggestedOrder\": 0,"},{"kind":"del","oldNumber":59,"newNumber":null,"text":"      \"hunkRefs\": [\"src/auth/session.ts\"]"},{"kind":"add","oldNumber":null,"newNumber":59,"text":"      \"hunkRefs\": [\"src/auth/session.ts\", \"src/auth/session.test.ts\"]"},{"kind":"ctx","oldNumber":60,"newNumber":60,"text":"    },"},{"kind":"ctx","oldNumber":61,"newNumber":61,"text":"    {"},{"kind":"ctx","oldNumber":62,"newNumber":62,"text":"      \"id\": \"login\","}]}]}]}
