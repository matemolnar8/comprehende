{"path":".github/workflows/eval.yml","ref":"d05d71698063919c5928f161ed03e5c749f26594","side":"new","content":"# Full graded eval after skill, schema, review, or harness land on main.\n# Needs repo secret CURSOR_API_KEY. Skips (does not fail) when that secret is unset.\n# Event-driven: path filter on main, plus workflow_dispatch. Not a cron.\n\nname: Eval suite\n\non:\n  push:\n    branches: [main]\n    paths:\n      - \"skills-next/comprehende/**\"\n      - \"src/schema/**\"\n      - \"src/review/**\"\n      - \"scripts/eval/**\"\n      - \"eval/cases/**\"\n      - \".github/workflows/eval.yml\"\n  workflow_dispatch:\n\nconcurrency:\n  group: eval-suite\n  cancel-in-progress: true\n\npermissions:\n  contents: read\n\njobs:\n  eval:\n    runs-on: ubuntu-latest\n    timeout-minutes: 120\n    steps:\n      - uses: actions/checkout@v5\n      - uses: pnpm/setup@v2\n        with:\n          runtime: node@24\n          cache: true\n          install: false\n      - run: pnpm install --frozen-lockfile\n      - name: Full graded eval\n        env:\n          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}\n        run: |\n          set -euo pipefail\n          if [ -z \"${CURSOR_API_KEY}\" ]; then\n            echo \"Repo secret CURSOR_API_KEY is not set. Skipping the full graded suite.\"\n            echo \"Add it under Settings → Secrets and variables → Actions.\"\n            echo \"::warning::CURSOR_API_KEY secret missing; full eval skipped\"\n            exit 0\n          fi\n          pnpm eval -- --json\n      - name: Upload eval run\n        if: always()\n        uses: actions/upload-artifact@v4\n        with:\n          name: eval-run\n          path: eval/runs\n          if-no-files-found: ignore\n","language":"yaml"}
