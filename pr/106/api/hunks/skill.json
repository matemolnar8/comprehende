{"hunks":[{"path":"skills-next/comprehende/SKILL.md","oldStart":31,"oldLines":7,"newStart":31,"newLines":7,"header":"@@ -31,7 +31,7 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":31,"newNumber":31,"text":"   git diff --stat <base>...<head>"},{"kind":"ctx","oldNumber":32,"newNumber":32,"text":"   ```"},{"kind":"ctx","oldNumber":33,"newNumber":33,"text":""},{"kind":"del","oldNumber":34,"newNumber":null,"text":"   If `npm view` reports a version newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes every hunk ref into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those hunk refs; do not invent replacements."},{"kind":"add","oldNumber":null,"newNumber":34,"text":"   If `npm view` reports a version newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"ctx","oldNumber":35,"newNumber":35,"text":"3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec."},{"kind":"ctx","oldNumber":36,"newNumber":36,"text":""},{"kind":"ctx","oldNumber":37,"newNumber":37,"text":"   ```sh"}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":39,"oldLines":8,"newStart":39,"newLines":8,"header":"@@ -39,8 +39,8 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"   ```"},{"kind":"ctx","oldNumber":40,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":41,"text":"   Sources are listed under The why and The title. Done when the diff and those sources are in context."},{"kind":"del","oldNumber":42,"newNumber":null,"text":"4. Read the skeleton once, for its hunk refs. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"del","oldNumber":43,"newNumber":null,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy hunk objects from the skeleton; do not invent refs. Set document `size` from review burden, not `git diff --stat`. Shape and field names: [references/example.md](./references/example.md) shows every field. Write the whole `review.json` in one write; the skeleton's `ungrouped` group is replaced, not kept. Done when every hunk ref from the skeleton appears in at least one group, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"add","oldNumber":null,"newNumber":42,"text":"4. Read the skeleton once, for its paths. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"add","oldNumber":null,"newNumber":43,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Shape and field names: [references/example.md](./references/example.md) shows every field. Write the whole `review.json` in one write; the skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"ctx","oldNumber":44,"newNumber":44,"text":"6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"ctx","oldNumber":45,"newNumber":45,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":46,"newNumber":46,"text":""}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":95,"oldLines":11,"newStart":95,"newLines":11,"header":"@@ -95,11 +95,11 @@ Group `summary` is one sentence saying what the group is, describing how its hun","language":"markdown","lines":[{"kind":"ctx","oldNumber":95,"newNumber":95,"text":"- Mechanical work (import reordering, identifier-only renames, generated code, formatting, type re-exports) is its own group that keeps every hunk as refs, risk visible rather than folded into a file list. When it exists only because of a story, it is that part's last group with nothing depending on it. When it could have been its own PR, it is its own part."},{"kind":"ctx","oldNumber":96,"newNumber":96,"text":"- `suggestedOrder` walks the whole review, independent parts included. Mechanical parts, independent documentation, and test-only cleanup go last."},{"kind":"ctx","oldNumber":97,"newNumber":97,"text":"- Document `size` is human review burden: `trivial`, `small`, `medium`, `large`, `very-large`. Forty files changing one import in one group are `small`; three files rewriting a contract the rest of the system hangs on can be `large`."},{"kind":"del","oldNumber":98,"newNumber":null,"text":"- Coverage: every hunk from the skeleton sits in at least one group; duplicate refs across groups are allowed. Unreferenced hunks fail `validate` and show as Unassigned in the UI."},{"kind":"add","oldNumber":null,"newNumber":98,"text":"- Coverage: every live hunk sits in at least one group. A path covers every live hunk of that file. Duplicate refs across groups are allowed. Unreferenced hunks fail `validate` and show as Unassigned in the UI."},{"kind":"ctx","oldNumber":99,"newNumber":99,"text":"- Lockfiles have no hunk refs; the CLI leaves them out of the skeleton and the UI gives them their own closed bucket. Hunk refs exist only for hunks the skeleton lists."},{"kind":"del","oldNumber":100,"newNumber":null,"text":"- Stale refs (rebase of the pinned commits) fail `validate`; `serve` still starts, shows git at those SHAs, and flags the broken pointer. Re-run `review` and copy fresh refs. A dirty work tree does not make refs stale."},{"kind":"add","oldNumber":null,"newNumber":100,"text":"- A path stays valid when a rebase only shifts line numbers. `path@oldStart+newStart` goes stale when those starts move, and that fails `validate`. `serve` still starts, shows git at those SHAs, and flags the broken pointer. Re-run `review` and copy fresh paths. A dirty work tree does not make refs stale."},{"kind":"ctx","oldNumber":101,"newNumber":101,"text":""},{"kind":"del","oldNumber":102,"newNumber":null,"text":"Hunk identity is `(path, oldStart, newStart)` plus `oldPath` when renamed. Copy `oldStart`, `oldLines`, `newStart`, and `newLines` from the skeleton. Image files are hunks; copy their refs into groups. Git LFS images are read from `.git/lfs/objects` in the clone; a missing object leaves the image slot empty."},{"kind":"add","oldNumber":null,"newNumber":102,"text":"A `hunkRefs` entry is a path string when the group holds every live hunk of that file. Copy the path from the skeleton. When a file is split across groups, name each hunk as `path@oldStart+newStart`, or `old/path -> new/path@oldStart+newStart` when that hunk is a rename. Take `oldStart` and `newStart` from the hunk's `@@ -oldStart,oldLines +newStart,newLines @@` header. The object form with `path`, `oldPath`, `oldStart`, `oldLines`, `newStart`, and `newLines` still validates. Identity is `(path, oldStart, newStart)` plus `oldPath` when renamed. Image files are paths. Git LFS images are read from `.git/lfs/objects` in the clone; a missing object leaves the image slot empty."},{"kind":"ctx","oldNumber":103,"newNumber":103,"text":""},{"kind":"ctx","oldNumber":104,"newNumber":104,"text":"## lookFor"},{"kind":"ctx","oldNumber":105,"newNumber":105,"text":""}]},{"path":"skills-next/comprehende/references/example.md","oldStart":1,"oldLines":12,"newStart":1,"newLines":13,"header":"@@ -1,12 +1,13 @@","language":"markdown","lines":[{"kind":"ctx","oldNumber":1,"newNumber":1,"text":"# Example review.json"},{"kind":"ctx","oldNumber":2,"newNumber":2,"text":""},{"kind":"del","oldNumber":3,"newNumber":null,"text":"Pointers and prose only. The `@@` numbers must come from `comprehende review`, not from reading the patch."},{"kind":"add","oldNumber":null,"newNumber":3,"text":"Pointers and prose only. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header."},{"kind":"ctx","oldNumber":4,"newNumber":4,"text":""},{"kind":"ctx","oldNumber":5,"newNumber":5,"text":"- `login` depends on `cookie`. Both use `part` \"Session cookie\"."},{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"- `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request."},{"kind":"ctx","oldNumber":7,"newNumber":7,"text":"- Ticket #12 names why this work exists, so document `why` is present. Document `title` keeps the ticket title. Document `summary` names both stories. Document `parts` holds a one-sentence what per story. The why cites the ticket with `[#12](source:s1)`."},{"kind":"ctx","oldNumber":8,"newNumber":8,"text":"- `login` `summary` names how those hunks meet. `login` `lookFor` is a predicted trace. `docs` has no `lookFor`."},{"kind":"ctx","oldNumber":9,"newNumber":9,"text":"- Document `lookFor` compares ticket #12 with the diff: work the ticket asks for that no hunk does, and one claim about the whole change. Both cite `[#12](source:s1)`. Group `lookFor` stays inside its hunks."},{"kind":"add","oldNumber":null,"newNumber":10,"text":"- Each group holds every hunk of its file, so each `hunkRefs` entry is that path. A split file uses `path@oldStart+newStart` (`old/path -> new/path@oldStart+newStart` when renamed). The object form still validates."},{"kind":"ctx","oldNumber":10,"newNumber":11,"text":""},{"kind":"ctx","oldNumber":11,"newNumber":12,"text":"```json"},{"kind":"ctx","oldNumber":12,"newNumber":13,"text":"{"}]},{"path":"skills-next/comprehende/references/example.md","oldStart":55,"oldLines":15,"newStart":56,"newLines":7,"header":"@@ -55,15 +56,7 @@ Pointers and prose only. The `@@` numbers must come from `comprehende review`, n","language":"markdown","lines":[{"kind":"ctx","oldNumber":55,"newNumber":56,"text":"        \"Breaking. `setSessionCookie` throws when the caller passes `httpOnly: false`.\""},{"kind":"ctx","oldNumber":56,"newNumber":57,"text":"      ],"},{"kind":"ctx","oldNumber":57,"newNumber":58,"text":"      \"suggestedOrder\": 0,"},{"kind":"del","oldNumber":58,"newNumber":null,"text":"      \"hunkRefs\": ["},{"kind":"del","oldNumber":59,"newNumber":null,"text":"        {"},{"kind":"del","oldNumber":60,"newNumber":null,"text":"          \"path\": \"src/auth/session.ts\","},{"kind":"del","oldNumber":61,"newNumber":null,"text":"          \"oldStart\": 1,"},{"kind":"del","oldNumber":62,"newNumber":null,"text":"          \"oldLines\": 20,"},{"kind":"del","oldNumber":63,"newNumber":null,"text":"          \"newStart\": 1,"},{"kind":"del","oldNumber":64,"newNumber":null,"text":"          \"newLines\": 40"},{"kind":"del","oldNumber":65,"newNumber":null,"text":"        }"},{"kind":"del","oldNumber":66,"newNumber":null,"text":"      ]"},{"kind":"add","oldNumber":null,"newNumber":59,"text":"      \"hunkRefs\": [\"src/auth/session.ts\"]"},{"kind":"ctx","oldNumber":67,"newNumber":60,"text":"    },"},{"kind":"ctx","oldNumber":68,"newNumber":61,"text":"    {"},{"kind":"ctx","oldNumber":69,"newNumber":62,"text":"      \"id\": \"login\","}]},{"path":"skills-next/comprehende/references/example.md","oldStart":77,"oldLines":15,"newStart":70,"newLines":7,"header":"@@ -77,15 +70,7 @@ Pointers and prose only. The `@@` numbers must come from `comprehende review`, n","language":"markdown","lines":[{"kind":"ctx","oldNumber":77,"newNumber":70,"text":"      ],"},{"kind":"ctx","oldNumber":78,"newNumber":71,"text":"      \"dependsOn\": [\"cookie\"],"},{"kind":"ctx","oldNumber":79,"newNumber":72,"text":"      \"suggestedOrder\": 1,"},{"kind":"del","oldNumber":80,"newNumber":null,"text":"      \"hunkRefs\": ["},{"kind":"del","oldNumber":81,"newNumber":null,"text":"        {"},{"kind":"del","oldNumber":82,"newNumber":null,"text":"          \"path\": \"src/api/login.ts\","},{"kind":"del","oldNumber":83,"newNumber":null,"text":"          \"oldStart\": 10,"},{"kind":"del","oldNumber":84,"newNumber":null,"text":"          \"oldLines\": 8,"},{"kind":"del","oldNumber":85,"newNumber":null,"text":"          \"newStart\": 10,"},{"kind":"del","oldNumber":86,"newNumber":null,"text":"          \"newLines\": 24"},{"kind":"del","oldNumber":87,"newNumber":null,"text":"        }"},{"kind":"del","oldNumber":88,"newNumber":null,"text":"      ]"},{"kind":"add","oldNumber":null,"newNumber":73,"text":"      \"hunkRefs\": [\"src/api/login.ts\"]"},{"kind":"ctx","oldNumber":89,"newNumber":74,"text":"    },"},{"kind":"ctx","oldNumber":90,"newNumber":75,"text":"    {"},{"kind":"ctx","oldNumber":91,"newNumber":76,"text":"      \"id\": \"docs\","}]},{"path":"skills-next/comprehende/references/example.md","oldStart":94,"oldLines":15,"newStart":79,"newLines":7,"header":"@@ -94,15 +79,7 @@ Pointers and prose only. The `@@` numbers must come from `comprehende review`, n","language":"markdown","lines":[{"kind":"ctx","oldNumber":94,"newNumber":79,"text":"      \"summary\": \"The README section on sessions matches the new cookie behavior.\","},{"kind":"ctx","oldNumber":95,"newNumber":80,"text":"      \"part\": \"README\","},{"kind":"ctx","oldNumber":96,"newNumber":81,"text":"      \"suggestedOrder\": 2,"},{"kind":"del","oldNumber":97,"newNumber":null,"text":"      \"hunkRefs\": ["},{"kind":"del","oldNumber":98,"newNumber":null,"text":"        {"},{"kind":"del","oldNumber":99,"newNumber":null,"text":"          \"path\": \"README.md\","},{"kind":"del","oldNumber":100,"newNumber":null,"text":"          \"oldStart\": 1,"},{"kind":"del","oldNumber":101,"newNumber":null,"text":"          \"oldLines\": 4,"},{"kind":"del","oldNumber":102,"newNumber":null,"text":"          \"newStart\": 1,"},{"kind":"del","oldNumber":103,"newNumber":null,"text":"          \"newLines\": 8"},{"kind":"del","oldNumber":104,"newNumber":null,"text":"        }"},{"kind":"del","oldNumber":105,"newNumber":null,"text":"      ]"},{"kind":"add","oldNumber":null,"newNumber":82,"text":"      \"hunkRefs\": [\"README.md\"]"},{"kind":"ctx","oldNumber":106,"newNumber":83,"text":"    }"},{"kind":"ctx","oldNumber":107,"newNumber":84,"text":"  ]"},{"kind":"ctx","oldNumber":108,"newNumber":85,"text":"}"}]},{"path":"scripts/eval/skill.test.ts","oldStart":37,"oldLines":6,"newStart":37,"newLines":8,"header":"@@ -37,6 +37,8 @@ describe(\"next skill workflow\", () => {","language":"typescript","lines":[{"kind":"ctx","oldNumber":37,"newNumber":37,"text":"    assert.doesNotMatch(md, /poetry\\.lock/);"},{"kind":"ctx","oldNumber":38,"newNumber":38,"text":"    assert.doesNotMatch(md, /go\\.sum/);"},{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"    assert.match(grouping, /Lockfiles have no hunk refs/);"},{"kind":"add","oldNumber":null,"newNumber":40,"text":"    assert.match(grouping, /path@oldStart\\+newStart/);"},{"kind":"add","oldNumber":null,"newNumber":41,"text":"    assert.match(grouping, /A path covers every live hunk of that file/);"},{"kind":"ctx","oldNumber":40,"newNumber":42,"text":"    assert.doesNotMatch(grouping, /Lockfiles stay in `skipped`/);"},{"kind":"ctx","oldNumber":41,"newNumber":43,"text":"    const example = await readFile(join(findPackageRoot(), \"skills-next/comprehende/references/example.md\"), \"utf8\");"},{"kind":"ctx","oldNumber":42,"newNumber":44,"text":"    assert.doesNotMatch(example, /review\\.schema\\.json/);"}]}],"files":[{"path":"skills-next/comprehende/SKILL.md","kind":"text","status":"modified","patch":"diff --git a/skills-next/comprehende/SKILL.md b/skills-next/comprehende/SKILL.md\nindex a11c141..0ccc9cd 100644\n--- a/skills-next/comprehende/SKILL.md\n+++ b/skills-next/comprehende/SKILL.md\n@@ -31,7 +31,7 @@ Each step is one tool call where the step says so. Batch the commands as written\n    git diff --stat <base>...<head>\n    ```\n \n-   If `npm view` reports a version newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes every hunk ref into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those hunk refs; do not invent replacements.\n+   If `npm view` reports a version newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file.\n 3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec.\n \n    ```sh\n@@ -39,8 +39,8 @@ Each step is one tool call where the step says so. Batch the commands as written\n    ```\n \n    Sources are listed under The why and The title. Done when the diff and those sources are in context.\n-4. Read the skeleton once, for its hunk refs. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet.\n-5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy hunk objects from the skeleton; do not invent refs. Set document `size` from review burden, not `git diff --stat`. Shape and field names: [references/example.md](./references/example.md) shows every field. Write the whole `review.json` in one write; the skeleton's `ungrouped` group is replaced, not kept. Done when every hunk ref from the skeleton appears in at least one group, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry.\n+4. Read the skeleton once, for its paths. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet.\n+5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Shape and field names: [references/example.md](./references/example.md) shows every field. Write the whole `review.json` in one write; the skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry.\n 6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0.\n 7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n \n@@ -95,11 +95,11 @@ Group `summary` is one sentence saying what the group is, describing how its hun\n - Mechanical work (import reordering, identifier-only renames, generated code, formatting, type re-exports) is its own group that keeps every hunk as refs, risk visible rather than folded into a file list. When it exists only because of a story, it is that part's last group with nothing depending on it. When it could have been its own PR, it is its own part.\n - `suggestedOrder` walks the whole review, independent parts included. Mechanical parts, independent documentation, and test-only cleanup go last.\n - Document `size` is human review burden: `trivial`, `small`, `medium`, `large`, `very-large`. Forty files changing one import in one group are `small`; three files rewriting a contract the rest of the system hangs on can be `large`.\n-- Coverage: every hunk from the skeleton sits in at least one group; duplicate refs across groups are allowed. Unreferenced hunks fail `validate` and show as Unassigned in the UI.\n+- Coverage: every live hunk sits in at least one group. A path covers every live hunk of that file. Duplicate refs across groups are allowed. Unreferenced hunks fail `validate` and show as Unassigned in the UI.\n - Lockfiles have no hunk refs; the CLI leaves them out of the skeleton and the UI gives them their own closed bucket. Hunk refs exist only for hunks the skeleton lists.\n-- Stale refs (rebase of the pinned commits) fail `validate`; `serve` still starts, shows git at those SHAs, and flags the broken pointer. Re-run `review` and copy fresh refs. A dirty work tree does not make refs stale.\n+- A path stays valid when a rebase only shifts line numbers. `path@oldStart+newStart` goes stale when those starts move, and that fails `validate`. `serve` still starts, shows git at those SHAs, and flags the broken pointer. Re-run `review` and copy fresh paths. A dirty work tree does not make refs stale.\n \n-Hunk identity is `(path, oldStart, newStart)` plus `oldPath` when renamed. Copy `oldStart`, `oldLines`, `newStart`, and `newLines` from the skeleton. Image files are hunks; copy their refs into groups. Git LFS images are read from `.git/lfs/objects` in the clone; a missing object leaves the image slot empty.\n+A `hunkRefs` entry is a path string when the group holds every live hunk of that file. Copy the path from the skeleton. When a file is split across groups, name each hunk as `path@oldStart+newStart`, or `old/path -> new/path@oldStart+newStart` when that hunk is a rename. Take `oldStart` and `newStart` from the hunk's `@@ -oldStart,oldLines +newStart,newLines @@` header. The object form with `path`, `oldPath`, `oldStart`, `oldLines`, `newStart`, and `newLines` still validates. Identity is `(path, oldStart, newStart)` plus `oldPath` when renamed. Image files are paths. Git LFS images are read from `.git/lfs/objects` in the clone; a missing object leaves the image slot empty.\n \n ## lookFor\n \n","complete":true,"hunks":[{"path":"skills-next/comprehende/SKILL.md","oldStart":31,"oldLines":7,"newStart":31,"newLines":7,"header":"@@ -31,7 +31,7 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":31,"newNumber":31,"text":"   git diff --stat <base>...<head>"},{"kind":"ctx","oldNumber":32,"newNumber":32,"text":"   ```"},{"kind":"ctx","oldNumber":33,"newNumber":33,"text":""},{"kind":"del","oldNumber":34,"newNumber":null,"text":"   If `npm view` reports a version newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes every hunk ref into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those hunk refs; do not invent replacements."},{"kind":"add","oldNumber":null,"newNumber":34,"text":"   If `npm view` reports a version newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"ctx","oldNumber":35,"newNumber":35,"text":"3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec."},{"kind":"ctx","oldNumber":36,"newNumber":36,"text":""},{"kind":"ctx","oldNumber":37,"newNumber":37,"text":"   ```sh"}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":39,"oldLines":8,"newStart":39,"newLines":8,"header":"@@ -39,8 +39,8 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"   ```"},{"kind":"ctx","oldNumber":40,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":41,"text":"   Sources are listed under The why and The title. Done when the diff and those sources are in context."},{"kind":"del","oldNumber":42,"newNumber":null,"text":"4. Read the skeleton once, for its hunk refs. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"del","oldNumber":43,"newNumber":null,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy hunk objects from the skeleton; do not invent refs. Set document `size` from review burden, not `git diff --stat`. Shape and field names: [references/example.md](./references/example.md) shows every field. Write the whole `review.json` in one write; the skeleton's `ungrouped` group is replaced, not kept. Done when every hunk ref from the skeleton appears in at least one group, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"add","oldNumber":null,"newNumber":42,"text":"4. Read the skeleton once, for its paths. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"add","oldNumber":null,"newNumber":43,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Shape and field names: [references/example.md](./references/example.md) shows every field. Write the whole `review.json` in one write; the skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"ctx","oldNumber":44,"newNumber":44,"text":"6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"ctx","oldNumber":45,"newNumber":45,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":46,"newNumber":46,"text":""}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":95,"oldLines":11,"newStart":95,"newLines":11,"header":"@@ -95,11 +95,11 @@ Group `summary` is one sentence saying what the group is, describing how its hun","language":"markdown","lines":[{"kind":"ctx","oldNumber":95,"newNumber":95,"text":"- Mechanical work (import reordering, identifier-only renames, generated code, formatting, type re-exports) is its own group that keeps every hunk as refs, risk visible rather than folded into a file list. When it exists only because of a story, it is that part's last group with nothing depending on it. When it could have been its own PR, it is its own part."},{"kind":"ctx","oldNumber":96,"newNumber":96,"text":"- `suggestedOrder` walks the whole review, independent parts included. Mechanical parts, independent documentation, and test-only cleanup go last."},{"kind":"ctx","oldNumber":97,"newNumber":97,"text":"- Document `size` is human review burden: `trivial`, `small`, `medium`, `large`, `very-large`. Forty files changing one import in one group are `small`; three files rewriting a contract the rest of the system hangs on can be `large`."},{"kind":"del","oldNumber":98,"newNumber":null,"text":"- Coverage: every hunk from the skeleton sits in at least one group; duplicate refs across groups are allowed. Unreferenced hunks fail `validate` and show as Unassigned in the UI."},{"kind":"add","oldNumber":null,"newNumber":98,"text":"- Coverage: every live hunk sits in at least one group. A path covers every live hunk of that file. Duplicate refs across groups are allowed. Unreferenced hunks fail `validate` and show as Unassigned in the UI."},{"kind":"ctx","oldNumber":99,"newNumber":99,"text":"- Lockfiles have no hunk refs; the CLI leaves them out of the skeleton and the UI gives them their own closed bucket. Hunk refs exist only for hunks the skeleton lists."},{"kind":"del","oldNumber":100,"newNumber":null,"text":"- Stale refs (rebase of the pinned commits) fail `validate`; `serve` still starts, shows git at those SHAs, and flags the broken pointer. Re-run `review` and copy fresh refs. A dirty work tree does not make refs stale."},{"kind":"add","oldNumber":null,"newNumber":100,"text":"- A path stays valid when a rebase only shifts line numbers. `path@oldStart+newStart` goes stale when those starts move, and that fails `validate`. `serve` still starts, shows git at those SHAs, and flags the broken pointer. Re-run `review` and copy fresh paths. A dirty work tree does not make refs stale."},{"kind":"ctx","oldNumber":101,"newNumber":101,"text":""},{"kind":"del","oldNumber":102,"newNumber":null,"text":"Hunk identity is `(path, oldStart, newStart)` plus `oldPath` when renamed. Copy `oldStart`, `oldLines`, `newStart`, and `newLines` from the skeleton. Image files are hunks; copy their refs into groups. Git LFS images are read from `.git/lfs/objects` in the clone; a missing object leaves the image slot empty."},{"kind":"add","oldNumber":null,"newNumber":102,"text":"A `hunkRefs` entry is a path string when the group holds every live hunk of that file. Copy the path from the skeleton. When a file is split across groups, name each hunk as `path@oldStart+newStart`, or `old/path -> new/path@oldStart+newStart` when that hunk is a rename. Take `oldStart` and `newStart` from the hunk's `@@ -oldStart,oldLines +newStart,newLines @@` header. The object form with `path`, `oldPath`, `oldStart`, `oldLines`, `newStart`, and `newLines` still validates. Identity is `(path, oldStart, newStart)` plus `oldPath` when renamed. Image files are paths. Git LFS images are read from `.git/lfs/objects` in the clone; a missing object leaves the image slot empty."},{"kind":"ctx","oldNumber":103,"newNumber":103,"text":""},{"kind":"ctx","oldNumber":104,"newNumber":104,"text":"## lookFor"},{"kind":"ctx","oldNumber":105,"newNumber":105,"text":""}]}]},{"path":"skills-next/comprehende/references/example.md","kind":"text","status":"modified","patch":"diff --git a/skills-next/comprehende/references/example.md b/skills-next/comprehende/references/example.md\nindex 2b051d0..e23e4b9 100644\n--- a/skills-next/comprehende/references/example.md\n+++ b/skills-next/comprehende/references/example.md\n@@ -1,12 +1,13 @@\n # Example review.json\n \n-Pointers and prose only. The `@@` numbers must come from `comprehende review`, not from reading the patch.\n+Pointers and prose only. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header.\n \n - `login` depends on `cookie`. Both use `part` \"Session cookie\".\n - `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request.\n - Ticket #12 names why this work exists, so document `why` is present. Document `title` keeps the ticket title. Document `summary` names both stories. Document `parts` holds a one-sentence what per story. The why cites the ticket with `[#12](source:s1)`.\n - `login` `summary` names how those hunks meet. `login` `lookFor` is a predicted trace. `docs` has no `lookFor`.\n - Document `lookFor` compares ticket #12 with the diff: work the ticket asks for that no hunk does, and one claim about the whole change. Both cite `[#12](source:s1)`. Group `lookFor` stays inside its hunks.\n+- Each group holds every hunk of its file, so each `hunkRefs` entry is that path. A split file uses `path@oldStart+newStart` (`old/path -> new/path@oldStart+newStart` when renamed). The object form still validates.\n \n ```json\n {\n@@ -55,15 +56,7 @@ Pointers and prose only. The `@@` numbers must come from `comprehende review`, n\n         \"Breaking. `setSessionCookie` throws when the caller passes `httpOnly: false`.\"\n       ],\n       \"suggestedOrder\": 0,\n-      \"hunkRefs\": [\n-        {\n-          \"path\": \"src/auth/session.ts\",\n-          \"oldStart\": 1,\n-          \"oldLines\": 20,\n-          \"newStart\": 1,\n-          \"newLines\": 40\n-        }\n-      ]\n+      \"hunkRefs\": [\"src/auth/session.ts\"]\n     },\n     {\n       \"id\": \"login\",\n@@ -77,15 +70,7 @@ Pointers and prose only. The `@@` numbers must come from `comprehende review`, n\n       ],\n       \"dependsOn\": [\"cookie\"],\n       \"suggestedOrder\": 1,\n-      \"hunkRefs\": [\n-        {\n-          \"path\": \"src/api/login.ts\",\n-          \"oldStart\": 10,\n-          \"oldLines\": 8,\n-          \"newStart\": 10,\n-          \"newLines\": 24\n-        }\n-      ]\n+      \"hunkRefs\": [\"src/api/login.ts\"]\n     },\n     {\n       \"id\": \"docs\",\n@@ -94,15 +79,7 @@ Pointers and prose only. The `@@` numbers must come from `comprehende review`, n\n       \"summary\": \"The README section on sessions matches the new cookie behavior.\",\n       \"part\": \"README\",\n       \"suggestedOrder\": 2,\n-      \"hunkRefs\": [\n-        {\n-          \"path\": \"README.md\",\n-          \"oldStart\": 1,\n-          \"oldLines\": 4,\n-          \"newStart\": 1,\n-          \"newLines\": 8\n-        }\n-      ]\n+      \"hunkRefs\": [\"README.md\"]\n     }\n   ]\n }\n","complete":true,"hunks":[{"path":"skills-next/comprehende/references/example.md","oldStart":1,"oldLines":12,"newStart":1,"newLines":13,"header":"@@ -1,12 +1,13 @@","language":"markdown","lines":[{"kind":"ctx","oldNumber":1,"newNumber":1,"text":"# Example review.json"},{"kind":"ctx","oldNumber":2,"newNumber":2,"text":""},{"kind":"del","oldNumber":3,"newNumber":null,"text":"Pointers and prose only. The `@@` numbers must come from `comprehende review`, not from reading the patch."},{"kind":"add","oldNumber":null,"newNumber":3,"text":"Pointers and prose only. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header."},{"kind":"ctx","oldNumber":4,"newNumber":4,"text":""},{"kind":"ctx","oldNumber":5,"newNumber":5,"text":"- `login` depends on `cookie`. Both use `part` \"Session cookie\"."},{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"- `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request."},{"kind":"ctx","oldNumber":7,"newNumber":7,"text":"- Ticket #12 names why this work exists, so document `why` is present. Document `title` keeps the ticket title. Document `summary` names both stories. Document `parts` holds a one-sentence what per story. The why cites the ticket with `[#12](source:s1)`."},{"kind":"ctx","oldNumber":8,"newNumber":8,"text":"- `login` `summary` names how those hunks meet. `login` `lookFor` is a predicted trace. `docs` has no `lookFor`."},{"kind":"ctx","oldNumber":9,"newNumber":9,"text":"- Document `lookFor` compares ticket #12 with the diff: work the ticket asks for that no hunk does, and one claim about the whole change. Both cite `[#12](source:s1)`. Group `lookFor` stays inside its hunks."},{"kind":"add","oldNumber":null,"newNumber":10,"text":"- Each group holds every hunk of its file, so each `hunkRefs` entry is that path. A split file uses `path@oldStart+newStart` (`old/path -> new/path@oldStart+newStart` when renamed). The object form still validates."},{"kind":"ctx","oldNumber":10,"newNumber":11,"text":""},{"kind":"ctx","oldNumber":11,"newNumber":12,"text":"```json"},{"kind":"ctx","oldNumber":12,"newNumber":13,"text":"{"}]},{"path":"skills-next/comprehende/references/example.md","oldStart":55,"oldLines":15,"newStart":56,"newLines":7,"header":"@@ -55,15 +56,7 @@ Pointers and prose only. The `@@` numbers must come from `comprehende review`, n","language":"markdown","lines":[{"kind":"ctx","oldNumber":55,"newNumber":56,"text":"        \"Breaking. `setSessionCookie` throws when the caller passes `httpOnly: false`.\""},{"kind":"ctx","oldNumber":56,"newNumber":57,"text":"      ],"},{"kind":"ctx","oldNumber":57,"newNumber":58,"text":"      \"suggestedOrder\": 0,"},{"kind":"del","oldNumber":58,"newNumber":null,"text":"      \"hunkRefs\": ["},{"kind":"del","oldNumber":59,"newNumber":null,"text":"        {"},{"kind":"del","oldNumber":60,"newNumber":null,"text":"          \"path\": \"src/auth/session.ts\","},{"kind":"del","oldNumber":61,"newNumber":null,"text":"          \"oldStart\": 1,"},{"kind":"del","oldNumber":62,"newNumber":null,"text":"          \"oldLines\": 20,"},{"kind":"del","oldNumber":63,"newNumber":null,"text":"          \"newStart\": 1,"},{"kind":"del","oldNumber":64,"newNumber":null,"text":"          \"newLines\": 40"},{"kind":"del","oldNumber":65,"newNumber":null,"text":"        }"},{"kind":"del","oldNumber":66,"newNumber":null,"text":"      ]"},{"kind":"add","oldNumber":null,"newNumber":59,"text":"      \"hunkRefs\": [\"src/auth/session.ts\"]"},{"kind":"ctx","oldNumber":67,"newNumber":60,"text":"    },"},{"kind":"ctx","oldNumber":68,"newNumber":61,"text":"    {"},{"kind":"ctx","oldNumber":69,"newNumber":62,"text":"      \"id\": \"login\","}]},{"path":"skills-next/comprehende/references/example.md","oldStart":77,"oldLines":15,"newStart":70,"newLines":7,"header":"@@ -77,15 +70,7 @@ Pointers and prose only. The `@@` numbers must come from `comprehende review`, n","language":"markdown","lines":[{"kind":"ctx","oldNumber":77,"newNumber":70,"text":"      ],"},{"kind":"ctx","oldNumber":78,"newNumber":71,"text":"      \"dependsOn\": [\"cookie\"],"},{"kind":"ctx","oldNumber":79,"newNumber":72,"text":"      \"suggestedOrder\": 1,"},{"kind":"del","oldNumber":80,"newNumber":null,"text":"      \"hunkRefs\": ["},{"kind":"del","oldNumber":81,"newNumber":null,"text":"        {"},{"kind":"del","oldNumber":82,"newNumber":null,"text":"          \"path\": \"src/api/login.ts\","},{"kind":"del","oldNumber":83,"newNumber":null,"text":"          \"oldStart\": 10,"},{"kind":"del","oldNumber":84,"newNumber":null,"text":"          \"oldLines\": 8,"},{"kind":"del","oldNumber":85,"newNumber":null,"text":"          \"newStart\": 10,"},{"kind":"del","oldNumber":86,"newNumber":null,"text":"          \"newLines\": 24"},{"kind":"del","oldNumber":87,"newNumber":null,"text":"        }"},{"kind":"del","oldNumber":88,"newNumber":null,"text":"      ]"},{"kind":"add","oldNumber":null,"newNumber":73,"text":"      \"hunkRefs\": [\"src/api/login.ts\"]"},{"kind":"ctx","oldNumber":89,"newNumber":74,"text":"    },"},{"kind":"ctx","oldNumber":90,"newNumber":75,"text":"    {"},{"kind":"ctx","oldNumber":91,"newNumber":76,"text":"      \"id\": \"docs\","}]},{"path":"skills-next/comprehende/references/example.md","oldStart":94,"oldLines":15,"newStart":79,"newLines":7,"header":"@@ -94,15 +79,7 @@ Pointers and prose only. The `@@` numbers must come from `comprehende review`, n","language":"markdown","lines":[{"kind":"ctx","oldNumber":94,"newNumber":79,"text":"      \"summary\": \"The README section on sessions matches the new cookie behavior.\","},{"kind":"ctx","oldNumber":95,"newNumber":80,"text":"      \"part\": \"README\","},{"kind":"ctx","oldNumber":96,"newNumber":81,"text":"      \"suggestedOrder\": 2,"},{"kind":"del","oldNumber":97,"newNumber":null,"text":"      \"hunkRefs\": ["},{"kind":"del","oldNumber":98,"newNumber":null,"text":"        {"},{"kind":"del","oldNumber":99,"newNumber":null,"text":"          \"path\": \"README.md\","},{"kind":"del","oldNumber":100,"newNumber":null,"text":"          \"oldStart\": 1,"},{"kind":"del","oldNumber":101,"newNumber":null,"text":"          \"oldLines\": 4,"},{"kind":"del","oldNumber":102,"newNumber":null,"text":"          \"newStart\": 1,"},{"kind":"del","oldNumber":103,"newNumber":null,"text":"          \"newLines\": 8"},{"kind":"del","oldNumber":104,"newNumber":null,"text":"        }"},{"kind":"del","oldNumber":105,"newNumber":null,"text":"      ]"},{"kind":"add","oldNumber":null,"newNumber":82,"text":"      \"hunkRefs\": [\"README.md\"]"},{"kind":"ctx","oldNumber":106,"newNumber":83,"text":"    }"},{"kind":"ctx","oldNumber":107,"newNumber":84,"text":"  ]"},{"kind":"ctx","oldNumber":108,"newNumber":85,"text":"}"}]}]},{"path":"scripts/eval/skill.test.ts","kind":"text","status":"modified","patch":"diff --git a/scripts/eval/skill.test.ts b/scripts/eval/skill.test.ts\nindex 62f630b..cea8546 100644\n--- a/scripts/eval/skill.test.ts\n+++ b/scripts/eval/skill.test.ts\n@@ -37,6 +37,8 @@ describe(\"next skill workflow\", () => {\n     assert.doesNotMatch(md, /poetry\\.lock/);\n     assert.doesNotMatch(md, /go\\.sum/);\n     assert.match(grouping, /Lockfiles have no hunk refs/);\n+    assert.match(grouping, /path@oldStart\\+newStart/);\n+    assert.match(grouping, /A path covers every live hunk of that file/);\n     assert.doesNotMatch(grouping, /Lockfiles stay in `skipped`/);\n     const example = await readFile(join(findPackageRoot(), \"skills-next/comprehende/references/example.md\"), \"utf8\");\n     assert.doesNotMatch(example, /review\\.schema\\.json/);\n","complete":true,"hunks":[{"path":"scripts/eval/skill.test.ts","oldStart":37,"oldLines":6,"newStart":37,"newLines":8,"header":"@@ -37,6 +37,8 @@ describe(\"next skill workflow\", () => {","language":"typescript","lines":[{"kind":"ctx","oldNumber":37,"newNumber":37,"text":"    assert.doesNotMatch(md, /poetry\\.lock/);"},{"kind":"ctx","oldNumber":38,"newNumber":38,"text":"    assert.doesNotMatch(md, /go\\.sum/);"},{"kind":"ctx","oldNumber":39,"newNumber":39,"text":"    assert.match(grouping, /Lockfiles have no hunk refs/);"},{"kind":"add","oldNumber":null,"newNumber":40,"text":"    assert.match(grouping, /path@oldStart\\+newStart/);"},{"kind":"add","oldNumber":null,"newNumber":41,"text":"    assert.match(grouping, /A path covers every live hunk of that file/);"},{"kind":"ctx","oldNumber":40,"newNumber":42,"text":"    assert.doesNotMatch(grouping, /Lockfiles stay in `skipped`/);"},{"kind":"ctx","oldNumber":41,"newNumber":43,"text":"    const example = await readFile(join(findPackageRoot(), \"skills-next/comprehende/references/example.md\"), \"utf8\");"},{"kind":"ctx","oldNumber":42,"newNumber":44,"text":"    assert.doesNotMatch(example, /review\\.schema\\.json/);"}]}]}]}
