{"hunks":[{"path":"skills-next/comprehende/SKILL.md","oldStart":21,"oldLines":17,"newStart":21,"newLines":21,"header":"@@ -21,17 +21,21 @@ The review document is interpretation only. It holds a title, groups, summaries,","language":"markdown","lines":[{"kind":"ctx","oldNumber":21,"newNumber":21,"text":""},{"kind":"ctx","oldNumber":22,"newNumber":22,"text":"Each step is one tool call where the step says so. Batch the commands as written; every extra round trip re-sends the whole context."},{"kind":"ctx","oldNumber":23,"newNumber":23,"text":""},{"kind":"del","oldNumber":24,"newNumber":null,"text":"1. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"del","oldNumber":25,"newNumber":null,"text":"2. One shell call: version check, covering skeleton, log, and stat. `$REVIEW_DIR` is a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries)."},{"kind":"add","oldNumber":null,"newNumber":24,"text":"1. Name base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch a ref that is missing from the local clone. Done when both ref names are chosen. The next shell call verifies them."},{"kind":"add","oldNumber":null,"newNumber":25,"text":"2. One shell call: verify the refs, check the CLI version, write the covering skeleton, log, and stat. `$REVIEW_DIR` comes from `mktemp -d` in that call (outside the repository; the work tree stays untouched, with no new gitignore entries). `&&` stops the shell before `review` when a ref does not resolve. Stop and tell the user which ref failed, rather than guess a ref."},{"kind":"ctx","oldNumber":26,"newNumber":26,"text":""},{"kind":"ctx","oldNumber":27,"newNumber":27,"text":"   ```sh"},{"kind":"del","oldNumber":28,"newNumber":null,"text":"   npm view comprehende version"},{"kind":"del","oldNumber":29,"newNumber":null,"text":"   npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"del","oldNumber":30,"newNumber":null,"text":"   git log --format='%s%n%n%b' --end-of-options <base>...<head>"},{"kind":"del","oldNumber":31,"newNumber":null,"text":"   git diff --stat <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":28,"text":"   REVIEW_DIR=$(mktemp -d) && git rev-parse --verify --end-of-options \"<base>^{commit}\" && git rev-parse --verify --end-of-options \"<head>^{commit}\" && {"},{"kind":"add","oldNumber":null,"newNumber":29,"text":"     npm view comprehende version || true"},{"kind":"add","oldNumber":null,"newNumber":30,"text":"     npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"add","oldNumber":null,"newNumber":31,"text":"     git log --format='%s%n%n%b' --end-of-options <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":32,"text":"     git diff --stat <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":33,"text":"   }"},{"kind":"ctx","oldNumber":32,"newNumber":34,"text":"   ```"},{"kind":"ctx","oldNumber":33,"newNumber":35,"text":""},{"kind":"del","oldNumber":34,"newNumber":null,"text":"   If `npm view` reports a version newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"add","oldNumber":null,"newNumber":36,"text":"   When the user names a local executable, use that command as written in place of `npx comprehende@0.8.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version."},{"kind":"add","oldNumber":null,"newNumber":37,"text":""},{"kind":"add","oldNumber":null,"newNumber":38,"text":"   If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"ctx","oldNumber":35,"newNumber":39,"text":"3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec."},{"kind":"ctx","oldNumber":36,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":37,"newNumber":41,"text":"   ```sh"}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":40,"oldLines":10,"newStart":44,"newLines":36,"header":"@@ -40,10 +44,36 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":40,"newNumber":44,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":45,"text":"   Sources are listed under The why and The title. Done when the diff and those sources are in context."},{"kind":"ctx","oldNumber":42,"newNumber":46,"text":"4. Read the skeleton once, for its paths. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"del","oldNumber":43,"newNumber":null,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Shape and field names: [references/example.md](./references/example.md) shows every field. Write the whole `review.json` in one write; the skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"add","oldNumber":null,"newNumber":47,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Write the whole `review.json` in one write from the field shape below. Keep `version` and `source` from the skeleton. The skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"add","oldNumber":null,"newNumber":48,"text":""},{"kind":"add","oldNumber":null,"newNumber":49,"text":"   Field shape. The words are placeholders. This shape is enough to write a valid document."},{"kind":"add","oldNumber":null,"newNumber":50,"text":""},{"kind":"add","oldNumber":null,"newNumber":51,"text":"   ```json"},{"kind":"add","oldNumber":null,"newNumber":52,"text":"   {"},{"kind":"add","oldNumber":null,"newNumber":53,"text":"     \"version\": 1,"},{"kind":"add","oldNumber":null,"newNumber":54,"text":"     \"source\": { \"baseRef\": \"<base>\", \"headRef\": \"<head>\", \"range\": \"<base>...<head>\" },"},{"kind":"add","oldNumber":null,"newNumber":55,"text":"     \"size\": \"small\","},{"kind":"add","oldNumber":null,"newNumber":56,"text":"     \"title\": \"Short name\","},{"kind":"add","oldNumber":null,"newNumber":57,"text":"     \"summary\": \"What the change is.\","},{"kind":"add","oldNumber":null,"newNumber":58,"text":"     \"groups\": ["},{"kind":"add","oldNumber":null,"newNumber":59,"text":"       {"},{"kind":"add","oldNumber":null,"newNumber":60,"text":"         \"id\": \"helper\","},{"kind":"add","oldNumber":null,"newNumber":61,"text":"         \"title\": \"Group title\","},{"kind":"add","oldNumber":null,"newNumber":62,"text":"         \"why\": \"Why this group exists.\","},{"kind":"add","oldNumber":null,"newNumber":63,"text":"         \"summary\": \"What this group is.\","},{"kind":"add","oldNumber":null,"newNumber":64,"text":"         \"suggestedOrder\": 0,"},{"kind":"add","oldNumber":null,"newNumber":65,"text":"         \"hunkRefs\": [\"src/file.ts\"]"},{"kind":"add","oldNumber":null,"newNumber":66,"text":"       }"},{"kind":"add","oldNumber":null,"newNumber":67,"text":"     ]"},{"kind":"add","oldNumber":null,"newNumber":68,"text":"   }"},{"kind":"add","oldNumber":null,"newNumber":69,"text":"   ```"},{"kind":"add","oldNumber":null,"newNumber":70,"text":""},{"kind":"add","oldNumber":null,"newNumber":71,"text":"   Add a field when the section that defines it says to write it: document `why`, `lookFor`, and `parts` (`name`, `summary`); `sources` (`id`, `kind` of `ticket`, `pr`, `pr-comment`, `commit`, or `transcript`, `label`, plus `url`, `title`, `gist`, and `part` when you have them). On a group: `part`, `sources` (those ids), `lookFor`, `dependsOn` (group ids). A `pr-comment` source also has `author` and `body`. A line pin adds `path`, `side` (`old` or `new`), and `line` together. `size` is `trivial`, `small`, `medium`, `large`, or `very-large`."},{"kind":"ctx","oldNumber":44,"newNumber":72,"text":"6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"ctx","oldNumber":45,"newNumber":73,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":46,"newNumber":74,"text":""},{"kind":"add","oldNumber":null,"newNumber":75,"text":"`references/example.md` is an optional filled sample of the field shape. The shape in step 5 is enough to write the document."},{"kind":"add","oldNumber":null,"newNumber":76,"text":""},{"kind":"ctx","oldNumber":47,"newNumber":77,"text":"## Export"},{"kind":"ctx","oldNumber":48,"newNumber":78,"text":""},{"kind":"ctx","oldNumber":49,"newNumber":79,"text":"Write a static site and put that folder where they asked."}]},{"path":"skills-next/comprehende/references/example.md","oldStart":1,"oldLines":6,"newStart":1,"newLines":6,"header":"@@ -1,6 +1,6 @@","language":"markdown","lines":[{"kind":"ctx","oldNumber":1,"newNumber":1,"text":"# Example review.json"},{"kind":"ctx","oldNumber":2,"newNumber":2,"text":""},{"kind":"del","oldNumber":3,"newNumber":null,"text":"Pointers and prose only. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header."},{"kind":"add","oldNumber":null,"newNumber":3,"text":"Optional filled sample. The field shape in `SKILL.md` is enough to write a document. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header."},{"kind":"ctx","oldNumber":4,"newNumber":4,"text":""},{"kind":"ctx","oldNumber":5,"newNumber":5,"text":"- `login` depends on `cookie`. Both use `part` \"Session cookie\"."},{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"- `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request."}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":21,"oldLines":17,"newStart":21,"newLines":21,"header":"@@ -21,17 +21,21 @@ The review document is interpretation only. It holds a title, groups, summaries,","language":"markdown","lines":[{"kind":"ctx","oldNumber":21,"newNumber":21,"text":""},{"kind":"ctx","oldNumber":22,"newNumber":22,"text":"Each step is one tool call where the step says so. Batch the commands as written; every extra round trip re-sends the whole context."},{"kind":"ctx","oldNumber":23,"newNumber":23,"text":""},{"kind":"del","oldNumber":24,"newNumber":null,"text":"1. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"del","oldNumber":25,"newNumber":null,"text":"2. One shell call: version check, covering skeleton, log, and stat. `$REVIEW_DIR` is a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries)."},{"kind":"add","oldNumber":null,"newNumber":24,"text":"1. Name base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch a ref that is missing from the local clone. Done when both ref names are chosen. The next shell call verifies them."},{"kind":"add","oldNumber":null,"newNumber":25,"text":"2. One shell call: verify the refs, check the CLI version, write the covering skeleton, log, and stat. `$REVIEW_DIR` comes from `mktemp -d` in that call (outside the repository; the work tree stays untouched, with no new gitignore entries). `&&` stops the shell before `review` when a ref does not resolve. Stop and tell the user which ref failed, rather than guess a ref."},{"kind":"ctx","oldNumber":26,"newNumber":26,"text":""},{"kind":"ctx","oldNumber":27,"newNumber":27,"text":"   ```sh"},{"kind":"del","oldNumber":28,"newNumber":null,"text":"   npm view comprehende version"},{"kind":"del","oldNumber":29,"newNumber":null,"text":"   npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"del","oldNumber":30,"newNumber":null,"text":"   git log --format='%s%n%n%b' --end-of-options <base>...<head>"},{"kind":"del","oldNumber":31,"newNumber":null,"text":"   git diff --stat <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":28,"text":"   REVIEW_DIR=$(mktemp -d) && git rev-parse --verify --end-of-options \"<base>^{commit}\" && git rev-parse --verify --end-of-options \"<head>^{commit}\" && {"},{"kind":"add","oldNumber":null,"newNumber":29,"text":"     npm view comprehende version || true"},{"kind":"add","oldNumber":null,"newNumber":30,"text":"     npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"add","oldNumber":null,"newNumber":31,"text":"     git log --format='%s%n%n%b' --end-of-options <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":32,"text":"     git diff --stat <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":33,"text":"   }"},{"kind":"ctx","oldNumber":32,"newNumber":34,"text":"   ```"},{"kind":"ctx","oldNumber":33,"newNumber":35,"text":""},{"kind":"del","oldNumber":34,"newNumber":null,"text":"   If `npm view` reports a version newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"add","oldNumber":null,"newNumber":36,"text":"   When the user names a local executable, use that command as written in place of `npx comprehende@0.8.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version."},{"kind":"add","oldNumber":null,"newNumber":37,"text":""},{"kind":"add","oldNumber":null,"newNumber":38,"text":"   If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"ctx","oldNumber":35,"newNumber":39,"text":"3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec."},{"kind":"ctx","oldNumber":36,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":37,"newNumber":41,"text":"   ```sh"}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":40,"oldLines":10,"newStart":44,"newLines":36,"header":"@@ -40,10 +44,36 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":40,"newNumber":44,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":45,"text":"   Sources are listed under The why and The title. Done when the diff and those sources are in context."},{"kind":"ctx","oldNumber":42,"newNumber":46,"text":"4. Read the skeleton once, for its paths. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"del","oldNumber":43,"newNumber":null,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Shape and field names: [references/example.md](./references/example.md) shows every field. Write the whole `review.json` in one write; the skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"add","oldNumber":null,"newNumber":47,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Write the whole `review.json` in one write from the field shape below. Keep `version` and `source` from the skeleton. The skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"add","oldNumber":null,"newNumber":48,"text":""},{"kind":"add","oldNumber":null,"newNumber":49,"text":"   Field shape. The words are placeholders. This shape is enough to write a valid document."},{"kind":"add","oldNumber":null,"newNumber":50,"text":""},{"kind":"add","oldNumber":null,"newNumber":51,"text":"   ```json"},{"kind":"add","oldNumber":null,"newNumber":52,"text":"   {"},{"kind":"add","oldNumber":null,"newNumber":53,"text":"     \"version\": 1,"},{"kind":"add","oldNumber":null,"newNumber":54,"text":"     \"source\": { \"baseRef\": \"<base>\", \"headRef\": \"<head>\", \"range\": \"<base>...<head>\" },"},{"kind":"add","oldNumber":null,"newNumber":55,"text":"     \"size\": \"small\","},{"kind":"add","oldNumber":null,"newNumber":56,"text":"     \"title\": \"Short name\","},{"kind":"add","oldNumber":null,"newNumber":57,"text":"     \"summary\": \"What the change is.\","},{"kind":"add","oldNumber":null,"newNumber":58,"text":"     \"groups\": ["},{"kind":"add","oldNumber":null,"newNumber":59,"text":"       {"},{"kind":"add","oldNumber":null,"newNumber":60,"text":"         \"id\": \"helper\","},{"kind":"add","oldNumber":null,"newNumber":61,"text":"         \"title\": \"Group title\","},{"kind":"add","oldNumber":null,"newNumber":62,"text":"         \"why\": \"Why this group exists.\","},{"kind":"add","oldNumber":null,"newNumber":63,"text":"         \"summary\": \"What this group is.\","},{"kind":"add","oldNumber":null,"newNumber":64,"text":"         \"suggestedOrder\": 0,"},{"kind":"add","oldNumber":null,"newNumber":65,"text":"         \"hunkRefs\": [\"src/file.ts\"]"},{"kind":"add","oldNumber":null,"newNumber":66,"text":"       }"},{"kind":"add","oldNumber":null,"newNumber":67,"text":"     ]"},{"kind":"add","oldNumber":null,"newNumber":68,"text":"   }"},{"kind":"add","oldNumber":null,"newNumber":69,"text":"   ```"},{"kind":"add","oldNumber":null,"newNumber":70,"text":""},{"kind":"add","oldNumber":null,"newNumber":71,"text":"   Add a field when the section that defines it says to write it: document `why`, `lookFor`, and `parts` (`name`, `summary`); `sources` (`id`, `kind` of `ticket`, `pr`, `pr-comment`, `commit`, or `transcript`, `label`, plus `url`, `title`, `gist`, and `part` when you have them). On a group: `part`, `sources` (those ids), `lookFor`, `dependsOn` (group ids). A `pr-comment` source also has `author` and `body`. A line pin adds `path`, `side` (`old` or `new`), and `line` together. `size` is `trivial`, `small`, `medium`, `large`, or `very-large`."},{"kind":"ctx","oldNumber":44,"newNumber":72,"text":"6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"ctx","oldNumber":45,"newNumber":73,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":46,"newNumber":74,"text":""},{"kind":"add","oldNumber":null,"newNumber":75,"text":"`references/example.md` is an optional filled sample of the field shape. The shape in step 5 is enough to write the document."},{"kind":"add","oldNumber":null,"newNumber":76,"text":""},{"kind":"ctx","oldNumber":47,"newNumber":77,"text":"## Export"},{"kind":"ctx","oldNumber":48,"newNumber":78,"text":""},{"kind":"ctx","oldNumber":49,"newNumber":79,"text":"Write a static site and put that folder where they asked."}]},{"path":".agents/skills/comprehende/references/example.md","oldStart":1,"oldLines":6,"newStart":1,"newLines":6,"header":"@@ -1,6 +1,6 @@","language":"markdown","lines":[{"kind":"ctx","oldNumber":1,"newNumber":1,"text":"# Example review.json"},{"kind":"ctx","oldNumber":2,"newNumber":2,"text":""},{"kind":"del","oldNumber":3,"newNumber":null,"text":"Pointers and prose only. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header."},{"kind":"add","oldNumber":null,"newNumber":3,"text":"Optional filled sample. The field shape in `SKILL.md` is enough to write a document. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header."},{"kind":"ctx","oldNumber":4,"newNumber":4,"text":""},{"kind":"ctx","oldNumber":5,"newNumber":5,"text":"- `login` depends on `cookie`. Both use `part` \"Session cookie\"."},{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"- `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request."}]}],"files":[{"path":"skills-next/comprehende/SKILL.md","kind":"text","status":"modified","patch":"diff --git a/skills-next/comprehende/SKILL.md b/skills-next/comprehende/SKILL.md\nindex ca3703e..9f77e85 100644\n--- a/skills-next/comprehende/SKILL.md\n+++ b/skills-next/comprehende/SKILL.md\n@@ -21,17 +21,21 @@ The review document is interpretation only. It holds a title, groups, summaries,\n \n Each step is one tool call where the step says so. Batch the commands as written; every extra round trip re-sends the whole context.\n \n-1. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref.\n-2. One shell call: version check, covering skeleton, log, and stat. `$REVIEW_DIR` is a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries).\n+1. Name base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch a ref that is missing from the local clone. Done when both ref names are chosen. The next shell call verifies them.\n+2. One shell call: verify the refs, check the CLI version, write the covering skeleton, log, and stat. `$REVIEW_DIR` comes from `mktemp -d` in that call (outside the repository; the work tree stays untouched, with no new gitignore entries). `&&` stops the shell before `review` when a ref does not resolve. Stop and tell the user which ref failed, rather than guess a ref.\n \n    ```sh\n-   npm view comprehende version\n-   npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\"\n-   git log --format='%s%n%n%b' --end-of-options <base>...<head>\n-   git diff --stat <base>...<head>\n+   REVIEW_DIR=$(mktemp -d) && git rev-parse --verify --end-of-options \"<base>^{commit}\" && git rev-parse --verify --end-of-options \"<head>^{commit}\" && {\n+     npm view comprehende version || true\n+     npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\"\n+     git log --format='%s%n%n%b' --end-of-options <base>...<head>\n+     git diff --stat <base>...<head>\n+   }\n    ```\n \n-   If `npm view` reports a version newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file.\n+   When the user names a local executable, use that command as written in place of `npx comprehende@0.8.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version.\n+\n+   If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file.\n 3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec.\n \n    ```sh\n@@ -40,10 +44,36 @@ Each step is one tool call where the step says so. Batch the commands as written\n \n    Sources are listed under The why and The title. Done when the diff and those sources are in context.\n 4. Read the skeleton once, for its paths. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet.\n-5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Shape and field names: [references/example.md](./references/example.md) shows every field. Write the whole `review.json` in one write; the skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry.\n+5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Write the whole `review.json` in one write from the field shape below. Keep `version` and `source` from the skeleton. The skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry.\n+\n+   Field shape. The words are placeholders. This shape is enough to write a valid document.\n+\n+   ```json\n+   {\n+     \"version\": 1,\n+     \"source\": { \"baseRef\": \"<base>\", \"headRef\": \"<head>\", \"range\": \"<base>...<head>\" },\n+     \"size\": \"small\",\n+     \"title\": \"Short name\",\n+     \"summary\": \"What the change is.\",\n+     \"groups\": [\n+       {\n+         \"id\": \"helper\",\n+         \"title\": \"Group title\",\n+         \"why\": \"Why this group exists.\",\n+         \"summary\": \"What this group is.\",\n+         \"suggestedOrder\": 0,\n+         \"hunkRefs\": [\"src/file.ts\"]\n+       }\n+     ]\n+   }\n+   ```\n+\n+   Add a field when the section that defines it says to write it: document `why`, `lookFor`, and `parts` (`name`, `summary`); `sources` (`id`, `kind` of `ticket`, `pr`, `pr-comment`, `commit`, or `transcript`, `label`, plus `url`, `title`, `gist`, and `part` when you have them). On a group: `part`, `sources` (those ids), `lookFor`, `dependsOn` (group ids). A `pr-comment` source also has `author` and `body`. A line pin adds `path`, `side` (`old` or `new`), and `line` together. `size` is `trivial`, `small`, `medium`, `large`, or `very-large`.\n 6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0.\n 7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n \n+`references/example.md` is an optional filled sample of the field shape. The shape in step 5 is enough to write the document.\n+\n ## Export\n \n Write a static site and put that folder where they asked.\n","complete":true,"hunks":[{"path":"skills-next/comprehende/SKILL.md","oldStart":21,"oldLines":17,"newStart":21,"newLines":21,"header":"@@ -21,17 +21,21 @@ The review document is interpretation only. It holds a title, groups, summaries,","language":"markdown","lines":[{"kind":"ctx","oldNumber":21,"newNumber":21,"text":""},{"kind":"ctx","oldNumber":22,"newNumber":22,"text":"Each step is one tool call where the step says so. Batch the commands as written; every extra round trip re-sends the whole context."},{"kind":"ctx","oldNumber":23,"newNumber":23,"text":""},{"kind":"del","oldNumber":24,"newNumber":null,"text":"1. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"del","oldNumber":25,"newNumber":null,"text":"2. One shell call: version check, covering skeleton, log, and stat. `$REVIEW_DIR` is a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries)."},{"kind":"add","oldNumber":null,"newNumber":24,"text":"1. Name base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch a ref that is missing from the local clone. Done when both ref names are chosen. The next shell call verifies them."},{"kind":"add","oldNumber":null,"newNumber":25,"text":"2. One shell call: verify the refs, check the CLI version, write the covering skeleton, log, and stat. `$REVIEW_DIR` comes from `mktemp -d` in that call (outside the repository; the work tree stays untouched, with no new gitignore entries). `&&` stops the shell before `review` when a ref does not resolve. Stop and tell the user which ref failed, rather than guess a ref."},{"kind":"ctx","oldNumber":26,"newNumber":26,"text":""},{"kind":"ctx","oldNumber":27,"newNumber":27,"text":"   ```sh"},{"kind":"del","oldNumber":28,"newNumber":null,"text":"   npm view comprehende version"},{"kind":"del","oldNumber":29,"newNumber":null,"text":"   npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"del","oldNumber":30,"newNumber":null,"text":"   git log --format='%s%n%n%b' --end-of-options <base>...<head>"},{"kind":"del","oldNumber":31,"newNumber":null,"text":"   git diff --stat <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":28,"text":"   REVIEW_DIR=$(mktemp -d) && git rev-parse --verify --end-of-options \"<base>^{commit}\" && git rev-parse --verify --end-of-options \"<head>^{commit}\" && {"},{"kind":"add","oldNumber":null,"newNumber":29,"text":"     npm view comprehende version || true"},{"kind":"add","oldNumber":null,"newNumber":30,"text":"     npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"add","oldNumber":null,"newNumber":31,"text":"     git log --format='%s%n%n%b' --end-of-options <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":32,"text":"     git diff --stat <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":33,"text":"   }"},{"kind":"ctx","oldNumber":32,"newNumber":34,"text":"   ```"},{"kind":"ctx","oldNumber":33,"newNumber":35,"text":""},{"kind":"del","oldNumber":34,"newNumber":null,"text":"   If `npm view` reports a version newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"add","oldNumber":null,"newNumber":36,"text":"   When the user names a local executable, use that command as written in place of `npx comprehende@0.8.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version."},{"kind":"add","oldNumber":null,"newNumber":37,"text":""},{"kind":"add","oldNumber":null,"newNumber":38,"text":"   If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"ctx","oldNumber":35,"newNumber":39,"text":"3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec."},{"kind":"ctx","oldNumber":36,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":37,"newNumber":41,"text":"   ```sh"}]},{"path":"skills-next/comprehende/SKILL.md","oldStart":40,"oldLines":10,"newStart":44,"newLines":36,"header":"@@ -40,10 +44,36 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":40,"newNumber":44,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":45,"text":"   Sources are listed under The why and The title. Done when the diff and those sources are in context."},{"kind":"ctx","oldNumber":42,"newNumber":46,"text":"4. Read the skeleton once, for its paths. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"del","oldNumber":43,"newNumber":null,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Shape and field names: [references/example.md](./references/example.md) shows every field. Write the whole `review.json` in one write; the skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"add","oldNumber":null,"newNumber":47,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Write the whole `review.json` in one write from the field shape below. Keep `version` and `source` from the skeleton. The skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"add","oldNumber":null,"newNumber":48,"text":""},{"kind":"add","oldNumber":null,"newNumber":49,"text":"   Field shape. The words are placeholders. This shape is enough to write a valid document."},{"kind":"add","oldNumber":null,"newNumber":50,"text":""},{"kind":"add","oldNumber":null,"newNumber":51,"text":"   ```json"},{"kind":"add","oldNumber":null,"newNumber":52,"text":"   {"},{"kind":"add","oldNumber":null,"newNumber":53,"text":"     \"version\": 1,"},{"kind":"add","oldNumber":null,"newNumber":54,"text":"     \"source\": { \"baseRef\": \"<base>\", \"headRef\": \"<head>\", \"range\": \"<base>...<head>\" },"},{"kind":"add","oldNumber":null,"newNumber":55,"text":"     \"size\": \"small\","},{"kind":"add","oldNumber":null,"newNumber":56,"text":"     \"title\": \"Short name\","},{"kind":"add","oldNumber":null,"newNumber":57,"text":"     \"summary\": \"What the change is.\","},{"kind":"add","oldNumber":null,"newNumber":58,"text":"     \"groups\": ["},{"kind":"add","oldNumber":null,"newNumber":59,"text":"       {"},{"kind":"add","oldNumber":null,"newNumber":60,"text":"         \"id\": \"helper\","},{"kind":"add","oldNumber":null,"newNumber":61,"text":"         \"title\": \"Group title\","},{"kind":"add","oldNumber":null,"newNumber":62,"text":"         \"why\": \"Why this group exists.\","},{"kind":"add","oldNumber":null,"newNumber":63,"text":"         \"summary\": \"What this group is.\","},{"kind":"add","oldNumber":null,"newNumber":64,"text":"         \"suggestedOrder\": 0,"},{"kind":"add","oldNumber":null,"newNumber":65,"text":"         \"hunkRefs\": [\"src/file.ts\"]"},{"kind":"add","oldNumber":null,"newNumber":66,"text":"       }"},{"kind":"add","oldNumber":null,"newNumber":67,"text":"     ]"},{"kind":"add","oldNumber":null,"newNumber":68,"text":"   }"},{"kind":"add","oldNumber":null,"newNumber":69,"text":"   ```"},{"kind":"add","oldNumber":null,"newNumber":70,"text":""},{"kind":"add","oldNumber":null,"newNumber":71,"text":"   Add a field when the section that defines it says to write it: document `why`, `lookFor`, and `parts` (`name`, `summary`); `sources` (`id`, `kind` of `ticket`, `pr`, `pr-comment`, `commit`, or `transcript`, `label`, plus `url`, `title`, `gist`, and `part` when you have them). On a group: `part`, `sources` (those ids), `lookFor`, `dependsOn` (group ids). A `pr-comment` source also has `author` and `body`. A line pin adds `path`, `side` (`old` or `new`), and `line` together. `size` is `trivial`, `small`, `medium`, `large`, or `very-large`."},{"kind":"ctx","oldNumber":44,"newNumber":72,"text":"6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"ctx","oldNumber":45,"newNumber":73,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":46,"newNumber":74,"text":""},{"kind":"add","oldNumber":null,"newNumber":75,"text":"`references/example.md` is an optional filled sample of the field shape. The shape in step 5 is enough to write the document."},{"kind":"add","oldNumber":null,"newNumber":76,"text":""},{"kind":"ctx","oldNumber":47,"newNumber":77,"text":"## Export"},{"kind":"ctx","oldNumber":48,"newNumber":78,"text":""},{"kind":"ctx","oldNumber":49,"newNumber":79,"text":"Write a static site and put that folder where they asked."}]}]},{"path":"skills-next/comprehende/references/example.md","kind":"text","status":"modified","patch":"diff --git a/skills-next/comprehende/references/example.md b/skills-next/comprehende/references/example.md\nindex 3012ff6..b4e0059 100644\n--- a/skills-next/comprehende/references/example.md\n+++ b/skills-next/comprehende/references/example.md\n@@ -1,6 +1,6 @@\n # Example review.json\n \n-Pointers and prose only. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header.\n+Optional filled sample. The field shape in `SKILL.md` is enough to write a document. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header.\n \n - `login` depends on `cookie`. Both use `part` \"Session cookie\".\n - `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request.\n","complete":true,"hunks":[{"path":"skills-next/comprehende/references/example.md","oldStart":1,"oldLines":6,"newStart":1,"newLines":6,"header":"@@ -1,6 +1,6 @@","language":"markdown","lines":[{"kind":"ctx","oldNumber":1,"newNumber":1,"text":"# Example review.json"},{"kind":"ctx","oldNumber":2,"newNumber":2,"text":""},{"kind":"del","oldNumber":3,"newNumber":null,"text":"Pointers and prose only. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header."},{"kind":"add","oldNumber":null,"newNumber":3,"text":"Optional filled sample. The field shape in `SKILL.md` is enough to write a document. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header."},{"kind":"ctx","oldNumber":4,"newNumber":4,"text":""},{"kind":"ctx","oldNumber":5,"newNumber":5,"text":"- `login` depends on `cookie`. Both use `part` \"Session cookie\"."},{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"- `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request."}]}]},{"path":".agents/skills/comprehende/SKILL.md","kind":"text","status":"modified","patch":"diff --git a/.agents/skills/comprehende/SKILL.md b/.agents/skills/comprehende/SKILL.md\nindex ca3703e..9f77e85 100644\n--- a/.agents/skills/comprehende/SKILL.md\n+++ b/.agents/skills/comprehende/SKILL.md\n@@ -21,17 +21,21 @@ The review document is interpretation only. It holds a title, groups, summaries,\n \n Each step is one tool call where the step says so. Batch the commands as written; every extra round trip re-sends the whole context.\n \n-1. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref.\n-2. One shell call: version check, covering skeleton, log, and stat. `$REVIEW_DIR` is a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries).\n+1. Name base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch a ref that is missing from the local clone. Done when both ref names are chosen. The next shell call verifies them.\n+2. One shell call: verify the refs, check the CLI version, write the covering skeleton, log, and stat. `$REVIEW_DIR` comes from `mktemp -d` in that call (outside the repository; the work tree stays untouched, with no new gitignore entries). `&&` stops the shell before `review` when a ref does not resolve. Stop and tell the user which ref failed, rather than guess a ref.\n \n    ```sh\n-   npm view comprehende version\n-   npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\"\n-   git log --format='%s%n%n%b' --end-of-options <base>...<head>\n-   git diff --stat <base>...<head>\n+   REVIEW_DIR=$(mktemp -d) && git rev-parse --verify --end-of-options \"<base>^{commit}\" && git rev-parse --verify --end-of-options \"<head>^{commit}\" && {\n+     npm view comprehende version || true\n+     npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\"\n+     git log --format='%s%n%n%b' --end-of-options <base>...<head>\n+     git diff --stat <base>...<head>\n+   }\n    ```\n \n-   If `npm view` reports a version newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file.\n+   When the user names a local executable, use that command as written in place of `npx comprehende@0.8.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version.\n+\n+   If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file.\n 3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec.\n \n    ```sh\n@@ -40,10 +44,36 @@ Each step is one tool call where the step says so. Batch the commands as written\n \n    Sources are listed under The why and The title. Done when the diff and those sources are in context.\n 4. Read the skeleton once, for its paths. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet.\n-5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Shape and field names: [references/example.md](./references/example.md) shows every field. Write the whole `review.json` in one write; the skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry.\n+5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Write the whole `review.json` in one write from the field shape below. Keep `version` and `source` from the skeleton. The skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry.\n+\n+   Field shape. The words are placeholders. This shape is enough to write a valid document.\n+\n+   ```json\n+   {\n+     \"version\": 1,\n+     \"source\": { \"baseRef\": \"<base>\", \"headRef\": \"<head>\", \"range\": \"<base>...<head>\" },\n+     \"size\": \"small\",\n+     \"title\": \"Short name\",\n+     \"summary\": \"What the change is.\",\n+     \"groups\": [\n+       {\n+         \"id\": \"helper\",\n+         \"title\": \"Group title\",\n+         \"why\": \"Why this group exists.\",\n+         \"summary\": \"What this group is.\",\n+         \"suggestedOrder\": 0,\n+         \"hunkRefs\": [\"src/file.ts\"]\n+       }\n+     ]\n+   }\n+   ```\n+\n+   Add a field when the section that defines it says to write it: document `why`, `lookFor`, and `parts` (`name`, `summary`); `sources` (`id`, `kind` of `ticket`, `pr`, `pr-comment`, `commit`, or `transcript`, `label`, plus `url`, `title`, `gist`, and `part` when you have them). On a group: `part`, `sources` (those ids), `lookFor`, `dependsOn` (group ids). A `pr-comment` source also has `author` and `body`. A line pin adds `path`, `side` (`old` or `new`), and `line` together. `size` is `trivial`, `small`, `medium`, `large`, or `very-large`.\n 6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0.\n 7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only).\n \n+`references/example.md` is an optional filled sample of the field shape. The shape in step 5 is enough to write the document.\n+\n ## Export\n \n Write a static site and put that folder where they asked.\n","complete":true,"hunks":[{"path":".agents/skills/comprehende/SKILL.md","oldStart":21,"oldLines":17,"newStart":21,"newLines":21,"header":"@@ -21,17 +21,21 @@ The review document is interpretation only. It holds a title, groups, summaries,","language":"markdown","lines":[{"kind":"ctx","oldNumber":21,"newNumber":21,"text":""},{"kind":"ctx","oldNumber":22,"newNumber":22,"text":"Each step is one tool call where the step says so. Batch the commands as written; every extra round trip re-sends the whole context."},{"kind":"ctx","oldNumber":23,"newNumber":23,"text":""},{"kind":"del","oldNumber":24,"newNumber":null,"text":"1. Resolve base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch refs missing from the local clone. Done when both refs resolve in cwd; if one still does not resolve, stop and tell the user rather than guess a ref."},{"kind":"del","oldNumber":25,"newNumber":null,"text":"2. One shell call: version check, covering skeleton, log, and stat. `$REVIEW_DIR` is a fresh temp directory outside the repository (`mktemp -d` or the platform equivalent; the work tree stays untouched, with no new gitignore entries)."},{"kind":"add","oldNumber":null,"newNumber":24,"text":"1. Name base and head. Use the refs the user named; three-dot (`base...head`) is the merge request or branch diff. When the change is already on the default branch, use the request's recorded base and head SHAs; the moving default-branch `HEAD` includes later merges. When only the head SHA is known, base is the merge-base of that head with the named base branch. Fetch a ref that is missing from the local clone. Done when both ref names are chosen. The next shell call verifies them."},{"kind":"add","oldNumber":null,"newNumber":25,"text":"2. One shell call: verify the refs, check the CLI version, write the covering skeleton, log, and stat. `$REVIEW_DIR` comes from `mktemp -d` in that call (outside the repository; the work tree stays untouched, with no new gitignore entries). `&&` stops the shell before `review` when a ref does not resolve. Stop and tell the user which ref failed, rather than guess a ref."},{"kind":"ctx","oldNumber":26,"newNumber":26,"text":""},{"kind":"ctx","oldNumber":27,"newNumber":27,"text":"   ```sh"},{"kind":"del","oldNumber":28,"newNumber":null,"text":"   npm view comprehende version"},{"kind":"del","oldNumber":29,"newNumber":null,"text":"   npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"del","oldNumber":30,"newNumber":null,"text":"   git log --format='%s%n%n%b' --end-of-options <base>...<head>"},{"kind":"del","oldNumber":31,"newNumber":null,"text":"   git diff --stat <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":28,"text":"   REVIEW_DIR=$(mktemp -d) && git rev-parse --verify --end-of-options \"<base>^{commit}\" && git rev-parse --verify --end-of-options \"<head>^{commit}\" && {"},{"kind":"add","oldNumber":null,"newNumber":29,"text":"     npm view comprehende version || true"},{"kind":"add","oldNumber":null,"newNumber":30,"text":"     npx comprehende@0.8.0 review --base <base> --head <head> --data \"$REVIEW_DIR/review.json\""},{"kind":"add","oldNumber":null,"newNumber":31,"text":"     git log --format='%s%n%n%b' --end-of-options <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":32,"text":"     git diff --stat <base>...<head>"},{"kind":"add","oldNumber":null,"newNumber":33,"text":"   }"},{"kind":"ctx","oldNumber":32,"newNumber":34,"text":"   ```"},{"kind":"ctx","oldNumber":33,"newNumber":35,"text":""},{"kind":"del","oldNumber":34,"newNumber":null,"text":"   If `npm view` reports a version newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"add","oldNumber":null,"newNumber":36,"text":"   When the user names a local executable, use that command as written in place of `npx comprehende@0.8.0`, and run `<executable> --version` in this same shell call in place of `npm view`. That printed line is the CLI version."},{"kind":"add","oldNumber":null,"newNumber":37,"text":""},{"kind":"add","oldNumber":null,"newNumber":38,"text":"   If that version is newer than this pin (`npx comprehende@0.8.0`), stop and tell the user. Show `npx skills update` as an option they can run. Do not run that command. Wait for them to continue with this pin, or to update and start this skill again. If the versions match or the query fails, continue. Defaults: `--head` is `HEAD`; `--base` is `origin/HEAD`, falling back to `main` or `master`. `review` indexes live git and writes one path per changed file into one group named `ungrouped`, with stub prose. It does not invent a review. Done when that file exists. Keep those paths. A path covers every live hunk of that file."},{"kind":"ctx","oldNumber":35,"newNumber":39,"text":"3. One shell call for the covering diff, lockfiles in that covering change excluded (the skeleton has no hunk refs for them), and read the sources in the same round trip. From the `--stat` in step 2, pass `:(exclude)<path>` for each lockfile in that covering change. Nested paths stay nested. When the covering `--stat` has no lockfile, run `git diff` with no pathspec."},{"kind":"ctx","oldNumber":36,"newNumber":40,"text":""},{"kind":"ctx","oldNumber":37,"newNumber":41,"text":"   ```sh"}]},{"path":".agents/skills/comprehende/SKILL.md","oldStart":40,"oldLines":10,"newStart":44,"newLines":36,"header":"@@ -40,10 +44,36 @@ Each step is one tool call where the step says so. Batch the commands as written","language":"markdown","lines":[{"kind":"ctx","oldNumber":40,"newNumber":44,"text":""},{"kind":"ctx","oldNumber":41,"newNumber":45,"text":"   Sources are listed under The why and The title. Done when the diff and those sources are in context."},{"kind":"ctx","oldNumber":42,"newNumber":46,"text":"4. Read the skeleton once, for its paths. Recover the why, write the title, write the what, and compare the sources with the diff. Then write document `title` (always), document `summary` (always), document `why` (only when a source names the motive), and document `lookFor` (only when a source names work to check against the diff; see lookFor). Summaries come from the code, not the log. Replace the stub title and summary. Done when every piece of work a source names is either in the diff or in a document `lookFor` bullet."},{"kind":"del","oldNumber":43,"newNumber":null,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Shape and field names: [references/example.md](./references/example.md) shows every field. Write the whole `review.json` in one write; the skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"add","oldNumber":null,"newNumber":47,"text":"5. Group the hunks by review concern, following the Grouping rules. Split the covering group. Copy each path from the skeleton into the group that holds that file. When one file's hunks belong in different groups, name each hunk as in the hunk identity paragraph. Set document `size` from review burden, not `git diff --stat`. Write the whole `review.json` in one write from the field shape below. Keep `version` and `source` from the skeleton. The skeleton's `ungrouped` group is replaced, not kept. Done when every path from the skeleton is in at least one group, every live hunk of a split file is named, every group has its `why`, stub prose is gone, and every named `part` has a matching document `parts[]` entry."},{"kind":"add","oldNumber":null,"newNumber":48,"text":""},{"kind":"add","oldNumber":null,"newNumber":49,"text":"   Field shape. The words are placeholders. This shape is enough to write a valid document."},{"kind":"add","oldNumber":null,"newNumber":50,"text":""},{"kind":"add","oldNumber":null,"newNumber":51,"text":"   ```json"},{"kind":"add","oldNumber":null,"newNumber":52,"text":"   {"},{"kind":"add","oldNumber":null,"newNumber":53,"text":"     \"version\": 1,"},{"kind":"add","oldNumber":null,"newNumber":54,"text":"     \"source\": { \"baseRef\": \"<base>\", \"headRef\": \"<head>\", \"range\": \"<base>...<head>\" },"},{"kind":"add","oldNumber":null,"newNumber":55,"text":"     \"size\": \"small\","},{"kind":"add","oldNumber":null,"newNumber":56,"text":"     \"title\": \"Short name\","},{"kind":"add","oldNumber":null,"newNumber":57,"text":"     \"summary\": \"What the change is.\","},{"kind":"add","oldNumber":null,"newNumber":58,"text":"     \"groups\": ["},{"kind":"add","oldNumber":null,"newNumber":59,"text":"       {"},{"kind":"add","oldNumber":null,"newNumber":60,"text":"         \"id\": \"helper\","},{"kind":"add","oldNumber":null,"newNumber":61,"text":"         \"title\": \"Group title\","},{"kind":"add","oldNumber":null,"newNumber":62,"text":"         \"why\": \"Why this group exists.\","},{"kind":"add","oldNumber":null,"newNumber":63,"text":"         \"summary\": \"What this group is.\","},{"kind":"add","oldNumber":null,"newNumber":64,"text":"         \"suggestedOrder\": 0,"},{"kind":"add","oldNumber":null,"newNumber":65,"text":"         \"hunkRefs\": [\"src/file.ts\"]"},{"kind":"add","oldNumber":null,"newNumber":66,"text":"       }"},{"kind":"add","oldNumber":null,"newNumber":67,"text":"     ]"},{"kind":"add","oldNumber":null,"newNumber":68,"text":"   }"},{"kind":"add","oldNumber":null,"newNumber":69,"text":"   ```"},{"kind":"add","oldNumber":null,"newNumber":70,"text":""},{"kind":"add","oldNumber":null,"newNumber":71,"text":"   Add a field when the section that defines it says to write it: document `why`, `lookFor`, and `parts` (`name`, `summary`); `sources` (`id`, `kind` of `ticket`, `pr`, `pr-comment`, `commit`, or `transcript`, `label`, plus `url`, `title`, `gist`, and `part` when you have them). On a group: `part`, `sources` (those ids), `lookFor`, `dependsOn` (group ids). A `pr-comment` source also has `author` and `body`. A line pin adds `path`, `side` (`old` or `new`), and `line` together. `size` is `trivial`, `small`, `medium`, `large`, or `very-large`."},{"kind":"ctx","oldNumber":44,"newNumber":72,"text":"6. Run `npx comprehende@0.8.0 validate --data \"$REVIEW_DIR/review.json\"` with the absolute path. It checks exactly these: every live hunk sits in a group, every ref matches live git, every `source:` id exists in `sources`, every group `part` has a `parts[]` entry, every PR comment pin (`path`, `side`, `line`) matches a live line, and the document has no unknown fields. On failure, fix what the message names; the diff is git's, leave it alone. Done when validate exits 0."},{"kind":"ctx","oldNumber":45,"newNumber":73,"text":"7. When they ask to upload the report, follow Export. Otherwise run `npx comprehende@0.8.0 serve --data \"$REVIEW_DIR/review.json\" --open` and give the user the localhost URL (`127.0.0.1` only)."},{"kind":"ctx","oldNumber":46,"newNumber":74,"text":""},{"kind":"add","oldNumber":null,"newNumber":75,"text":"`references/example.md` is an optional filled sample of the field shape. The shape in step 5 is enough to write the document."},{"kind":"add","oldNumber":null,"newNumber":76,"text":""},{"kind":"ctx","oldNumber":47,"newNumber":77,"text":"## Export"},{"kind":"ctx","oldNumber":48,"newNumber":78,"text":""},{"kind":"ctx","oldNumber":49,"newNumber":79,"text":"Write a static site and put that folder where they asked."}]}]},{"path":".agents/skills/comprehende/references/example.md","kind":"text","status":"modified","patch":"diff --git a/.agents/skills/comprehende/references/example.md b/.agents/skills/comprehende/references/example.md\nindex 3012ff6..b4e0059 100644\n--- a/.agents/skills/comprehende/references/example.md\n+++ b/.agents/skills/comprehende/references/example.md\n@@ -1,6 +1,6 @@\n # Example review.json\n \n-Pointers and prose only. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header.\n+Optional filled sample. The field shape in `SKILL.md` is enough to write a document. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header.\n \n - `login` depends on `cookie`. Both use `part` \"Session cookie\".\n - `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request.\n","complete":true,"hunks":[{"path":".agents/skills/comprehende/references/example.md","oldStart":1,"oldLines":6,"newStart":1,"newLines":6,"header":"@@ -1,6 +1,6 @@","language":"markdown","lines":[{"kind":"ctx","oldNumber":1,"newNumber":1,"text":"# Example review.json"},{"kind":"ctx","oldNumber":2,"newNumber":2,"text":""},{"kind":"del","oldNumber":3,"newNumber":null,"text":"Pointers and prose only. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header."},{"kind":"add","oldNumber":null,"newNumber":3,"text":"Optional filled sample. The field shape in `SKILL.md` is enough to write a document. Paths come from `comprehende review`. When a file is split, `oldStart` and `newStart` come from that hunk's `@@` header."},{"kind":"ctx","oldNumber":4,"newNumber":4,"text":""},{"kind":"ctx","oldNumber":5,"newNumber":5,"text":"- `login` depends on `cookie`. Both use `part` \"Session cookie\"."},{"kind":"ctx","oldNumber":6,"newNumber":6,"text":"- `docs` is a separate part, last in `suggestedOrder`, because it could have been its own pull request."}]}]}]}
